# HANDOFF — 2026-08-26

**Branch `main`. Suite 2,180 passing, NO reds and NO expected-fails. Everything pushed.**

## 🏁 EVERY GATE IN THIS REPO READS ZERO

| Gate | Now |
|---|---|
| **SVG bytes, in-repo** | **0 of 424** — 1 ruled divergent, see below |
| SVG bytes, sibling | 0 of 356 |
| harvested corpus, four axes | 0 of 219 |
| `tune.lines` | 607,177 of 607,177 characters, 0 of 499 |
| warnings | 0 of 507 |
| `extractMeasures` | 0 of 265 files |
| everything else — parse values, render values, audio, MIDI, chord grids, timings, DOM, pixel, `tuneMetrics`, `voices-array`, `setupEvents` | 0 |

219 fixtures / 413 tunes in `tests/corpus-abcjs/fixtures`.

**The byte gate closed today.** Its last row — `abcts-directives-tune4`,
`%%stafftopmargin`, open since 2026-08-21 as "the deferred y-versus-pitch refactor" — is in
`PASSING` with the other 423.

⚠️ **THIS IS NOT A FINISH LINE, IT IS THE NORMAL CONDITION HERE.** No gate can name the next
defect and none has been able to for weeks. Every one of today's 27 findings came from
writing a shape neither corpus contains.

## WHAT PAID TODAY, AND IT IS THE METHOD RATHER THAN THE LIST

**27 defects, 26 commits.** Where they came from, in order of yield:

1. **TWO 36-SHAPE SWEEPS of odd-but-legal ABC**, run through both engines one shape at a
   time. 72 shapes, NINE differing, all nine resolved — eight ported rules and one ruled
   divergence. **This is the highest-yield instrument in the repo right now.** Write ~36
   shapes covering a territory (the first took rests, spacers, ties, decorations on odd
   hosts, inline fields mid-measure; the second tuplets, spanning decorations, extreme
   lengths, body directives), render both engines, diff, and only then reason.
2. **A FIXTURE FINDS WHAT A LADDER CANNOT.** Every sweep row that closed opened two or three
   more when it became a fixture, because a fixture renders a whole page and a ladder
   renders one shape. `abcts-rests-and-bars.abc` grew from 8 tunes to 16 that way.
3. **THE `ponytail:` LEDGER — still 100 entries, and it paid twice.** An entry that names a
   missing fixture is a WORK ITEM: `renderAbc({scale})` was "not read" (it was read, and
   wrongly, in a different quantity — 350x72 against abcjs's 1400x174), and "abcjs draws
   NOTHING past a breve … neither corpus does" was exactly right and had never been written.

## THE FIVE RULES THIS SESSION EARNED

⚠️ **A ROW WITHOUT A GATE CANNOT REPORT ITS OWN CLOSURE.** §5 (`clef=none`'s one stem) had
been open across three handoffs with a failed attempt recorded beside it. Nine control
shapes and the family's own spelling are byte-exact — it had closed under some later
arithmetic landing and NOTHING SAID SO, because the only thing carrying the row was prose in
a test comment. It is tune 12 of `abcts-key-modifiers` now.

⚠️ **WHEN A DECOMPOSITION HAS A BALANCING TERM, EVERY PART MOVES OR NONE DOES.** The last
byte row survived FOUR attempts because `originAdvances` closes on
`rest = flat - named - topPitch * STEP`, which silently absorbs any single-term change — or
doubles it, when the term is also added elsewhere (+40px, twice). All four parts moved
together and it closed in one edit.

⚠️ **BEFORE BUILDING PLUMBING, GREP FOR IT.** The dead-span mechanism two gates were asking
for already existed as `Score.unreadable`, built eight days earlier for a bare `#`. Two
producers were added and nothing else.

⚠️ **ASK THE WORKING PATH WHAT IT CARRIES.** Three attempts at the graced rest's 10px room
each guessed which term the line solve reads. Printing a graced NOTE's fields beside a
graced REST — `left=10.00` against `left=0.00` — settled it in one line.

⚠️ **A GREEN TEST CARRIES AN INFERENCE AS FIRMLY AS A COMMENT.** Two more flipped today
(`noteGlyph` refusing a duration abcjs approximates; a breve where abcjs draws no head), for
the fourth and fifth time on this branch.

## WHAT IS LEFT

### 1. THE `abselem` DECISION IS THE OWNER'S

Unchanged since 2026-08-21. `scripts/zzrv.ts` reads 10,570 rows differing ONLY by an absent
`abselem`/`staffGroup`. Publishing them means RETAINING THE `Layout`, which measurably
killed this suite's workers once (5.6s → 50-120s, a different test failing each run). A host
that wants the drawn element has `getSelectableArray`. **Do not start it without a decision.**

### 2. TWO RULED DIVERGENCES, BOTH abcjs PRINTING AN INTERNAL FAILURE INTO THE PAGE

`Docs/ABCJS-DIFFERENCES.md` carries both, and `svg-bytes`'s `DIVERGENT` list names the slug:

- **a note longer than a breve** draws `<text stroke="#ff0000">pitch is undefined</text>`
  and reserves a 4-pitch CHORD LANE, 19.18px, on a tune with no chord symbol
  (`create-note-head.js:24-25`). Everything else about it is reproduced.
- **a grace flag on an invisible rest** is drawn at `M NaN`.

Both are one small change each if the owner wants byte parity over valid output. **That is a
decision, not a defect.**

### 3. THE `ponytail:` LEDGER — 100 ENTRIES

`grep -rn "ponytail:" src tests scripts`. Two paid today. Read each entry against the
OUTPUT, not against the code it sits beside: the `{scale}` entry was true when written and
had stopped being true.

### 4. THE SWEEP HAS TERRITORY LEFT

Neither sweep touched: multi-voice and `%%score` shapes, `&` overlays, clef changes mid-line,
transposition, lyrics with odd alignment, `%%`-directives in the body beyond the five tried,
tempo shapes, or anything at the boundary between two features. 36 shapes is an hour.

## THE HARNESS

| Script | Prints |
|---|---|
| `D=<dir> O=<dir> npx tsx scripts/zzdecs.ts` | our SVG for every `<i>.abc` in a directory |
| `ABCJS_VERSION=6.7.0 node ../abcMusicKit/Tools/abcjs-debug/dump-svg.js --file x.abc --output x.svg` | abcjs's, at the goldens' own `{staffwidth: 670}` |
| `node scripts/harvest-abcjs-goldens.mjs --only <slug>` | regenerate ONE fixture's byte goldens |
| `node scripts/harvest-abcjs-extract-measures.mjs` | the `extractMeasures` oracle — **regenerate it when a fixture grows** |
| `ABCTS_H=1` | our height terms, per staff |
| `ABCTS_PROBE=1` | who set this staff's extent |

⚠️ **`ABCJS_VERSION` IS NOT OPTIONAL — `dump-svg.js` DEFAULTS TO 6.6.3.**

⚠️ **AND `cd` MATTERS.** It bit again today: a probe run reported five clean rungs that were
really "the reference never rendered", because the shell had reset and the relative path
resolved one directory up. **Check the abcjs SVG exists before believing a column of EXACTs.**

**THE SCRATCHPAD abcjs IS AT `/tmp/gp/abcjs`** — rebuilt today, currently carrying probes in
`incTop` (`ZZTOPA`), `_addChild` (`ZZCB`, `ZZCHA`), `calc-height` (`ZZCH`), `voice-elements`
(`ZZLX`), `set-upper-and-lower-elements` (`ZZAE`, `ZZYB`, `ZZSB`), `decoration.js` (`ZZARP`),
`abstract-engraver` (`ZZGS`, `ZZEND`). Every one is gated on its own env var. `zzprobe-dump.js`
sits beside `dump-svg.js` in `../abcMusicKit/Tools/abcjs-debug` with `abcjsPath` repointed —
⚠️ **that is a DIFFERENT repo and `zz-dump-probe.js` there is TRACKED**, so never `add -A` in it.

## THE METHOD, UNCHANGED

**Read the named abcjs function FIRST, then instrument to confirm it — neither half works
alone.** Run every command from `/Users/lrettberg/ICMLabs/Code/abcts`. Run `npx tsc --noEmit`
BEFORE every commit. After regenerating ANY golden, diff it against `HEAD` and assert only
NEW KEYS appeared. Re-ratchet every gate's PASSING list in the same commit. Commit and push
after every landing; never `--force`. Omit `Co-Authored-By` trailers in this workspace.

⚠️ **ANOTHER AGENT MAY BE IN THIS WORKTREE.** One was on 2026-08-25: it committed on top of
mine, swept up my in-flight edits, and reverted a change of mine with a measured reason.
Nothing broke, but treat any single commit's file list as approximate, and never
`git checkout` a whole file you did not just write.

# Handoff — 2026-08-03c

Full state is in `CHECKPOINT-2026-08-03c.md`. This is the prompt and the shortest statement
of where to start.

---

## THE STANDING ORDER

**100% parity with abcjs on every tune — the 41-fixture corpus, the 174-tune harvested
corpus, Gonzato, and the audio feature set. Work until it is reached, without stopping for
approval.** Checkpoint and hand off as you go so no context is lost.

---

## THE PROMPT

```
Continue to 100% abcjs parity in the abcts repo (~/ICMLabs/Code/abcts, branch
`geometry/vertical`, git@github.com:ICMLabs/abcts.git).

FIRST: read Docs/CHECKPOINT-2026-08-03c.md, then Docs/VERTICAL-ARC.md, then
ARCHITECTURE.md, then CLAUDE.md.

TWO REPOS, BOTH WITH A BRANCH CALLED `main`. `Code/abcts` is ours.
`Code/abcMusicKit` is a read-only sibling holding the vendored abcjs source, the
41-fixture corpus and its goldens — never commit there, and if you instrument it,
restore it and verify `git -C ../abcMusicKit status --short` is empty before you
finish. Note that `git status --short` EXITS 0 with output, so `&& echo CLEAN`
lies — read the output.

THE GOAL IS 100% PARITY, bugs and poor rendering choices included. A test that
asserts abcjs is wrong is a test to correct, and FOUR have been.

THE ALGORITHM IS IN ABCJS. Read the named function; do not infer it from a diff.
Most of what has landed is a port of one — merge's staff assignment, getClef's
prefix match, setCurrentVoice's line scan, the backslash preprocessing, the
accidental columns, the multi-measure rest's element. None could have been
guessed from the output.

BUT FINISH WITH A PROBE. THREE TIMES the source has predicted a number its own
SVG denies — putChordInLane's per-lane growth, setLane's invertLane, and Z24's
duration, which reads as `count * getBarLength()` and is a flat 1. And one rule
is not in the source at all: a [V:x] naming the voice already current is a no-op,
which only instrumenting shows.

START HERE, EVERY TIME:
  npx vitest run tests/corpus-abcjs-ranked.test.ts && cat /tmp/abcts-corpus-ranked.txt
Every fix on this branch came off that table. Read the SHAPE, not the total.

NEVER RAISE A CEILING TO MAKE A CHANGE PASS. One red: ragtime-nightingale's oy at
1.58 against 0.59. Anything else failing is yours.
```

---

## WHERE TO START

### 1. THE FONT DIRECTIVES — the next cluster, four of the top ten

`%%gchordfont` and its seventeen siblings are parsed nowhere.
`visual-tablature-17-stretchlast` is `dy` 300.3 on `%%gchordfont` alone;
`visual-options-01-fonts` is `oy` −153.5 on all eighteen plus `%%header`/`%%footer`; the two
`selection-test` fixtures are 69.9 each.

abcjs's mechanism is ONE place: `getFontAndAttr.calc(type, klass)` reads
`abcTune.formatting[type]`, and every size in the engraver comes from it. The grammar is
`%%<type>font <family> <size> [box]` — the same shape `%%vocalfont` already parses into
`LyricFont`, so the model can grow a `fonts` record keyed by type.

**It can change MID-TUNE** — that fixture sets `%%gchordfont` four times between music
lines — so it is per-element state, not a tune-level constant. A boxed font also measures
`height + padding * 4`, which `%%partsbox` already proved.

### 2. The horizontal model is a faithful PORT; the divergences are numbers in it

`lineAt` reproduces `layoutStaffGroup` + `voice-elements.js` element for element. So a
horizontal divergence is almost never the loop — it is a `w`, an `extraw`, a `minspacing`
or a duration. Instrument `layoutOneItem` to print
`type / dur / w / extraw / minspacing / x` and compare item by item; three of this
session's four horizontal fixes came out of exactly that, one run each.

Do not "fix" our `spacingReference` — our units are abcjs's × √2 and our `spacingScale` its
30 ÷ √2. The solve uses the product, so both are consistent.

### 3. Then, in order

- `visual-layout-09-endings` (496.9) — `%%voicecolor`, and `dx`-led.
- `visual-transpose-output-01` (106.8, pure `dx`) and `synth-flattener-22` (84.5, pure
  `dx`) — chord-symbol and note widths, no directives involved.
- `%%barnumbers` / `%%measurenb` / `%%setbarnb` — the bar number is a POINT at
  `vert + height/STEP` where `vert` is 13.5 on a wide number over a treble clef and 11
  otherwise (`abstract-engraver.js:946-953`), added by `addFixed` so it enters the ink.
- The two content gaps: a grace note before a `y` spacer, and the leading-header split
  (`book_parser-04-wed`, the one TUNE COUNT mismatch left).
- Then Gonzato, then audio.

---

## STILL NEEDING A DECISION, NOT A COMMIT

**The gate hides failures.** All eight per-fixture assertions live in one `it`, so the first
to fail ends it. Collecting all four axes before asserting is the honest fix and will show
more red before it shows less.

**Mid-tune free text is dropped.** A `%%begintext` between two music lines is not drawn:
abcjs's justification rule is per LINE, so marking it as trailing justifies the wrong one,
and placing it properly needs free text to be a LINE rather than a property of the tune.

**A grace note before a `y` spacer is dropped.** `Rest` has no `graceNotes` field and the
model says so deliberately. abcjs disagrees for a SPACER.

---

## WHAT NOT TO CHASE

`ragtime-nightingale` dy 58.1 — two mis-paired heads over a real spread of ~5px.
`vree-grace-notes` dy 11.6 / dx 32.5 — grace emission order. `little swallow` dx — the
goldens' ASCII-only width table at 8px per CJK character. ragtime's five remaining staff
extents — horizontal in origin, see `-08-03b`.

A fixture with ONE paired notehead has no spread: its `dy`/`dx` of 0.00 are arithmetic, not
parity.

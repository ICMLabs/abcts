# HANDOFF — 2026-08-14

## The session prompt

```
You are continuing abcts development in /Users/lrettberg/ICMLabs/Code/abcts. Work
autonomously — do not stop to ask for approval or report status. Keep landing changes
until you genuinely cannot make further progress, then report.

THE STANDING ORDER, in Lance's words: abcts exists to build an abcjs-modern whose output —
the SVG FILE and the AUDIO — is 100% BYTE-EQUAL to abcjs 6.7.0. A tolerance is not a
compromise to be balanced against effort; it is A DEFECT THAT HAS NOT BEEN WRITTEN DOWN
YET. Anything you decline to reproduce goes in Docs/ABCJS-DIFFERENCES.md with its evidence
and its slug goes in svg-bytes.test.ts's DIVERGENT list. That list is EMPTY and stays empty.

INSTRUMENT BOTH ENGINES. You have Lance's standing authority to add as much instrumentation
to abcjs AND to abcts as it takes. Reaching for a probe is the FIRST move, not the last
resort. PRINT THE SAME QUANTITY FROM BOTH ENGINES IN ONE SITTING — a source read gives a
hypothesis, only the pair of numbers gives the answer.

Read Docs/HANDOFF-2026-08-14.md FIRST, then Docs/CHECKPOINT-2026-08-14.md, then CLAUDE.md.

Run every command from /Users/lrettberg/ICMLabs/Code/abcts. `cd` elsewhere silently resets
the shell to the workspace root, where vitest collects every sibling repo.
Run `npx tsc --noEmit` BEFORE every `git commit`. COMMIT AND PUSH AFTER EVERY LANDING.
Instrument a SCRATCHPAD COPY of abcjs at /tmp/gp/abcjs — NEVER ../abcMusicKit.
```

---

## 1. THE STATE

| Gate | Now |
|---|---|
| **SVG bytes, in-repo corpus (178 rows)** | **0** |
| **SVG bytes, 41-fixture corpus (113 rows)** | **27** |
| Audio events / timings / element timings / chord grids / MIDI | 0 of 72 / 0 of 38 / 0 of 13 / 0 of 23 / 0 of 3 |
| Harvested geometry / pixel targets / DOM contract | 0 of 174 / 0 of 120 / 0 of 25 |

**EVERY TABLE THAT EXISTED YESTERDAY IS AT ZERO.** The one open gate is the one built today,
and it opened at 38 of 113 over goldens that had sat unread since April. Eleven landings
took it to 27.

Suite 1416/1417. **THE ONE RED IS NOT OURS** — `content-parity`'s `S7-voices` is an
uncommitted fixture edit in `../abcMusicKit` whose goldens were never regenerated. Do not
bisect it (`CHECKPOINT-2026-08-12.md` §5); the new byte gate EXCLUDES it by name.

---

## 2. WHERE TO START

**`CHECKPOINT-2026-08-14.md` §4 is the work list**, classified by first difference. In order
of what a session buys:

### 2.1 The structural rows — one fixture each, and each one a named rule

`S6-keys-tune3` draws `accidentals.nat` where abcjs draws `accidentals.flat` in a KEY
SIGNATURE; `S4-bars-repeats-tune1`'s ending bracket is at 203.41 against 58.05;
`zocharti-loch`'s whole rest is at 133.75 against 116.84; `S5-directives-tune4` is missing a
`<text>` after a stem. Each is one row of the table and each is visible in its first
difference — probe with `S=<slug> T=<tune> npx tsx scripts/zzs1.ts`.

### 2.2 THE DOT'S `dx` — measured, NOT landed, and it is two rows

`CHECKPOINT-2026-08-14.md` §4.1: `S3-note-syntax-tune17` and `happy-birthday` differ on ONE
ULP of a `dots.dot` x, and the cause is the same association the key signature's `dx` had.
What is missing is which of our `headX`/`rightmost`/`offsetAt` corresponds to abcjs's
`abselem.x`, `notehead.w` and `dotshiftx` — **print them from abcjs beside `ABCJS_GDX`
before writing any arithmetic.**

### 2.3 The ULP family

`S8-layout-tune10` is `862.3369999999996` against `862.3369999999998` and nothing else in
249,348 bytes; `-tune7`, `-tune9`, `S6-keys-tune2`, `S3-note-syntax-tune17`,
`happy-birthday`, `frere-jacques` (`854.4015` against `854.4205`) and
`ragtime-nightingale` (ONE ULP of `height` in 2,007,011 bytes) are the same shape.
`CHECKPOINT-2026-08-11b.md` §5 has the four probes that count tokens by axis; the rule is
that **an ASSOCIATION IS A DECISION** and abcjs's grouping is part of the port.

---

## 3. THE HARNESS

**abcjs, instrumented, one fixture** — a SCRATCHPAD COPY at `/tmp/gp/abcjs`, NEVER
`../abcMusicKit`:

```bash
NODE_PATH=../abcMusicKit/Tools/abcjs-debug/node_modules \
ABCJS_PATH=/tmp/gp/abcjs ABCJS_MYPROBE=1 \
node /tmp/gp/walk.js --file <path to .abc> 2>&1 | grep MYPROBE
```

Gate every probe on its own env var. `/tmp/gp/abcjs` carries about thirty; the four added
today are `ABCJS_CST` (the key's whole journey from `createStaff` to the engraver),
`ABCJS_CLEF` (a clef's `clefPos` and declared box), `ABCJS_ABSY` (each staff's `absoluteY`,
`top` and `bottom` AFTER `setUpperAndLowerElements`) and `ABCJS_NM`, which now prints
`renderer.y` beside each nonMusic row — the page cursor row by row.
`CHECKPOINT-2026-08-13.md` §5 tabulates the earlier ones.

**Ours:**

```bash
ABCTS_FIX=<slug> [ABCTS_TUNE=n] npx tsx scripts/zzpr.ts   # in-repo corpus -> /tmp/ours.svg
S=<slug> [T=<tune>]             npx tsx scripts/zzs1.ts   # sibling corpus, first difference
S=<slug>  T=<tune>              npx tsx scripts/zzs2.ts   # sibling corpus -> /tmp/ours.svg
```

**vitest swallows `console.log`**, so probe through `tsx`, not through a test.
`ABCTS_PROBE=1` adds the staff extents with the contributing source line.

**THE TWO RATCHETS.** `tests/svg-bytes.test.ts`'s `PASSING` names all 178 in-repo rows and
`tests/svg-bytes-sibling.test.ts`'s names the 82 sibling rows that are exact. Both GROW and
never shrink. Regenerate after every landing:

```bash
node /tmp/exact.mjs      # in-repo: reads /tmp/abcts-svg-bytes-ranked.txt -> /tmp/exact.txt
node /tmp/exact-sib.mjs  # sibling: reads /tmp/abcts-svg-bytes-sibling-ranked.txt
# then splice each into its test's PASSING
```

Both enumerate `<slug>.svg` AND `<slug>-tune{i}.svg`. If either script is missing, rebuild
it — it is fifteen lines and the shape is in `CHECKPOINT-2026-08-14.md` §5.

---

## 4. THE RULES

- **PRINT THE SAME QUANTITY FROM BOTH ENGINES IN ONE SITTING.** A source read gives a
  hypothesis; the pair of numbers gives the answer. Six careful chains of source reads on
  this branch have predicted something abcjs's own output denies — the latest being
  `-08-12` §3.3b's conclusion that a `V:… clef=` never reaches a drawn key signature, which
  abcjs's own parse tree contradicts in one line.
- **A GATE'S REACH IS A PROPERTY OF ITS ENUMERATION, NOT OF ITS COMPARISON.** Four times
  now, and every time a NOTE explaining the hole away is the reason nobody re-measured it.
  Before concluding a gate is exhausted, ask what evidence EXISTS.
- **A RULE PORTED IN HALF LOOKS LIKE A RULE THAT DOES NOT APPLY**, and the narrowing gets
  written down as a finding. Two of today's landings were exactly that.
- **WHEN A CHANGE TO AN INPUT MOVES NOTHING, THE OUTPUT IS NOT READING THAT INPUT.**
- **READ THE BASELINE DIFF'S SHAPE**: a new feature only ADDS, a reorder is a pure
  PERMUTATION, REMOVALS mean something broke.
- **A HALF-UNDERSTOOD FIX IS WORTH LESS THAN A WRITTEN-DOWN MEASUREMENT.**
- **A GATE'S REPORT FILE IS NOT ITS RESULT.** `/tmp/abcts-*.txt` outlives the run.
- Never `push --force`, never `pull --rebase` unattended; if a push is rejected as
  non-fast-forward, STOP and report it. Omit `Co-Authored-By` trailers in this workspace.

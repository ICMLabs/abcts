# Handoff — 2026-08-04b

Full state and findings 41–50 are in `CHECKPOINT-2026-08-04b.md`; the earlier ledger is
`CHECKPOINT-2026-08-03d.md` (16–40) and the METHOD is in `CHECKPOINT-2026-08-04.md`. This is
the prompt and where to start.

---

## THE PROMPT

```
Continue to 100% abcjs parity in the abcts repo (~/ICMLabs/Code/abcts, branch
`geometry/vertical`, git@github.com:ICMLabs/abcts.git).

THE STANDING ORDER: 100% parity with abcjs on EVERY tune — the 41-fixture corpus,
the 174-tune harvested corpus, Gonzato, and the audio/MIDI feature set. Work
until it is reached without stopping for approval or status. Checkpoint, hand off
and compress as you go so no context is lost.

FIRST: read Docs/CHECKPOINT-2026-08-04b.md, then Docs/CHECKPOINT-2026-08-04.md
for the METHOD and the expensive lesson, then Docs/CHECKPOINT-2026-08-03d.md for
the findings ledger 16-40, then ARCHITECTURE.md, then CLAUDE.md.

TWO REPOS, BOTH WITH A BRANCH CALLED `main`. `Code/abcts` is ours.
`Code/abcMusicKit` is a read-only sibling holding the vendored abcjs source, the
41-fixture corpus and its goldens — never commit there, and if you instrument it,
restore it and verify `git -C ../abcMusicKit status --short` is empty before you
finish. That command EXITS 0 with output, so `&& echo CLEAN` lies — read it. And
never write `--output /dev/null` in a loop over fixtures: a multi-tune file makes
`-tuneN` files in that repo. `cd` does NOT persist between tool calls, and a `cd`
inside a compound command leaves the shell somewhere else for the NEXT one —
running `npx vitest` from abcjs-debug silently runs THAT package's suite and
leaves the last table in place, which reads as "the change did nothing".

THE GOAL IS 100% PARITY, bugs and poor rendering choices included. A test that
asserts abcjs is wrong is a test to correct, and SEVEN have been.

A LADDER OF CONTROL TUNES, THEN THE NAMED FUNCTION, THEN A PROBE. Ten rules
landed on 2026-08-04 and not one came off a diff: write four or five tunes into
/tmp/abcts-probe/, each one FEATURE longer than the last, generate abcjs's SVG
for each, run tests/controlled-pair.test.ts and read the STAFF TOPLINES. The rung
where the number appears names the INTERACTION, not the feature — `"D7"…|1…`
needed five rungs to say "a chord AND an ending", which is a BRANCH in
set-upper-and-lower-elements.js and invisible in either feature alone.

THEN OPEN THE NAMED FUNCTION. getKeyAccidentals2, createClef, getMinWidth,
containsLyrics, getMode, calcWidth, translateChord, addChord, getChordDim,
createTimeSignature — every fix is a port of one, and none could have been
guessed from the output. BUT FINISH WITH A PROBE: four times the source has
predicted a number its own SVG denies.

AND "THE GATE CANNOT SEE THIS" IS NOT "THE GOLDEN IS WRONG", AND NEITHER OF
THOSE IS "SO LEAVE IT". THERE ARE NO GOLDEN LIMITATIONS — all four that were
filed as such are closed, two of them the generator's own text metrics, which
byte parity obliges us to reproduce.

START HERE, EVERY TIME:
  npx vitest run tests/corpus-abcjs-ranked.test.ts && cat /tmp/abcts-corpus-ranked.txt
Read the SHAPE, not the total — and read the STAFF TOPLINES, not only the
notehead average. tests/notehead-dx.test.ts prints every paired notehead: a dx
that RESETS at each system is a prefix, one that grows inside a system is
spacing, a constant one is an offset.

NEVER RAISE A CEILING TO MAKE A CHANGE PASS. One red: ragtime-nightingale's oy at
0.656 against 0.59. Anything else failing is yours. And when a fixture improves,
LOWER its ceiling in the same commit.

NEXT, in order: the remaining font SITES (voicefont, measurefont, repeatfont,
annotationfont, tripletfont, tempofont) and %%header/%%footer, which close
visual-options-01; then %%barnumbers/%%measurenb/%%setbarnb; then %%setfont; then
visual-selection-03's seven clefs; then %%percmap and %%MIDI channel 10; then
%%voicecolor; then Gonzato; then audio.
```

---

## WHERE TO START

### 1. THE FONT SITES — `visual-options-01-fonts`, 37.04 and the top of the table

`score.fonts` holds every `%%<type>font` already, and the top-text block reads them. The
DRAWING sites still read constants:

| type | site | notes |
|---|---|---|
| `voicefont` | the voice-name label, hardcoded `17 / 7.75` | also feeds `voiceNameWidthPx`, whose table is TNR Bold 17 |
| `measurefont` | bar numbers | see 2 |
| `repeatfont` | the volta's DRAWN number — its RESERVE is already right | |
| `annotationfont` | annotations | |
| `tripletfont` | tuplet numbers | |
| `tempofont` | the tempo mark's text | |

`%%header` and `%%footer` are parsed and not drawn at all. `visual-options-01` sets all
eighteen fonts with `box` on every one, so the box padding lands on each row too.

### 2. `%%barnumbers` / `%%measurenb` / `%%setbarnb`

Four fixtures, all `oy`-only and all under 11px: `visual-wrap-03`, `visual-tablature-04`,
`visual-misc-01`, `visual-misc-02`. The bar number is a POINT at `vert + height/STEP`, with
`vert` 13.5 on a wide number over a treble clef and 11 otherwise
(`abstract-engraver.js:946-953`), added by `addFixed` so it enters the ink. Note
`_addChild`'s exception: a `barNumber` on a CLEF element does NOT push the top
(`absolute-element.js:182-187`).

### 3. `visual-selection-03` — seven clefs, `oy` −11.6 and nothing else

`K:C clef=treble`, `+8`, `-8`, `tenor`, `bass-8`, `bass+8`, `bass`, one bar each. The fixture
IS a ladder; take the rungs one at a time against `tests/controlled-pair.test.ts`.

### 4. `visual-tablature-17`'s remaining dx 33.9

Vertical is closed on it (dy 0.0). What is left is horizontal, with `%%stretchlast` and five
boxed `%%gchordfont` sizes — the chord's WIDTH also grows by `padding * 4` for a boxed font
(`get-text-size.js:46-48`) and only its HEIGHT was ported.

---

## WHAT NOT TO CHASE

**Still empty of geometry.** The only thing worth not chasing is arithmetic: a fixture with
ONE paired notehead has no spread, so its `dy`/`dx` of 0.00 are arithmetic, not parity.

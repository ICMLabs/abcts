# HANDOFF — 2026-08-22b

**Branch `main`. Suite 2,078 tests, 70 files, NO reds and no expected-fails. Everything
pushed.**

Twenty-three commits. **TWENTY-SIX DEFECTS**, and every one came from the method the previous handoff
named: **enumerate the REFERENCE's own list, not our notes.** Five lists were taken —
abcjs's five decoration tables (96 names), its `V:` modifier switch (96 labels), its eleven
`midiCmdParam*` tables (53 sub-commands), its `K:` switch (31 labels, and NOT the same list
as `V:`'s), and finally its own TEST INPUTS, counted per file against the fixtures already
here.

**Two of the fifteen were found by a CONTROL rather than by any variable**, which is the
rule this session paid for twice over.

**START HERE:** WHAT IS LEFT §1 is still the single byte row from 2026-08-21, §2 is still
the owner decision, §3 names what the four enumerations left behind — one geometry row with
its cause identified, one feature, two diagnostics, and the last unenumerated list.

---

## THE GATES

| Gate | Now |
|---|---|
| abcjs API surface | 0 of 64 absent |
| **SVG bytes, in-repo** | **1 of 363 fixtures** — the 2026-08-21 row alone, see §1 |
| SVG bytes, sibling | 0 of 356 tunes |
| `tune.lines` characters | 590,636 of 590,636, 0 of 472 tunes |
| `parseOnly` VALUES | 0 of 13,015 rows |
| RENDERED values | 0 of 13,015 rows |
| `tune.deline` | 0 of 944 cases |
| `synth.sequence` | 5,425 of 5,425, 0 of 209 |
| **`makeVoicesArray`** | **4,768 of 4,778, 1 of 209** — `parsing-x10`, see §3a |
| `tune.setupEvents` | 3,366 of 3,366 |
| `TimingCallbacks` | 4,696 of 4,696 |
| **`tuneMetrics`** | **1 of 255** — `parsing-x10`, see §3a |
| `extractMeasures` / `selectables` | 0 |
| **warnings** | **1 of 487** — DIAGNOSTIC-only, see §3e |
| corpus / pixel / DOM / animation / audio / MIDI / chord grids / editor / synth | 0 |

⚠️ **A GATE'S REPORT FILE OUTLIVES ITS RUN.** `/tmp/abcts-*.txt` is stale until you re-run
the test that writes it.

⚠️ **AND A RED FULL-SUITE RUN IS NOT NECESSARILY RED.** Confirmed again: one run reported
`pixel-parity > S3-note-syntax-tune21` and `svg-bytes-sibling` failing and took five
minutes; both passed in isolation and the next full run was green in 23 seconds. Use
`npx vitest run --maxWorkers=2` and re-run before believing a red.

---

## WHAT IS LEFT

### 1. ONE BYTE ROW, AND IT IS THE ONE FROM 2026-08-21

`abcts-directives-tune4` — `%%stafftopmargin 30` on a tune WITH a title — differs in the
last bits of the root `height` (`201.76700000000002` against `201.767`). It is the
y-versus-pitch asymmetry a staff carrying a HEADING BLOCK already has, recorded at
`verticalExtent` since 2026-08-11, and the deferred refactor rather than a new defect.

`abcjs-visual-parsing-x10` stood beside it for one day and is CLOSED (§4.11).

### 2. THE `abselem` DECISION IS STILL THE OWNER'S

`scripts/zzrv.ts` reads 10,570 rows differing ONLY by an absent `abselem`/`staffGroup`.
Publishing them means RETAINING THE `Layout`, which measurably killed this suite's workers
once (2026-08-16: 5.6s → 50-120s). A host that wants the drawn element has
`getSelectableArray`. **Do not start it without a decision.**

### 3. WHAT THE FIVE ENUMERATIONS LEFT

**a. THE ULP CLASS IS CLOSED, AND ONE BYTE ROW IS LEFT IN THE WHOLE GATE.** All three
rows are fixed. The tempo rung (§4.10) closed the first; a key signature's own WIDTH closed
the other two — `abselem.w` is `max(dx + w)` over the `addRight` children and both
`layoutKeySignature` and `layoutKeyChange` took the cursor and subtracted the trailing gap
back off it, so `(a + 2) - 2` gave `15.649999999999999` for abcjs's `15.65` and every
element after a key signature inherited it (§4.11).

**b. `K:C clef=none`'s ONE STEM — THE LAST GEOMETRY ROW, AND IT IS NOT ON ANY GATE.** The
K: sweep reads 31 of 33; `clef=none` (twice, the same tune) keeps one stem whose centre is
`origin + (headInk - half)` and whose drawn edge the emitter rebuilds as
`(centre - half) + half`, three operations where abcjs has the single
`abselem.x + heads[0].w`: `58.604999999999996873` against `58.605000000000003979`.

⚠️ **AND THE STEM'S OWN EXPRESSION IS NOT THE OBSTACLE — THREE ATTEMPTS SAY SO.** Do not
spend a fourth on it.

1. `headX + baseShift + headInk` carried as an anchor field, every stem: broke all 33 rows.
   `baseShift` is the BEAMED bridge and a beamed stem's x is not that sum at all — the beam
   pass rebuilds it from `furthestHead` in `createStems` (`layout/beam.js:117-122`).
2. The same field on UNBEAMED stems only, which IS abcjs's expression: broke all 33 again,
   because **`placeElement` translates a line's `x1`/`x2` and leaves the field behind.** Any
   carried anchor needs translating by `placeElement`, `displaceHeads` and the beam pass.
3. No field at all — build the CENTRE as `((headX + baseShift) + headInk) - half`, so the
   emitter's `centre + half` recovers abcjs's edge. `(a - half) + half === a` for every
   value the corpus produces, checked. This is translation-safe and correct, and it changed
   NOTHING: the sweep stayed at 31 of 33 and the whole suite stayed green.

**Because the drift is upstream, in the NOTE's x.** Ours is `48.7950000000000017` where
abcjs's is `48.795000000000003979`, and no grouping of the terms this engine spends —
`15`, the time signature's `11.795`, the `10` minspacing and the chord symbol's `12` of
left overhang — reaches abcjs's value. The next move is to instrument abcjs's
`layoutOneItem` on that tune and read the `er`/`extraWidth` arm, NOT to touch the stem.

**c. `V:… scale=` AND `cue=` — THE FEATURE.** Both are `voiceScale`, which abcjs DOES save
per voice (unlike the style — see §4.5) and which scales noteheads, stems, flags, beams,
rests and graces together (`create-note-head.js:12`, `abstract-engraver.js:740, 495, 405,
601`). `cue=on` is `scale = 0.6`. This engine has a scale for GRACE notes only; threading
it is a session's work. Measured: `scale=1.5` draws the untouched baseline.

**d. `wrap` — A SECOND FEATURE, WITH A NINE-CASE JSON ORACLE.** `renderAbc(…, {wrap})`
returns `explanation` (widths, `lineBreakPoint`, `minLineSize`, `minWidth`) and
`lineBreaks`; this engine returns NEITHER — measured, both `undefined`. `tests/visual/
wrap.test.js` asserts both across nine tunes, five of whose inputs are already in the
corpus. Same shape as `chordGrid` was.

**e. ONE WARNING THIS ENGINE DOES NOT EMIT**, geometry-exact and diagnostic-only.
`abcts-key-modifiers-tune9` wants abcjs's two `Unknown parameter:` rows for the `=f` that
follows a `clef=`. ⚠️ **AND IT NEEDS AN EXPLICIT COLUMN THREADED THROUGH THE DIAGNOSTIC**:
the mapping derives the column as `text.indexOf(token)`, so two warnings on the same `=`
cannot both land — the first would take the `=` inside `clef=`. Everything else on that
gate is closed (§4.12).

**f. WHAT IS LEFT OF THE LAST LIST.** abcjs's 30 test files by ASSERTION TARGET: the INPUTS
are now swept (§4.8), and of the assertion targets the audit in `CHECKPOINT-2026-08-09.md`
marked unharvested, only `wrap` (§3d) and `charPos` remain — and `charPos` is done (§4.7).
Everything else on that table has a gate now.

**g. THE `ponytail:` LEDGER** — `grep -rn "ponytail:" src tests scripts`. Its fourth sweep
found nothing, which is what sent the fifth pass at abcjs instead. Three entries added
across this session.

---

## WHAT CLOSED, AND THE RULE EACH ONE PAID FOR

### 1. THE DECORATION TABLES — 96 NAMES, 93 COMPARABLE, THREE WRONG

- **`!slide!`'s EXTENT.** Its curve was already byte-exact and its reserve was missing, so
  only the root `height` showed it — 154.552 against 139.052. ⚠️ **AND THE EXTENT BOWS THE
  OTHER WAY FROM THE DRAWING**: `getYBounds` branches on `isTie`, which nothing sets at
  layout time, so it takes `calcSlurDirection`, where a slide's undefined `voiceNumber`
  falls to a `stemDir` read off two BLANKS that have none — `above` is FALSE, always. The
  drawing recomputes `isTie` and reaches `calcTieDirection`, which says above.
- **`~(` / `~)` ARE `glissando(` / `glissando)`** — case labels sharing an arm in the
  WRITER's switch (`decoration.js:341-348`), never spelled upstream.
- **A GLISSANDO INSETS BY HALF THE ELEMENT, NOT HALF THE NOTEHEAD** — `anchor1.w` off the
  ABSOLUTE element, which an unbeamed up-stem flag grows past the head (15.902 against
  9.81). `NoteAnchor.absWidth` carries abcjs's `max(dx + w)` over the `addRight` children
  only: heads, unbeamed flag, dots.

⚠️ **`!glissando)!` AND `!~)!` WITH NOTHING OPEN CRASH abcjs**, so no golden can exist for
either. Ours renders both; a crash is not parity.

### 2. `checkLastBarX` — FOUND BY A CONTROL, NOT BY A VARIABLE

The V: probe reported **0 of 96 identical**, which is not a result about modifiers. The
two-voice control itself diverged, on the one place abcjs departs from the pure time cursor
this engine reproduces: a voice that runs out of music early left its closing rule 30px
short. ⚠️ **IT IS A ONE-DIRECTIONAL FORWARD PASS, WHICH IS abcjs's OWN BUG AND IS KEPT**;
`abcts-last-bar-tune1` gates it with the longest voice in the MIDDLE. ⚠️ **AND ONLY THE
RULE MOVES, NOT THE ELEMENT** — moving the element is byte-identical on the drawing and
wrong on the x every host reads back, which three ratcheted `makeVoicesArray` rows caught
and the byte gate could not.

### 3. THE V: MODIFIERS — THREE

`^8`/`_8` as clef octave suffixes (⚠️ `+16`/`-16` are NOT — the rule tests for an `8`);
`cl=` as `clef=`; and the staff-connecting rule starting at the FIRST staff's own top line
rather than at pitch 10 (⚠️ a ONE-LINE staff is the exception and keeps the full span).

### 4. `%%MIDI` — TWO, AND THE FIRST ENUMERATION AIMED OFF THE SVG

All 53 sub-commands, one tune each. **NOT ONE OF THEM MOVES THE DRAWING** — the byte gate
is 0 of 56 on the fixture and `synth.sequence` 0 of 191 — so `tune.formatting.midi` is what
states the defects, which is exactly why the list was worth taking off the SVG gate.
`midiCmdParam1String` pushes the RAW TOKEN (`ptstress 1` is `["1"]`, not `[1]`) and
`midiCmdParamFraction` pushes TWO INTEGERS (`grace 1/4` is `[1, 4]`, not `["1/4"]`).

⚠️ **AND `%%MIDI` TOKENISES WITH `alphaUntilWhiteSpace`, WHICH IS WHY `1/4` SPLITS AND
`d2z/d/d` DOES NOT.** A first cut split on `/` unconditionally and `flatten-drum`'s
`%%MIDI drum d2z/d/d 35 38 38 100 50 50` denied it — the pattern became three tokens and
the third line's drum track lost its beats. **THE FIXTURE THAT CAUGHT IT WAS ALREADY IN THE
SUITE.**

### 5. `V:… style=`, AND THE LEAK THAT KEEPS IT UNFINISHED

The arm that reads `V:`'s modifiers had CLAIMED to read `style=` since it was written and
did not. All five shapes are byte-exact on ONE voice. ⚠️ **AND IN abcjs IT LEAKS INTO EVERY
VOICE ENGRAVED AFTER IT** — `pushCrossLineElems` saves the slurs, ties, endings, COLOUR and
SCALE per voice and NOT the style (`abstract-engraver.js:92-107`). Measured both ways:
`V:1 style=x` gives the lower staff x heads it never asked for, and `[V:2 style=x]` reaches
voice 1's SECOND line. Not reproduced — the leak is a running value in (line, staff, voice)
ENGRAVING order where this parser resolves per voice at parse time, and source order is
engraving order only for interleaved `[V:…]` lines.

### 6. THE K: MODIFIERS — FIVE, AND THE SWITCH IS NOT V:'s

- **`C`, `F` AND `G` NAME CLEFS, IN EITHER CASE** — ⚠️ only after `clef=`; a bare `K:C G`
  is an unknown parameter, and reading a bare letter would make the KEY's own letter a clef.
- **A MODIFIER'S OWN `=` IS ONE OF THE THREE ACCIDENTAL SIGNS.** `K:C clef=C` read a
  natural on C and widened the staff by 15.4px. ⚠️ **AND THE FIX IS A STOP, NOT A SKIP** —
  accidentals are a PREFIX: `K:C =f clef=alto` draws the natural, `K:C clef=alto =f` draws
  nothing. ⚠️ **AND NOT EVERY `x=y` IS A MODIFIER** — `K:C=c` really is a natural.
- **`m` IS MINOR WHENEVER THE NEXT CHARACTER IS ` `, `^`, `_` OR `=`** — `getMode`'s own
  truncation, with abcjs's comment "this will handle the case of 'm'". So `K:C m=B` is C
  MINOR WITH A NATURAL B, and `getMode` consumes only the alphabetic run so the `=B` still
  scans. With the mode slot filled (`K:Cmaj m=d`) the token IS the middle override.
- **`middle=` REACHES THE CLEF FALLBACK**, exactly as `stafflines=` does.
- **AN UP STEM'S ANCHOR IS THE CENTRE PLUS A HALF, NOT `(centre - half) + w`** — abcjs
  hands `printStem` ONE number. `153.91499999999999204` against `153.91500000000002046`.

### 7. THE BEAM AND THE STEM — TWO RE-ASSOCIATIONS, THREE CLEFS

**A BEAM'S LEVEL-0 x GROUPS THE 0.6 DIFFERENTLY FROM AN AUXILIARY BEAM'S.** `calcXPos`
writes `startX += starthead.w - 0.6` — off the WIDTH — where `createAdditionalBeams` builds
`x = furthestHead.x + (asc ? furthestHead.w : 0)` and THEN `x + (asc ? -0.6 : 0)`, off the
SUM (`layout/beam.js:74-81, 175-186`). Both are abcjs's; this engine had the auxiliary one
twice, so the beam's y was SOLVED at one x and DRAWN at another. ⚠️ **AND FIXING IT MAKES
LEVEL 0's OWN COMMENT TRUE AGAIN** — "its ends ARE the beam's own endpoints, so `yAt(x1)` is
`startY` by construction" only held while `x1 === beamStartX`. Four local baselines moved by
a thousandth of a pitch and were re-recorded; every abcjs gate stayed green.

### 8. `%%keywarn` IS READ AT THE `K:`, AND abcjs'S OWN TEST INPUTS

Counting each visual test file's `var abc` declarations against the fixtures already here
named ~20 absent — nine in `multi-voice`, which is where `checkLastBarX` came from — plus
all four `tie-slur` tunes, whose file the harvester skipped entirely. Seventeen added,
SIXTEEN byte-exact on arrival. The corpus now covers multi-part ties across staves,
`%%score`-shared staves, `[r:remark]`, `K:C none`, mid-tune `T:`/`K:` interleaving and
`!class=!` markup that nothing else here writes.

The seventeenth toggles `%%keywarn` three times and paid two defects:

- ⚠️ **THE DIRECTIVE IS READ AT THE `K:`, NOT AT THE END OF THE TUNE.** abcjs tests
  `multilineVars.keywarn !== false` inside `parseKey` itself, so a directive between two key
  changes governs only what follows it. This engine carried ONE tune-level flag.
  `Measure.keyChangeKeywarn` records it per `K:` and FOUR sites consult it.
- ⚠️ **AND THE GUARD IS ON COMPUTING THE NATURALS, NOT ONLY ON DRAWING THE KEY.** abcjs
  builds `impliedNaturals` inside that same `if`, so with the directive off the key carries
  none and the NEXT line's own signature has none to concatenate.
- ⚠️ **AND THE CAUTIONARY GUARD GOES AFTER THE "IS THERE A CHANGE" TEST.** Asked first, a
  measure with no change answers `undefined`, falls back to the tune-level flag and
  suppresses an element abcjs publishes — four `parseOnly` rows said so within one run.

### 12. TWO DIAGNOSTICS, AND THE WARNINGS GATE AT 1 OF 487

- **abcjs WARNS AS AN UNPARSEABLE TOKEN ENDS A CHORD** — `warn("Expected ']' to end the
  chords", line, index)`, the `else` arm of the chord loop. The RULE landed on 2026-08-21
  and the geometry has agreed ever since; only the diagnostic was missing.
- **AND A `:` THAT STARTS NO BAR IS TWO WARNINGS, NOT AN UNKNOWN CHARACTER.** `getBarLine`
  returns `{len: 1, warn: "Unknown bar symbol"}` and the caller then finds an empty type and
  warns AGAIN at the same column. `[V:1]P:A` is the shape — a `P:` on a music line — and
  all four `abcts-tempo-rung` tunes carry one.

⚠️ **A NEW FIXTURE CAN RAISE A GATE'S COUNT WITHOUT CAUSING ANYTHING.** `abcts-tempo-rung`
took warnings from 2 to 5 the day it landed: its `[V:1]P:A` exposed a diagnostic gap that
had always been there, on four tunes whose geometry was byte-exact. Read a rise against
what the fixture WROTE before reading it as a regression.

### 11. A KEY SIGNATURE'S WIDTH, AND WHAT `%%keywarn 0` ACTUALLY TAKES

Three defects on one fixture, each uncovering the next — the sharps were invisible while
the element was suppressed, and the ULP was invisible while the sharps were missing.

- **THE WIDTH WAS `dx` LESS THE TRAILING GAP.** `abselem.w` is `max(right.dx + right.w)`, a
  running maximum that never subtracts. `(a + 2) - 2` gave `15.649999999999999` for
  `15.65`, and the width is what the solve spends as `(x + w) + minspacing`.
- **AN INLINE `[K:]` IS EXEMPT FROM `%%keywarn`** — the guard is on
  `appendStartingElement`, the standalone `K:`'s route into the stream.
- **AND `%%keywarn 0` TAKES THE CANCELLATIONS, NOT THE SIGNATURE** — `impliedNaturals` is
  built inside `parseKey` under the same `if`.

### 10. THE TEMPO RUNG TRAVELS WITH ITS TEXT

`verticalExtent` recovered a `Q:` mark's reserve from the baseline it had just been placed
at — `x + a + b - a - b`. The rung now travels as `reserveTopPitch`, exactly as the CHORD
symbol's already did; that field's own doc comment predicted this case and it took a control
to find it. ⚠️ **ONE ULP OF `staff.top` ON EVERY TUNE WITH A `Q:`**, and `staff.top` is what
`calcHeight` sums, so the page cursor, every later system and the root `height` inherited
it. The two staves WITHOUT a tempo agreed bit for bit at every step, which is what named the
tempo lane out of the whole above-ladder.

### 9. AN OPENING SLUR'S ITERATION SPLIT

abcjs's own `tests/parse/start-char.test.js`, whose emoji is NOT the point: every other
field agreed and `startChar` was 54 against 56. `parseMusicLine` captures `startI` at the
head of each iteration and only THEN gathers spacers, chord symbols, decorations and grace
groups; a `(` is none of those, so the gather loop breaks on it, the slur is consumed, and
if what follows is not note-ish the iteration ends having appended nothing. ⚠️ **A `.`
BEFORE THE SLUR JOINS THE RUN AND ONE AFTER IT ENDS THE RUN** — `.(F4)` keeps its opening,
`(.F4)` does not. `tile` already had the rule and its walk stopped dead at a leading `.`.

---

## THE RULES THIS SESSION PAID FOR

⚠️ **A RICH CONTROL IS NOT A BETTER CONTROL.** Three enumerations paid this session on a
four-bar tune; a fourth was built on a two-voice tune with lyrics, a tuplet, a grace, a `W:`
and a mid-tune `P:` — and that control DIVERGED (§3a), which invalidates every row, while
abcjs rendered it at ~15s each against fractions of a second for the simple one. Reach for
the smallest shape that can REACH the thing measured, and no larger.

⚠️ **A DIVERGING CONTROL IS THE RESULT.** "0 of 96 identical" is not a statement about 96
modifiers; it says the shape they were measured in is wrong. Check the bare control BEFORE
reading a variable's column.

⚠️ **AND A CONTROL THAT CANNOT MOVE IS NOT A CONTROL.** `middle=B` read identical in both
engines through TWO enumerations — because B *is* the treble middle line, so neither engine
drew anything different. `middle=d` and `middle=G` found the defect at once. This is
yesterday's "a SAME is only as good as the shape that asked", and it cost two passes.

⚠️ **A LIST THIS ENGINE ALREADY ACCEPTS IS NOT AN ENUMERATED LIST.** All 96 decoration
names had been in `ABCJS_KNOWN_DECORATIONS` since the five-table finding. Accepting a name
and DRAWING it are two surfaces, and only the second was ever measured.

⚠️ **ONE SURFACE'S GREEN CANNOT CLEAR ANOTHER'S.** The whole-element bar move was
byte-perfect and broke three `makeVoicesArray` rows; the `%%MIDI` list moves NOTHING on the
byte gate and cost two `formatting` rows. Run every gate before believing a fix.

⚠️ **AND A NEW FIXTURE IS UNGATED ON EVERY SURFACE ITS GOLDEN WAS NOT REGENERATED FOR.**
Twenty-odd harvesters read `tests/corpus-abcjs/fixtures`. Every fixture added today was run
through all of them and key-diffed against `HEAD` — added-only, nothing changed or removed.

⚠️ **AN ARITHMETIC IDENTITY IS STILL NOT AN ARITHMETIC EQUALITY.** Two of today's landings
are re-associations: the stem anchor and the `%%MIDI` fraction. A third is still open (§3a).
Yesterday's rule, three more instances.

---

## THE HARNESS

| Script | Prints |
|---|---|
| `D=<dir> O=<dir> npx tsx scripts/zzdecs.ts` | our SVG for every probe tune in a generated directory, one file per case — the harness all four of today's sweeps used. `<dir>/names.json` names the cases. |

The generators are throwaway (scratchpad): read abcjs's own table or switch, write one
`<i>.abc` per case plus `names.json`, render each through `Tools/abcjs-debug/dump-svg.js`
with `ABCJS_VERSION=6.7.0`, render ours through `zzdecs`, compare. **ONE FILE PER CASE, NOT
ONE MULTI-TUNE FILE** — an unpaired `!glissando)!` crashes abcjs and takes every other tune
of a shared file with it.

⚠️ **THE abcjs REFERENCE THE TOOLS LOAD HAS NO PROBES IN IT.** `dump-svg.js` resolves
`../../Docs/References/abcjs/abcjs-6.7.0` from its own directory; the instrumented copy is
`/tmp/gp/abcjs`. To read a probe, copy `dump-svg.js` INTO `Tools/abcjs-debug/` with its
`abcjsPath` repointed — and ⚠️ **`zz-dump-probe.js` IS ALREADY A TRACKED FILE THERE**, so
name yours something else (this session clobbered it once and had to `git checkout` it back
in the abcMusicKit repo). Probes added today: **`ZZW`** at `addRight`, **`ZZTB`** at the
`TieElem` case of `setUpperAndLowerVoiceElements`, **`ZZGL`** at `drawGlissando`, **`ZZLB`**
at `checkLastBarX`, **`ZZSW`** at the stem's `dx`, **`ZZST`** at `printStem`.

⚠️ **AND `cd` MATTERS MORE THAN IT LOOKS.** Several probe runs in this session reported a
clean sweep of DIFFs that were really "the reference never rendered", because the shell had
reset to `~/ICMLabs/Code` and `../abcMusicKit` resolved one level off. Check that the
reference file EXISTS before reading a column of failures.

---

## THE METHOD, UNCHANGED

**Read the named abcjs function FIRST, then instrument to confirm it — neither half works
alone.** Run every command from `/Users/lrettberg/ICMLabs/Code/abcts`. Run `npx tsc
--noEmit` BEFORE every commit. After regenerating ANY golden, **diff it against `HEAD` and
assert that only NEW KEYS appeared**. Re-ratchet each gate's PASSING list in the same
commit — that is FIVE lists per fixture: `svg-bytes`, `lines` (list AND character floor),
`corpus-abcjs`'s `WITHIN` counts, `tune-metrics` and `voices-array`. Commit and push after
every landing; never `--force`. Omit `Co-Authored-By` trailers in this workspace.

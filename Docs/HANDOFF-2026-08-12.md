# HANDOFF — 2026-08-12

## The session prompt

```
We are continuing abcts development in the abcts repo (Code/abcts).

THE STANDING ORDER, in Lance's words: abcts exists to build an abcjs-modern whose
output — the SVG FILE and the AUDIO — is 100% BYTE-EQUAL to abcjs 6.7.0. A
tolerance is not a compromise to be balanced against effort; it is A DEFECT THAT
HAS NOT BEEN WRITTEN DOWN YET. Anything you decline to reproduce goes in
Docs/ABCJS-DIFFERENCES.md with its evidence, and its slug goes in
svg-bytes.test.ts's DIVERGENT list. That list is EMPTY and stays empty. Work
until byte-equal, without stopping for approval or status. Checkpoint, hand off
and compress as you go so no context is lost.

Take abcjs's arithmetic, order and data model even where a better one exists.
Mark deliberate abcjs-shaped compromises with `abcjs-debt:` comments and entries
in Docs/ABCJS-DEBT.md.

Read Docs/CHECKPOINT-2026-08-12.md FIRST — the state, thirty-one landings, §3 WHAT IS
LEFT (two rows measured to the digit and not landed), §5 THE ONE RED IS NOT OURS
(read this before you believe any suite result), §6 THE HARNESS and §7 the rules.
Then Docs/CHECKPOINT-2026-08-11b.md for the arithmetic arc and the earlier
ledger, then CLAUDE.md.

READ ABCJS'S CODE. IT HAS THE ANSWERS. Lance has said it three times; each time
it was ignored it cost a revert. Port its STRUCTURE, then its constants.

WHEN THE SOURCE IS NOT ENOUGH, INSTRUMENT A SCRATCHPAD COPY at /tmp/gp/abcjs —
NEVER ../abcMusicKit, another agent works there and its tree is dirty right now
(that is §5). Instrument to ANSWER A QUESTION, not to see what happens.

Run every command from /Users/lrettberg/ICMLabs/Code/abcts. `cd` elsewhere is
refused and silently resets the shell to the WORKSPACE ROOT, where vitest
collects every sibling repo. Run `npx tsc --noEmit` BEFORE `git commit`.

Confirm your lane with `git rev-parse --abbrev-ref HEAD`. It is `main`.
```

## Where it stands

**`svg-bytes` is 15 of 171 differing, from 49.** 156 fixtures byte-exact, all ratcheted —
`visual-selection-01` (202,156 bytes), `visual-tablature-15` and `visual-mouse-click-01`
(85,865 each) among them.

**THE FIFTEEN, BY WHAT THEY NEED:**
1. `svg-per-line-01` — a split curve's far end. `lineEndX` is abcjs's `params.w - 1`, the
   VOICE's own width (`draw/voice.js:12`); ours is `system.width - marginX` and comes out
   785 where abcjs has 687. Everything else on that fixture is now exact.
2. FIVE on the MID-TUNE BLOCK WALK — `visual-directives-01`, `synth-timing-05` (a
   `clefs.G` y), `visual-transpose-output-04` (root `width`), `visual-transpose-05`,
   `visual-options-01` (root `height`). §3.5 has the measurement: a system carrying a
   mid-tune block collapses its lead into `originAdvances = [flat]`, ONE number where
   abcjs spends four `moveY`s and a product. The TOP block already walks abcjs's eight
   adds — copy that, and give `freeTextBlock` the `advances` array `topTextBlock` returns.
3. `visual-layout-07` — §3.3b, the key signature's clef. Read the six-control ladder first.
4. `visual-svg-02-staffwidth-12` — §3.3, three `[M:]` in one measure. CONFIRMED by count:
   abcjs draws 3 `staff-extra time-signature`, we draw 1.
5. `visual-transpose-04` — §3.3c, the chord lane, 20px.
6. `visual-misc-12` — §3.3g, `beambr1`'s literal split array.
7. `visual-parsing-06`/`-07` — §3.1, the per-LINE forced stem.
8. `synth-flattener-28` — a `flags.u8th` x ULP.
9. `visual-svg-per-line-02-scaled` — a stem edge at 264.39 vs 264.4.
10. One multi-verse lyric: abcjs writes the verses as `<tspan>`s in ONE `<text>` and we
    write a `<text>` per verse. The site says "no corpus fixture puts two verses on one
    note" — one does.
The seven that closed after the first checkpoint are §2b; **§3.5 says what each of the 22
remaining rows is**, including a six-fixture ULP family measured down to the missing term.
Every other gate is at zero and none of them can name the next defect — that has been the
normal condition on this branch since 2026-08-07.

**The one standing red is `content-parity`'s `S7-voices`, and it is caused by an
uncommitted edit to a fixture in `../abcMusicKit`, not by anything here** — checkpoint §5.
**And `fuzz.test.ts` fails intermittently under a full run because it asserts WALL CLOCK**
(`SLOW edge#19: 1304ms for 50008 chars`) — §5b. Run it alone before believing it. Read both
before concluding anything from a suite result.

## What to do next, in order

0. **READ §3.5 FIRST.** It classifies all 22 open rows, and the largest single item is the
   ROOT-DIMENSION ULP FAMILY — SIX fixtures on ONE cause: a system carrying a MID-TUNE
   block collapses its lead into `originAdvances = [flat]`, one number where abcjs spends
   four `moveY`s and a product. The TOP block already walks abcjs's eight adds; copy that.

1. **Three rows are MEASURED AND NOT LANDED, each with the reason written down** —
   checkpoint §3.3 (a measure's meter change is singular), §3.3b (a key signature's
   positions are baked at parse time, and the two call sites pass different clefs) and
   §3.3c (the chord lane is inverted, and `placeInLane` does not walk source order) and
   §3.3d (a slur closes on a GRACE NOTE). Two of the four were implemented and reverted;
   read what they cost before re-trying them.

2. **The per-LINE forced stem** — checkpoint §3.1, measured to the element with abcjs's own
   three voice streams printed. Two fixtures. Re-read `stemForVoice`'s doc block first: the
   existing rule is load-bearing for `ave-verum-corpus` and `ragtime-nightingale`.

3. ~~The lyric lane's per-voice `diff`~~ — **LANDED, §2b.10.** Both terms, in pitch.

4. Then the rest of §3.4, and re-read the ranked table after every landing — three of this
   session's findings were surfaced by a fixture that had nothing to do with them.

## The two probes that did the work today

**abcjs, instrumented, one fixture:**

```bash
NODE_PATH=../abcMusicKit/Tools/abcjs-debug/node_modules \
ABCJS_PATH=/tmp/gp/abcjs \
node /tmp/gp/walk.js --file tests/corpus-abcjs/fixtures/<slug>.abc 2>&1 | grep MYPROBE
```

Edit `/tmp/gp/abcjs/src/...` freely; keep a `.bak` and restore it. `ABCJS_MOVEY=1` already
prints the whole page cursor walk with call sites, which is what §3.2 is built on.

**ours, one fixture, rendered exactly as the byte gate renders it** — `scripts/zzpr.ts` in
`CHECKPOINT-2026-08-12.md` §6. It must live in the repo; `tsx` will not resolve a script in
`/tmp` against these imports. `ABCTS_PROBE=1` adds the staff extents with the contributing
source line. **vitest swallows `console.log`, so probe through `tsx`, not through a test.**

# Handoff — 2026-08-03d

Full state is in `CHECKPOINT-2026-08-03d.md`. This is the prompt and the shortest statement
of where to start.

---

## THE PROMPT

```
Continue to 100% abcjs parity in the abcts repo (~/ICMLabs/Code/abcts, branch
`geometry/vertical`, git@github.com:ICMLabs/abcts.git).

THE STANDING ORDER: 100% parity with abcjs on EVERY tune — the 41-fixture corpus,
the 174-tune harvested corpus, Gonzato, and the audio/MIDI feature set. Work
until it is reached without stopping for approval or status. Checkpoint, hand off
and compress as you go so no context is lost.

FIRST: read Docs/CHECKPOINT-2026-08-03d.md, then -08-03c and -08-03b for the
accidental columns, the ragtime verdict and the beam findings, then
ARCHITECTURE.md, then CLAUDE.md.

TWO REPOS, BOTH WITH A BRANCH CALLED `main`. `Code/abcts` is ours.
`Code/abcMusicKit` is a read-only sibling holding the vendored abcjs source, the
41-fixture corpus and its goldens — never commit there, and if you instrument it,
restore it and verify `git -C ../abcMusicKit status --short` is empty before you
finish. That command EXITS 0 with output, so `&& echo CLEAN` lies — read it. And
never write `--output /dev/null` in a loop over fixtures: a multi-tune file makes
`-tuneN` files in that repo. `cd` does NOT persist between tool calls — use
absolute paths.

THE GOAL IS 100% PARITY, bugs and poor rendering choices included. A test that
asserts abcjs is wrong is a test to correct, and FIVE have been.

THE ALGORITHM IS IN ABCJS — read the named function, do not infer it from a diff.
Everything that landed today is a port of one: the mid-tune key signature's
declared box, the top-text rows, getFontParameter's bare `box`, the zero-duration
note's 0.25, getMinWidth on a rest, the VOICE-OVERLAP rule, the overlay pad's
durationThisBar, and the chord symbol's six substitutions. None could have been
guessed from the output.

BUT FINISH WITH A PROBE. Three times the source has predicted a number its own
SVG denies — putChordInLane's per-lane growth, setLane's invertLane, and Z24's
duration. And one rule is not in the source at all: a [V:x] naming the voice
already current is a no-op.

START HERE, EVERY TIME:
  npx vitest run tests/corpus-abcjs-ranked.test.ts && cat /tmp/abcts-corpus-ranked.txt

THE ITEM PROBE IS THE HORIZONTAL WORKHORSE and it now exists on BOTH sides.
ABCTS_PROBE=1 prints v/i/kind/dur/w/left/gap/er/x per item on the SOLVED pass;
add the matching line to abcjs's layoutOneItem (the checkpoint has it verbatim)
and read them side by side. OUR rod CARRIES THE GAP where abcjs's w does not, so
compare the sum — and READ THE DURATIONS FIRST, two of the day's finds were a
duration with a correct width beside it. console.log is SWALLOWED under vitest
here: capture it in the driver and write a file.

THREE GOLDEN LIMITATIONS, all three read out of dump-svg.js itself: little
swallow's dx (ASCII-only width table measures CJK at 8px), ANY NON-DEFAULT FONT
SIZE's width (calcWidth maps every size onto one of six per-character tables),
and %%jazzchords' oy (getBBox counts a chord's nested tspans as separate LINES).
Text HEIGHT is faithful at any size otherwise. Judge font work on the VERTICAL
axes. A FOURTH was on that list for two days — vree-grace-notes' and ragtime's
dy — and it was OURS: the grace emission order. "The gate cannot see this" and
"the golden is wrong" are different claims, and the second needs the golden
opened.

NEVER RAISE A CEILING TO MAKE A CHANGE PASS. One red: ragtime-nightingale's oy at
0.646 against 0.59 — down from 1.58 today. Anything else failing is yours.

NEXT, in order: mid-tune non-music LINES (a T: subtitle or a %%text between two
music lines is dropped — it is the top item and it blocks five fixtures),
%%voicecolor, %%barnumbers/%%measurenb/%%setbarnb, %%header/%%footer/%%sep/
%%musicspace, the remaining font SITES (annotation, triplet, measure, repeat,
voice, tempo now that score.fonts exists), the grace-note-before-a-y content gap,
the leading-header split, then Gonzato, then audio.
```

---

## WHERE TO START

### 1. DONE — mid-tune non-music lines, `%%sep`, the lanes, and the `K:` first token

All four landed after this handoff was first written; see findings 25-27 in the checkpoint.
The list below is what is LEFT.

### 1b. WAS: mid-tune non-music lines

A `T:` subtitle or a `%%text` block standing BETWEEN two music lines is dropped. abcjs
draws each as a LINE of its own, so it pushes the systems below it down.

The machinery is already there and is the reason this is now worth doing: the top-text
block is built by `topTextBlock` and injected as a `type: 'title'` element carrying
`blockHeight`, which `verticalExtent` already accounts for. A mid-tune block wants exactly
the same treatment on the first staff of a LATER system, which means:

- the parser recording which system a block precedes (the free-text blocks are on the
  Score today; they need a system index, or a `textBefore` on the measure that opens the
  system after them);
- `topTextBlock`'s row logic reused for a block with no title.

`visual-options-01-fonts` (oy −44.6), the two `selection-test` (116.6), `mouse-click-01`
and `tablature-15` (69.8) all carry one, and `S2-fields` is the 41-corpus case.

**Careful:** a `T:` that turns out to be after ALL the music is drawn at the FOOT and moves
nothing — measured, `mouse-click-01`'s "Inserted subtitle" sits at y 630.21 below both
systems. Its `T:` is in the last VOICE of a system, not between systems.

### 2. The rest of the ranked table, as it stands at the end of the session

```
499.38  dy= 40.8 dx=499.4 oy=  5.5 ox=113.3  visual-tablature-17-stretchlast   GOLDEN (dx)
 38.37  dy=  0.0 dx=  0.1 oy=-38.4 ox= -0.1  visual-misc-03-jazzchords         GOLDEN
 31.50  dy= 23.1 dx= 17.0 oy=-31.5 ox= -4.6  visual-options-01-fonts
 23.00  dy= 15.5 dx=  0.0 oy= 23.0 ox= -0.0  visual-layout-09-endings  [%%voicecolor]
 20.47  dy= 19.5 dx=  6.3 oy= 20.5 ox=  2.7  visual-selection-01 / visual-svg-per-line-01
 20.06  dy= 20.1 dx=  0.0 oy=-10.0 ox= -0.0  synth-flattener-31-tempo-change-three-voices
 18.49  dy=  0.0 dx= 15.1 oy=-18.5 ox= -2.6  visual-transpose-04-transpose-annotations
```

`synth-flattener-31` is three voices with a `[Q:]` on each — a mid-tune TEMPO CHANGE, which
we drop the way we dropped the mid-tune clef and meter. `visual-transpose-04` is
annotations (`"^text"`), `oy`-led.

**Nothing above 40px is ours any more.** Both of the top two are golden limitations.

Two of the top three are golden limitations and are not chaseable:

- **`visual-tablature-17`'s `dx`** — its chord symbols are 10, 20, 40 and 80 POINT and the
  generator measures three of the four with the wrong table. `dy` 40.8 and `ox` 113.3 ARE
  real.
- **`visual-misc-03-jazzchords`** — finding 31, worked through to 38.4px exactly.

What is left that is ours:

- **`visual-selection-01` / `visual-svg-per-line-01`** is down to 20.5 from 116.6, and its
  four staff toplines are now out by **11.63 / 11.64 / 27.34 / 31.17** — system 1 RIGIDLY
  11.63 too low, system 2 worse. **IT IS NOT ANY OF ITS DIRECTIVES**: deleting each of
  `%%vocalfont`, `%%voicefont`, `%%measurefont`, `%%partsfont`, `%%stretchlast`,
  `%%barnumbers`, `%%titlefont` and `%%musicspace` in turn leaves all four diffs unchanged
  (`%%vocalfont` moves only system 2's second staff, which is the lyric lane and is ours
  already). So it is in the MUSIC, and `mouse-click-01` — the same header with a shorter
  version of the same music — is CLEAN. Bisect the RH voice bar by bar between the two;
  that is how the volta's `minspacing` was found.
  Note `%%voicefont Helvetica-Bold 10.0` and `%%measurefont Times-Italic 11` ARE parsed into
  `score.fonts` and not yet read by the voice-name or bar-number sites — but neither is what
  this fixture is measuring.
- **`%%barnumbers` / `%%measurenb` / `%%setbarnb`** — five fixtures, all `oy`-only and all
  under 11px. The bar number is a POINT at `vert + height/STEP`, `vert` 13.5 on a wide
  number over a treble clef and 11 otherwise (`abstract-engraver.js:946-953`), added by
  `addFixed` so it enters the ink.
- **`%%voicecolor`** is all that is left of `visual-layout-09` besides a `dy` of 15.5.
- **`visual-misc-05` is DIAGNOSED: a decoration written immediately before a BARLINE
  attaches to the BARLINE.** `createBarLine` ends with `if (elem.decoration)
  this.decoration.createDecoration(voice, elem.decoration, 12, thick ? 3 : 1, abselem, 0,
  "down", 2, …)` (`abstract-engraver.js:1002`) — a fixed pitch 12, not the note's extent.
  Every one of that fixture's `!D.C.alcoda!` marks sits before a `|`, and our parser binds a
  decoration to the NEXT event, so all five are dropped. Measured on a control pair:
  `CCCC!D.C.alcoda!|` costs abcjs **6.85px** and us 0, and `!D.C.!` costs the same.
  `textDecoration` reserves `textFudge 2` above the placement cursor with `thickness: 3`
  and then `incrementPlacement(placement, 5)` (`decoration.js:146-153`).
  A FINGERING is the same shape: `CCC!5!C` costs abcjs 6.88 where we reserve 3.22.

### 3. The systemic 0.03px

Every fixture carries a rigid `oy` of about 0.03px and a sharp key signature adds another
0.04 — measured on `K:C`/`K:A` control pairs with nothing else in them. It is a hundredth
of a staff space and it is invisible in every gate but the ranked table's 0.05 threshold,
where it is the whole reason a dozen fixtures are still listed. Not chased.

---

## STILL NEEDING A DECISION, NOT A COMMIT

**The gate hides failures.** All eight per-fixture assertions live in one `it`, so the
first to fail ends it. Collecting all four axes before asserting is the honest fix and will
show more red before it shows less.

**A grace note before a `y` spacer is dropped.** `Rest` has no `graceNotes` field and the
model says so deliberately. abcjs disagrees for a SPACER. `visual-tablature-10` is
`(f3 {a})y` at `oy` −9.6.

**The overlay pad's second rule.** For lines BEFORE the one carrying the `&`, abcjs mirrors
the main voice NOTE FOR NOTE rather than one rest per measure. The two agree when a measure
holds one note, which is every case in the corpus so far.

---

## WHAT NOT TO CHASE

`little swallow` dx — the goldens' ASCII-only width table at 8px per CJK character.
`visual-misc-03-jazzchords` oy — the generator counting nested tspans as lines.
Any NON-DEFAULT font size's width.

**`ragtime-nightingale`'s dy and `vree-grace-notes`' dy/dx are OFF this list** — see finding
32. They were the grace emission order, which was ours, and they are now 1.12 and 0.02.

A fixture with ONE paired notehead has no spread: its `dy`/`dx` of 0.00 are arithmetic, not
parity.

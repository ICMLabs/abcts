# Handoff — 2026-08-07

State, findings 125–128 and what is left are in `CHECKPOINT-2026-08-07.md`.
`CHECKPOINT-2026-08-06.md` still keeps **THE HARNESS**, which is the first tool to reach for.
Earlier ledgers: `-08-06b.md` (106–124), `-08-05c.md` (90–103 and the `ENGRAVE` triage
table), `-08-05b.md` (71–89), `-08-05.md`, `-08-04c.md` (51–70 and the ladder method),
`-08-04b.md` (41–50), `-08-03d.md` (16–40).

---

## THE PROMPT

```
Continue to 100% abcjs parity in the abcts repo (~/ICMLabs/Code/abcts, branch
`geometry/vertical`, git@github.com:ICMLabs/abcts.git). Pushed and green at 886/886.

THE STANDING ORDER: 100% parity with abcjs on EVERY tune — the 41-fixture corpus,
the 174-tune harvested corpus, Gonzato, and the audio/MIDI feature set. Work until
it is reached without stopping for approval or status. Checkpoint, hand off and
compress as you go so no context is lost.

FIRST: read Docs/CHECKPOINT-2026-08-07.md — the state, findings 125-128, and above
all THE GATE WAS READING 29 OF THE 41 FIXTURES. Then Docs/HANDOFF-2026-08-07.md,
then CHECKPOINT-2026-08-06.md for THE HARNESS, then -08-06b.md (findings 106-124),
-08-05c.md (90-103 and the ENGRAVE triage table), -08-05b.md (71-89), -08-05.md,
-08-04c.md (51-70 and the ladder method), -08-04b.md (41-50), -08-03d.md (16-40),
ARCHITECTURE.md, CLAUDE.md.

READ ABCJS'S CODE. IT HAS THE ANSWERS. Lance's correction, twice. All four of the
last session's findings were reads of a named function, and none could have been
guessed from a diff.

BUT ALSO: BEFORE CONCLUDING A GATE IS EXHAUSTED, CHECK WHAT IT ENUMERATES. The
previous checkpoint's headline was that no gate could name the next defect. The
pixel gate was enumerating `<name>.svg`, which only a SINGLE-TUNE fixture has —
so twelve multi-tune fixtures and 89 tunes were unmeasured, with abcjs's own
`<name>-tuneN.svg` goldens sitting in the same directory. Widening it produced a
ranked list of twelve defects and four of them are now closed. ASK WHAT EVIDENCE
EXISTS BEFORE ASKING WHAT THE EVIDENCE SAYS.

START HERE, EVERY TIME — TWO tables now:
  npx vitest run tests/pixel-parity.test.ts && cat /tmp/abcts-pixel-ranked.txt
  npx vitest run tests/corpus-abcjs-ranked.test.ts && cat /tmp/abcts-corpus-ranked.txt

The harvested table is EMPTY. The pixel one has 19 of 119 rows; §"WHERE TO START"
below picks the two to take first and says why.

READ THE SHAPE of a per-notehead diff before hypothesising. A uniform `oy` is a
staff origin (128). A clean ramp inside ONE system is missing FIXED width that
justification spread (127). A staircase stepping at one construct is a
per-construct deficit accumulating (126). Each named its cause in one look.

PROVE IT ON A CONTROL TUNE, and prefer a LADDER. Three controls — plain,
`!slide!`, `!glissando(!` — put finding 128 on `!slide!` in a single run, and the
glissando was the likelier suspect.

USE THE HARNESS — a SCRATCHPAD COPY of abcjs with env-guarded console.log, written
out in CHECKPOINT-2026-08-06.md. Copy `dump-elements-char-widths.js` beside
`dump-svg.js` or it throws MODULE_NOT_FOUND. `node dump-svg.js --file <abs path>`;
a bare path argument is treated as ABC TEXT and silently renders something else.
DO NOT instrument ../abcMusicKit — another agent works there and it is DIRTY.

The two probe points that earned their keep this session:
  tune-builder.js, end of `cleanUp`   — per-line `staff.key` + inline key elements
  voice-elements.js, after setX       — every element's type/x/w/extraw/minsp/heads

NEVER RAISE A CEILING TO MAKE A CHANGE PASS. Eight were LOWERED on 2026-08-07 and
none raised. MEASURE BEFORE AND AFTER over all 119 targets, not against the
ceiling: EPSILON is 0.05 and a fixture recorded at 0.0 can measure 0.03.

`npx biome check src` is NOT clean and never was — 1 error, 4 warnings, all
pre-existing. Diff against a stash before blaming yourself.

`cd` DOES NOT PERSIST between tool calls, and a `cd` inside a compound command
leaves the shell there — it cost a confusing "No test files found" last session.
Put the absolute `cd /Users/lrettberg/ICMLabs/Code/abcts &&` in front of EVERY
command. vitest SWALLOWS console.log on a passing test — use
`--disableConsoleIntercept`. DELETE YOUR PROBE BEFORE COMMITTING.
```

---

## WHERE TO START

### 1. `S5-directives-tune1` — take its `dy` FIRST, not its `dx`

The row reads dx 24.72, but the tune holds **two independent defects** and the smaller one
is far cleaner.

**The `dy` 0.52.** Every head abcjs draws at gy 117.02 is exact; every head it draws at
116.50, 120.38, 124.25, 135.88 or 139.75 is +0.51. Those are the `n` (`!style=normal!`) heads
inside a `style=rhythm` voice — so a ROUND head in a rhythm voice sits half a pixel low for us
while the SLASH heads are exact. One glyph's vertical reference, on 188 noteheads, and
`S5-directives` X:502 is the only fixture that mixes the two.

**The `dx` 24.72** is system 0 alone, ramping 0 → 23.06 with plateaus at bar boundaries, in a
tune carrying `style=rhythm`, `U:n=`, `[K:style=x]`, `!style=harmonic!` and `B0`
zero-duration notes. Systems 1–4 are all inside 0.5px. Separate the two before starting.

### 2. ~~THE SEVEN 0.18px `ox` ROWS~~ — NOT WORK. Measured and closed as out of scope.

They are eight rows, not seven, and every one is a tune whose whole content is `G8` — six
`clefs`, one `S6-keys`, plus `clefs-tune7` which is a different shape. The 0.18 is the WHOLE
NOTEHEAD's OUTLINE, which `pixel-parity.test.ts`'s own header puts out of scope in its second
paragraph: abcjs's head inks 16.83px wide against Bravura's 15.03, the two are not
left-aligned, and positions are compared as bounding-box CENTRES. No placement rule can
remove it.

There is now a test asserting exactly 0.18 on those eight, so a REAL regression on them still
fails while the outline difference stops reading as a defect. **Do not chase it, and do not
"fix" it by moving the glyph** — that would trade a documented, intended difference for an
undocumented placement error.

### 3. `S8-layout-tune11` (dy 8.37 / dx 23.66 / oy 4.73)

X:812, "Changing Time or Key Signatures, Guitar Chords". `dy` with `oy` is a staff extent.
Note it MOVED when `tune10`'s accidental room landed (50.31 → 23.66), so re-measure the shape
rather than reasoning from the recorded number.

### 4. `ragtime-nightingale`, dx 12.13

Its other three axes are effectively closed — 0.04 / 0.01 / 0.02. `dx` did not move on any of
this session's four findings. The cancellation line is DONE and was not it. What remains from
`-08-06b`: the largest single band JUMPS 10.33 between two adjacent heads at golden x 323.1
and 442.9 on the y≈4600 system — ONE element's width, not a spread.

### 5. Then the fixed lanes, then Gonzato, then audio.

---

## WHAT NOT TO CHASE

- **The harvested ranked table.** Still 0 of 174. It is a regression net, not an instrument.
- **The cancellation line** — DONE, finding 125. And `-08-06b`'s description of it was
  WRONG in one respect: abcjs draws the cancellation TWICE, once at the end of the previous
  system and once in the next system's prefix. Do not re-derive it from that section.
- **A "same signature prints nothing" rule** — there is none in abcjs (127). If a control
  seems to show one, it is the voice's own `K:` reaching an EMPTY voice child list, which is
  a different mechanism.
- **`!slide!` as a glyph** — it is a tie between two zero-width blanks (128).
- **The `-classes-tuneN` and `-print-tuneN` goldens.** Same tunes, same geometry, three times
  the rows. The gate enumerates the plain family on purpose.
- **The four-step staircase on `visual-layout-04`** — closed by finding 104.
- **`minPadding` and abcjs's `spacing * minSpace > 50` guard** — both measured as 0.
- **An arithmetic order-of-operations difference** (69) and **the staff line's residual
  0.002px** (70). Both ruled out.
- **The 20 biome errors from `npx biome check src tests`** — pre-existing, nearly all in the
  harvested goldens' embedded CSS. `src` alone is not clean either; see above.

---

## ONE THING MEASURED AND NOT CHASED

**Our staff LINES are 40px longer than abcjs's**, and no gate can see it — the pixel gate
compares noteheads, the line-weight gate reads THICKNESS only.

On `ragtime-nightingale`, every staff line resolves to `x=350.05 w=700.10` for us against
abcjs's `x=355.05 w=660.10` — ours spans 0 → 700.1 where abcjs's spans 25 → 685.1. The
noteheads on that fixture are exact, so nothing else moves with it.

abcjs draws from `params.startx` to `params.w` (`draw/staff-group.js:92` →
`draw/staff.js`), where `startx` is `getLeftEdgeOfStaff` — `padding.left` plus the voice-name
and brace reservation — and `w` is the staff group's right edge. Ours appears to span the
whole page width. Not started; noticed while diffing a control's full item list.

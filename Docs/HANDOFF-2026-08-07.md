# Handoff — 2026-08-07

State, findings 125–133 and what is left are in `CHECKPOINT-2026-08-07.md`.
`CHECKPOINT-2026-08-06.md` still keeps **THE HARNESS**, which is the first tool to reach for.
Earlier ledgers: `-08-06b.md` (106–124), `-08-05c.md` (90–103 and the `ENGRAVE` triage
table), `-08-05b.md` (71–89), `-08-05.md`, `-08-04c.md` (51–70 and the ladder method),
`-08-04b.md` (41–50), `-08-03d.md` (16–40).

---

## THE PROMPT

```
Continue to 100% abcjs parity in the abcts repo (~/ICMLabs/Code/abcts, branch
`geometry/vertical`, git@github.com:ICMLabs/abcts.git). Pushed and green at 890/890.

THE STANDING ORDER: 100% parity with abcjs on EVERY tune — the 41-fixture corpus,
the 174-tune harvested corpus, Gonzato, and the audio/MIDI feature set. Work until
it is reached without stopping for approval or status. Checkpoint, hand off and
compress as you go so no context is lost.

FIRST: read Docs/CHECKPOINT-2026-08-07.md — the state and findings 125-133. Then
Docs/HANDOFF-2026-08-07.md, whose "WHERE TO START" is measured at the last commit.
Then CHECKPOINT-2026-08-06.md for THE HARNESS, -08-06b.md (findings 106-124),
-08-05c.md (90-103 and the ENGRAVE triage table), -08-05b.md (71-89), -08-05.md,
-08-04c.md (51-70 and the ladder method), -08-04b.md (41-50), -08-03d.md (16-40),
ARCHITECTURE.md, CLAUDE.md.

START HERE, EVERY TIME — TWO tables:
  npx vitest run tests/pixel-parity.test.ts && cat /tmp/abcts-pixel-ranked.txt
  npx vitest run tests/corpus-abcjs-ranked.test.ts && cat /tmp/abcts-corpus-ranked.txt

The harvested table is EMPTY (0 of 174). The pixel one has 18 of 119 rows, EIGHT
of which are the whole-note OUTLINE and not work, so TEN are live and nothing is
above 8.25.

READ ABCJS'S CODE. IT HAS THE ANSWERS. All nine of the last session's findings
were reads of a named function; not one could have been guessed from a diff.

READ THE SHAPE of a per-notehead diff FIRST, and let it choose the question:
  uniform oy, dx 0            a staff ORIGIN                       (128, 131)
  dy with oy, dx 0            a staff EXTENT
  clean ramp inside ONE system  missing FIXED width, spread by justification (127, 130)
  staircase stepping at one construct   a per-construct deficit ACCUMULATING (126)
  flat plateaus, zero spread    an ORIGIN, not a width

AND THE SHAPE CAN STILL MISLEAD. Ragtime's looked like "one element is 12.13 too
wide" and both engines agreed on every width involved — the defect was the SLACK
that was supposed to absorb it, in another voice. When both engines agree on a
quantity that is still pixels apart on the page, stop measuring the quantity and
start measuring what CONSUMES it.

RULE OUT THE MECHANISM YOU SUSPECT BEFORE HUNTING INSIDE IT. Finding 131's staff
had the one lane combination with a special rule; three controls against abcjs's
own instrumented `staff.top` proved the lanes exact and turned the search into a
one-line ink probe.

PROVE IT ON A CONTROL TUNE, and prefer a LADDER. Three controls — plain,
`!slide!`, `!glissando(!` — put finding 128 on `!slide!` in one run, and the
glissando was the likelier suspect.

USE THE HARNESS — a SCRATCHPAD COPY of abcjs with env-guarded console.log, written
out in CHECKPOINT-2026-08-06.md. Copy `dump-elements-char-widths.js` beside
`dump-svg.js` or it throws MODULE_NOT_FOUND. `node dump-svg.js --file <abs path>`;
a bare path argument is treated as ABC TEXT and silently renders something else.
DO NOT instrument ../abcMusicKit — another agent works there and it is DIRTY.

THE PROBE POINTS THAT EARNED THEIR KEEP:
  voice-elements.js, after setX          type/x/w/extraw/minsp/heads
  set-upper-and-lower-elements.js, end   staff.top/.bottom + specialY
  tune-builder.js, end of cleanUp        per-line staff.key + inline key elements
  abc_parse_header.js, the "M:" arm      which parser actually sees a field

AND `voice-elements.js` FIRES ON EVERY SOLVE PASS. abcjs re-solves a line several
times; the SECOND block is not the answer, the LAST one is. Two intermediate
passes read as real numbers and cost an hour. Anchor on a figure you can also read
out of the SVG — the first notehead's x — before trusting any of it.

NEVER RAISE A CEILING TO MAKE A CHANGE PASS. Sixteen were LOWERED on 2026-08-07.
TWO were raised mid-session and EACH was closed by the very next finding, because
each was a MEAN over a spread not yet fixed and the entry named what it was
waiting on. The test that separates the two cases: did every notehead move the
right way or not at all? If one moved the wrong way it is a regression and the
rule stands.

MEASURE BEFORE AND AFTER OVER ALL 119 TARGETS, not against the ceiling. EPSILON is
0.05 and a fixture recorded at 0.0 can measure 0.03.

`npx biome check src` is NOT clean and never was — 1 error, 4 warnings, all
pre-existing. Diff against a stash before blaming yourself.

`cd` DOES NOT PERSIST between tool calls, and a `cd` inside a compound command
leaves the shell there. Put the absolute `cd /Users/lrettberg/ICMLabs/Code/abcts &&`
in front of EVERY command. vitest SWALLOWS console.log on a passing test — use
`--disableConsoleIntercept`. DELETE YOUR PROBE BEFORE COMMITTING.
```

---

## WHERE TO START

The table below is measured at this session's LAST commit. Eight of its eighteen rows are the
whole-note outline and are not work (see WHAT NOT TO CHASE), so there are TEN live rows and
nothing above 8.25.

```
 8.25  dy=0.00 dx=8.25 oy= 0.00 ox= 3.58    99 heads  S8-layout-tune6
 6.24  dy=0.00 dx=6.24 oy= 0.00 ox= 0.11    64 heads  S3-note-syntax-tune24
 6.20  dy=0.01 dx=6.20 oy= 0.00 ox= 1.07    60 heads  S8-layout-tune5
 4.68  dy=4.68 dx=0.00 oy=-2.98 ox= 0.00    11 heads  S2-fields-tune1
 3.88  dy=0.00 dx=3.88 oy= 0.00 ox= 0.17    22 heads  S5-directives-tune4
 2.66  dy=2.66 dx=0.00 oy= 1.14 ox= 0.00    58 heads  S8-layout-tune7
 1.80  dy=0.00 dx=1.17 oy= 0.00 ox= 1.80     2 heads  S4-bars-repeats-tune2
 1.58  dy=0.01 dx=1.58 oy= 0.00 ox= 0.03  2009 heads  ragtime-nightingale
 1.19  dy=0.03 dx=1.19 oy= 0.00 ox=-0.03   188 heads  S5-directives-tune1
 0.18  dy=0.00 dx=0.18 oy= 0.00 ox= 0.02    16 heads  S3-note-syntax-tune12
```

### 1. `S8-layout-tune6` dx 8.25 — MEASURED, not started

X:807 "Gracenotes", 99 heads. Every head takes one of exactly three values, `dy` is 0.00
everywhere, and there is ZERO SPREAD within each run:

```
  40 heads  dx 0.00
  32 heads  dx 4.12
  27 heads  dx 8.25
```

**The runs are NOT one per system.** The tune lays out as TWO systems in both engines,
because its four source lines are joined in pairs by `\`:

```
  system 0   source lines 1+2   heads  0-39    dx 0.00
  system 1   source lines 3+4   heads 40-71    dx 4.12
                                heads 72-98    dx 8.25
```

So the step falls INSIDE system 1, at the `\` join between source lines 3 and 4 — and system
0 spans a `\` join too and is EXACT across it. Two joins, one of them 4.125px out. The
question is what line 4 opens with that line 2 does not:

```
  line 1 ends  f{AGA}df |\        line 2 opens   {E}c2 …      exact
  line 3 ends  {gBd}B<{e}G |\     line 4 opens  {G}[G4e4] …   4.125 out
```

A flat step with zero spread on either side is an ORIGIN, not a width — a wider element would
compress its own springs and ramp.

**A DEAD HYPOTHESIS, recorded so it is not tried twice.** An earlier draft said a running
grace `roomtaken` surviving a system boundary would explain it. There is no module-level
accumulator in the grace path — the only module-level `let`s in `layout.ts` are `PERC_MAP`,
`STRICT_TEXT_METRICS`, `JAZZ_CHORDS`, `LINE_WEIGHTS`, `SCORE_FONTS` and the probe variables,
all configuration — and the premise was wrong anyway, since the step is not at a system
boundary.

Do NOT read 8.25 as "one sharp's width". It is 2 x 4.125.

### 2. `S2-fields-tune1` dy 4.68 / oy −2.98, and `S8-layout-tune7` dy 2.66 / oy 1.14

Both are VERTICAL ONLY with `dx` at 0.00 — the shape finding 128 had, and finding 131. A
uniform `oy` is a staff origin; a `dy` with an `oy` is an extent. Unexamined.

### 3. `S3-note-syntax-tune24` dx 6.24, `S8-layout-tune5` dx 6.20, `S5-directives-tune4` 3.88

Unexamined. `S8-layout-tune5` is the "Chords, Unions, First and Second Endings" tune that
findings 126 and 133 both moved, so re-measure its shape rather than reading the number.

### 4. `S4-bars-repeats-tune2` ox 1.80 on TWO heads, `S3-note-syntax-tune12` dx 0.18

Small, and two noteheads is the smallest possible search space in the corpus.

### 5. `ragtime-nightingale`'s residual 1.58, `S5-directives-tune1`'s 1.19

Both were the top of the table earlier today and are now near the bottom. Unexamined.

### 6. Then the fixed lanes, then Gonzato, then audio.

`chordSymbolStep`, `dynamicAboveStep`, `dynamicBelowStep`, `annotationAboveStep`,
`annotationBelowStep`, `partStep`, `tempoStep`, `lyricStep`. One decision, not eight.
`anchorVoltas` (finding 93) is the model.

---

## CLOSED TODAY, kept only for the method

- **`ragtime-nightingale` 12.13 → 1.58** (133). The shape LIED: the diff drifted to −5.35 then
  jumped +6.78, so this file used to say "one element between golden x 384.5 and 442.9 is
  12.13 too wide". Both engines put the barline at the SAME offset from the note before it,
  and both agreed on the accidental's left reach — `extraw=-12.13` against our `left=12.13`,
  identical. It was the SLACK AFTER the barline, spent on an ending that voice does not own.
  **When both engines agree on a quantity that is still 12px apart on the page, stop measuring
  the quantity and start measuring what CONSUMES it.**
- **`S8-layout-tune11` → exact on all four** (131, 132). Its `dy` looked like the one lane
  combination with a special rule, so the first move was to PROVE the lanes right — three
  controls against abcjs's own instrumented `staff.top`, ours exact on all three — which
  turned a mechanism hunt into a one-line ink probe. **Rule out the mechanism you suspect
  before hunting inside it.**
- **`S5-directives-tune1` 24.72 → 1.19** (129, 130). A clean ramp inside ONE system is missing
  FIXED width that justification spread.
- **`S6-keys-tune3`, `S8-layout-tune10`, `S1-decorations-tune4`** → exact (127, 126, 128).

## WHAT NOT TO CHASE

- **The harvested ranked table.** Still 0 of 174. It is a regression net, not an instrument.
- **The cancellation line** — DONE, finding 125. And `-08-06b`'s description of it was
  WRONG in one respect: abcjs draws the cancellation TWICE, once at the end of the previous
  system and once in the next system's prefix. Do not re-derive it from that section.
- **A "same signature prints nothing" rule** — there is none in abcjs (127). If a control
  seems to show one, it is the voice's own `K:` reaching an EMPTY voice child list, which is
  a different mechanism.
- **`!slide!` as a glyph** — it is a tie between two zero-width blanks (128).
- **A zero-duration note's SPACING** — already a quarter since finding 105. The `ponytail:`
  note at `noteGlyph` saying otherwise is stale.
- **`S5-directives-tune1`'s `dy`** — closed (129). Its `ox` 1.94 is a recorded raise that
  comes down with the ramp, not on its own.
- **The `-classes-tuneN` and `-print-tuneN` goldens.** Same tunes, same geometry, three times
  the rows. The gate enumerates the plain family on purpose.
- **The four-step staircase on `visual-layout-04`** — closed by finding 104.
- **`minPadding` and abcjs's `spacing * minSpace > 50` guard** — both measured as 0.
- **An arithmetic order-of-operations difference** (69) and **the staff line's residual
  0.002px** (70). Both ruled out.
- **The 20 biome errors from `npx biome check src tests`** — pre-existing, nearly all in the
  harvested goldens' embedded CSS. `src` alone is not clean either; see above.

---

## ONE THING MEASURED AND NOT CHASED

**Our staff LINES are 40px longer than abcjs's**, and no gate can see it — the pixel gate
compares noteheads, the line-weight gate reads THICKNESS only.

On `ragtime-nightingale`, every staff line resolves to `x=350.05 w=700.10` for us against
abcjs's `x=355.05 w=660.10` — ours spans 0 → 700.1 where abcjs's spans 25 → 685.1. The
noteheads on that fixture are exact, so nothing else moves with it.

abcjs draws from `params.startx` to `params.w` (`draw/staff-group.js:92` →
`draw/staff.js`), where `startx` is `getLeftEdgeOfStaff` — `padding.left` plus the voice-name
and brace reservation — and `w` is the staff group's right edge. Ours appears to span the
whole page width. Not started; noticed while diffing a control's full item list.

# HANDOFF — 2026-08-18

Read `Docs/CHECKPOINT-2026-08-18.md` for the why; this is where every gate stands, what to
do next, the harness, and the traps this session paid for.

**Branch `main`. Suite 1,839 passing, 2 expected-fail, NO reds. Everything pushed.**

---

## WHERE IT STANDS

**The API surface is 7 of 64 absent, from 12. Two gates CLOSED, two built from nothing, and
four ENGINE bugs that only EDITED text could have found.**

- **`Editor` and `EditArea` are BUILT**, with `engraver.rangeHighlight` under them — and
  their gate found four bugs in a family: a voice that runs out of measures used to take the
  mid-tune block, the brace, the bar number and EVERY LATER LINE of `tune.lines` with it.
- **`tune.makeVoicesArray` and `tune.addElementToEvents` are BUILT** — 4,151 of 4,208 rows
  over every in-repo fixture, from 1,902 when the gate opened.
- **`synth.SynthSequence` is BUILT** and gated call by call; it is the one of the seven
  `synth.*` that needs no audio.
- **`TimingCallbacks` is CLOSED — 4,696 of 4,696**, on one reserve: a tie leaving the system.
- **`deline` is at 1,566 of 1,570**, on two parser fields that did not exist (`style`, per-line `font`).
- **`setupEvents` is at 3,345 of 3,366**, and its remaining 21 rows are ONE mechanism.

---

## THE GATES

| Gate | Now | Report file |
|---|---|---|
| **`tune.lines` characters** | **255,684 of 255,684, 295 of 295 — CLOSED** | `/tmp/abcts-lines-ranked.txt` |
| **`AbcTune` accessors** | **0 of 291 — CLOSED** | `/tmp/abcts-accessors.txt` (`npx tsx scripts/zzacc.ts`) |
| **`engraver.selectables`** | **0 of 4 cases, 389 of 389 — CLOSED** | `/tmp/abcts-selection-ranked.txt` |
| **`extractMeasures`** | **1,663 of 1,663 — CLOSED** | `/tmp/abcts-extract-measures-ranked.txt` |
| **`startAnimation`** | **834 of 834 frames — CLOSED** | `/tmp/abcts-animation-ranked.txt` |
| **`TimingCallbacks`** | **4,696 of 4,696 — CLOSED (was 4,674)** | `/tmp/abcts-timing-callbacks-ranked.txt` |
| **`Editor`, fourteen steps** | **166 of 196 steps, 11 of 14 cases** | `/tmp/abcts-editor-ranked.txt` |
| **`makeVoicesArray`, ten columns** | **4,151 of 4,208 rows, 168 of 177 tunes** | `/tmp/abcts-voices-array-ranked.txt` |
| **`SynthSequence`** | **9 of 9 steps — CLOSED** | — (asserts directly) |
| **`tune.deline`** | **1,566 of 1,570 rows (was 1,558)** | `/tmp/abcts-deline-ranked.txt` |
| **`tune.setupEvents`, every column** | **3,345 of 3,366 rows (was 3,339)** | `/tmp/abcts-setupevents-ranked.txt` |
| SVG bytes, in-repo / sibling | 0 of 188 / 0 of 356 | `/tmp/abcts-svg-bytes-ranked.txt`, `-sibling-` |
| `metaTextInfo` / `metaText` | 0 of 310 / 0 of 368 | `/tmp/abcts-metatextinfo.txt`, `-metatext.txt` |
| `topText`/`bottomText` / `formatting` | 0 of 1,023 / 0 of 7,219 | `/tmp/abcts-toptext.txt`, `-formatting.txt` |
| `strTranspose` / harvested geometry | 0 of 59 / 0 of 177 | `/tmp/abcts-transpose.txt`, `-corpus-ranked.txt` |
| Pixel targets / DOM contract | 0 of 120 / 0 of 25 | `/tmp/abcts-pixel-ranked.txt`, `-dom-ranked.txt` |
| Audio / timings / element timings / chord grids / MIDI | 0 of 72 / 38 / 13 / 23 / 3 | — |
| **`setGlyph`** | 5 of 6 renders byte-exact | `/tmp/abcts-set-glyph-ranked.txt` |
| **abcjs API surface** | **7 of 64 absent (was 12)** | `/tmp/abcts-compat-surface.txt` |

⚠️ **A GATE'S REPORT FILE OUTLIVES ITS RUN.** `/tmp/abcts-*.txt` is stale until you re-run
the test that writes it. Run the suite once before reading any of them.

---

## WHAT TO DO NEXT, IN ORDER

### 1. THE OVERLAY MODEL — 21 + 14 rows, and TWO WRONG ANSWERS ALREADY MEASURED

Every open `setupEvents` row and the one open `makeVoicesArray` tune are this. Ours pads
every measure of a voice with ONE invisible rest of the measure's duration (`padOverlays`),
so a row reads `startCharArray [82,82,82]` where abcjs reads `[82,85]`.

**`tune.lines` ALREADY REPRODUCES abcjs EXACTLY** — `npx tsx scripts/zzovl.ts` against
`/tmp/gp/ovl.js` prints the same five lines of 3, 3, 3, 2, 2 voices for
`synth-flattener-21`, element for element. So the algorithm is ported; it is the MODEL that
is padded differently, and the fix is to SHARE that resolution rather than re-derive it.

⚠️ **Two re-derivations were tried and both are measurably wrong. Do not repeat them.**

1. **Per-line layering in `timing.ts` alone** — the layer voices lose the GEOMETRY join.
   The rows' `left`/`width` come from `geometryOf`, which is keyed by MODEL EVENT IDENTITY;
   synthetic copies are in no layout, so `synth-flattener-22`'s row 0 reported `left 49.051`
   for abcjs's `70.846` and the gate went from 3,345 to 3,309.
2. **The same rule inside `padOverlays`** (so all three consumers see it) — 3,324 rows, ONE
   byte fixture red (`flattener-21`'s clef moved 5px) and ONE audio case red
   (`flatten-rep-and-over`, a note at 8 where abcjs says 10). The audio one is the tell: it
   proves the rule "a line past the last `&` gets nothing" is WRONG. abcjs's pass walks
   EVERY line, so a line with no `&` is back-filled whenever ANY LATER line has one — not
   only before the first — and the guard `staff.voices.length >= s.voices.length` (current
   line's count against the earlier line's) decides how often.

So the shape to build is: resolve the overlays ONCE, in the model, by the same iterative
`while (resolveOverlays(…))` the projection already runs — including the guard — and let the
renderer, `flatten` and `timing` all read it. The three of them expand `measure.overlays`
identically today (`layout.ts:10751`, `timing.ts:430`, `flatten.ts:1103`), which is where
the shared answer has to land.

### 2. The four `synth.*` that need WEBAUDIO

`CreateSynth` (646 lines), `SynthController` (312), `CreateSynthControl` (226) and
`playEvent` (46). **The harness is the missing piece, and the animation gate is the pattern
for it**: a RECORDING AudioContext in place of jsdom — capture what is scheduled (buffer,
gain, start time) rather than samples — driven through the same fixed clock. `SynthSequence`
is already built and is what `playEvent` assembles.

### 3. `synth.sequence` — SPLIT the walk, do not copy it

abcjs's `sequence(tune, options)` returns the INTERMEDIATE — per voice, `{el_type, timing,
duration, pitches:[{pitch, name, verticalPos, highestVert}]}` and the `instrument`/`tempo`/
`key`/`meter` rows before them. Comparable exactly (dump it with `/tmp/gp/seq.js`). Ours
fuses sequencing into `flattenAudio`, so exposing it means splitting that walk so the
intermediate is REAL — a second copy is a second thing to drift, which is the rule
`selectables.ts` states.

### 4. `tuneMetrics` is NOT the small one it looks

`getMeasureWidths` lays the tune out at width **0** (`engraver-controller.js:139`) and reads
voice 0's bar positions. abcjs's width-0 layout COMPRESSES to the rods — `twinkle`'s first
measure is 70.035 wide where ours is 4 × 42.426 — and our layout has no such mode. Building
it means a compress-to-minimum path in the packer.

### 5. The rows that are measured and left

- **`Editor`, 2 cases**: `tune.warnings`. abcjs hangs the PARSER's warning strings on the
  tune (`abc_tunebook.js:87-89`) and its own text is HTML — `Music Line:1:3: Unknown
  directive: example: %%<span …>e</span>xample`. Ours carries diagnostics in its own shape
  and wording; matching the SET and the TEXT is its own arc.
- **`Editor`, 1 case**: an octave marker one ULP apart at `staffwidth` 300 —
  `306.81250000000006` against our `306.8125` on `visual-selection-03`'s trailing clef, with
  the clef glyph itself identical.
- **`makeVoicesArray`, 23 `w` rows**: abcjs expresses a whole-chord shift as per-head
  `shiftheadx` (`dx = 14.985` on BOTH heads, so `getMinWidth` is 29.97) where ours applies it
  AFTER `layoutNote` — inside that function `x`, `noteX` and `accidentalWidth` are all 0 and
  the head still draws 14.985 right of the element. `minWidthOf` now reads the width back off
  the DRAWN heads, which gets the magnitude right and lands one ULP away
  (`29.970000000000013`), because `(g.x - element.x) + w` is not abcjs's `dx + w`. Closing it
  needs the offset carried as a SUM out of the layout, not subtracted back out here.
- **`makeVoicesArray`, 25 rows of float noise** in `top`/`height`.
- **`deline`, 2 rows**: `%%maxStaves` — an undrawn line's `staff.clef` has no `abselem`,
  which is what makes abcjs's `objEqual` differ and `deline` unshift a clef. It is an
  ENGRAVER mutation leaking into a DATA comparison, and reproducing it means crossing the
  parse/layout boundary this library keeps.
- **`deline`, 2 rows**: `frere-jacques`'s prose spans.

---

## THE HARNESS

New this session:

| Script | Prints |
|---|---|
| `node scripts/harvest-abcjs-editor.mjs` | the fourteen-step `Editor` oracle |
| `node scripts/harvest-abcjs-voices-array.mjs` | `makeVoicesArray` over every in-repo fixture |
| `node scripts/harvest-abcjs-synth-sequence.mjs` | `SynthSequence`, nine calls |
| `npx tsx scripts/zzva.ts` | `makeVoicesArray`'s disagreement BY COLUMN and by element type |
| `npx tsx scripts/zzdrawn.ts` | every element group the drawing opened, with its `abcelem` — `F=<path>` |
| `npx tsx scripts/zzhl.ts` | `rangeHighlight` over one tune, printed like the abcjs probe |
| `npx tsx scripts/zzo.ts` | our markup for one FILE — `F=<path> [W=staffwidth]` |
| `npx tsx scripts/zzlines2.ts` | our `tune.lines` shape, key by key — `F=<path>` |
| `npx tsx scripts/zzblocks.ts` | which measure each prose block hangs on — `F=<path>` |
| `npx tsx scripts/zzel.ts` | raw layout elements with x / width / rod — `F=<path>` |

Probes in the scratchpad (`NODE_PATH=$(cd ../abcMusicKit/Tools/abcjs-debug/node_modules && pwd) ABCJS_PATH=/tmp/gp/abcjs node /tmp/gp/<script>`):
`two.js` (abcjs's markup for one file, `F=`, `W=`), `edit1.js`/`edit2.js` (what `Editor` and
`rangeHighlight` do to the DOM), `edit3.js` (which groups a highlight paints), `edit4.js`
(every voice child with its char range), `dur2.js` (`makeVoicesArray` with `duration` and
`durationClass`), `kids.js` (an element's children with `dx`), `ext.js` (staff and voice
extents), `seq.js` (`synth.sequence`, cycles stripped), `del2.js` (`deline`'s `font` and
`style` elements), `clefs2.js` (each line's `staff.clef`), `heights.js` (abcjs's SVG height
per tune of a book), `lines2.js` (abcjs's `tune.lines` shape).

⚠️ **`two.js`, the editor harvester and the voices-array harvester ALL RENDER, so they all
need `dump-svg.js`'s `getBBox` STUB.** Without it abcjs lays a tune out 1.78px differently.

---

## THE TRAPS THIS SESSION PAID FOR

### ⚠️ /tmp/gp WAS DELETED TWICE IN ONE SESSION, AND THE SECOND TIME COMPLETELY

The handoff before this one said `src/` survives and the top-level files go. That is the
OPTIMISTIC case: this session found `src/` present but EMPTY, and later the whole `/tmp/gp`
gone mid-session. Restore with

    R=../abcMusicKit/Docs/References/abcjs/abcjs-6.7.0
    mkdir -p /tmp/gp/abcjs && cp -R $R/src /tmp/gp/abcjs/ && cp $R/{index,version,license,plugin}.js $R/package.json /tmp/gp/abcjs/

and re-copy `bbox-stub.js` (it is `dump-svg.js`'s stub, verbatim). Instrumentation does NOT
survive; never instrument `../abcMusicKit` itself. **The harvesters read the VENDORED tree,
so goldens are unaffected either way.**

### ⚠️ A GATE THAT RENDERS *EDITED* TEXT REACHES WHERE THE BYTE GATES CANNOT

Four engine bugs, all in one family, none reachable by rendering a fixture as written. The
byte gates are 0 of 188 and 0 of 356 and were green through every one of them. **If a
surface can be driven, drive it — the fixture is the input, not the test.**

### ⚠️ DELTA DEBUGGING CAN REDUCE TO A DIFFERENT BUG

The subtitle case reduced to four lines with no `X:` and no `K:` — and that reduction was a
DIFFERENT mechanism (a header-less tune), not the one in the full file. The reduction that
mattered was reached by writing candidate tunes from the SOURCE STRUCTURE instead: three
voices, a mid-tune `T:` between two of them, one music line after. Check that a reduction
still fails for the reason you are chasing.

### ⚠️ A TYPE'S `| null` IS PART OF THE PORT

`StaffConnector` is `'start' | 'continue' | 'end' | null`. The connector chain ported from
abcjs skipped only `undefined`, so a `null` marker opened a bogus connector — 85 tests and
31 byte fixtures red in one run, from a rule that was otherwise correct.

### ⚠️ A VISUAL BASELINE IS OURS, NOT AN ORACLE — CHECK IT AGAINST abcjs BEFORE RE-RECORDING

`S7-voices` moved 7.578px. It is EXCLUDED from the byte gate for a stale golden, so nothing
would have caught a wrong answer; abcjs was run live and agreed with the new one to the
digit before `ABCTS_SNAPSHOT_RECORD=1` was typed.

### ⚠️ THE RATCHET LIST IS COMPUTED FROM A REPORT THAT MUST BE FRESH

Twice this session a `PASSING` list was regenerated from a `/tmp/abcts-*-ranked.txt` written
by an EARLIER run, and both times the next suite run failed on a case that had since moved.
Re-run the gate, then re-ratchet, then run the suite.

---

## THE METHOD, UNCHANGED

Read the named abcjs function FIRST, then instrument to confirm it — **neither half works
alone**, and this session paid for both again:

- `rangeHighlight`'s DOM effect was read in `set-class.js` and then measured in jsdom; the
  empty `class=""` it leaves behind is only in the measurement.
- The tie's `-2` was measured (`ext.js`) and only then found in
  `set-upper-and-lower-elements.js:140-146`; the source alone reads as if the ink box did it.
- `durationForSpacing` looked like `durationClass` for eleven fixtures and parts from it on
  a `Z4`. One probe over one fixture is what said so.

Run every command from `/Users/lrettberg/ICMLabs/Code/abcts`. Run `npx tsc --noEmit` BEFORE
every commit — vitest transpiles without typechecking and will not tell you. Commit and push
after every landing; do not batch. Re-ratchet each gate's PASSING list in the same commit.
Never push `--force`; if a push is rejected, stop and report. Omit `Co-Authored-By` trailers
in this workspace.

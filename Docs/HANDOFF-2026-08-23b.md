# HANDOFF — 2026-08-23b

**Branch `main`. Suite 2,118 passing, 4 EXPECTED-FAIL, no reds. Everything pushed.**

Thirteen commits. **FIVE FEATURES, ONE GATE DEFECT, ONE REVERT** — and the revert is the row
worth reading first, because the fix that looks right is wrong and the probe that says so is
already written down.

**EVERY GATE IN THIS REPO READS ZERO EXCEPT ONE, AND THAT ONE READS ONE.** The byte table is
1 of 383 and its cause has been named since 2026-08-21.

**START HERE:** WHAT IS LEFT is the whole brief. §1 is the one byte row, §2 is the owner
decision this repo has carried since 2026-08-21, §3 is the inline-`[V:` rule with its next
probe named, and §4 is what the `AbcElement` sweep left.

## THE GATES

| Gate | Now |
|---|---|
| **SVG bytes, in-repo** | **1 of 383 fixtures** — see §1 |
| SVG bytes, sibling | 0 of 356 tunes |
| `tune.lines` | 606,481 of 606,481 characters, 0 of 499 tunes |
| harvested corpus | 0 of 215 fixtures |
| `parseOnly` field shape | 2,461 of 2,461 rows, 0 of 507 |
| `parseOnly` VALUES | 0 of 13,314 |
| RENDERED values | 0 of 13,314 |
| **`renderAbc({wrap})`** | **0 of 11 — NEW, three surfaces** |
| `tune.deline` | 2,456 of 2,456, 0 of 998 |
| warnings | 0 of 507 |
| `tuneMetrics` | 255 of 255 sections, 0 of 261 |
| `makeVoicesArray` | 4,842 of 4,842, 0 of 215 |
| `setupEvents` / `TimingCallbacks` / `extractMeasures` / `selectables` | 0 |
| audio / MIDI / chord grids / DOM / pixel / editor / animation / synth | 0 |

215 fixtures in `tests/corpus-abcjs/fixtures`, 383 per-tune byte goldens.

⚠️ **A GATE'S REPORT FILE OUTLIVES ITS RUN.** `/tmp/abcts-*.txt` is stale until you re-run
the test that writes it. ⚠️ **AND A RED FULL-SUITE RUN IS NOT NECESSARILY RED** — use
`npx vitest run --maxWorkers=2` and re-run before believing one.

⚠️ **AND FOUR EXPECTED-FAILS ARE BACK ON PURPOSE.** `tests/inline-voice-line.test.ts` holds
abcjs's numbers for a rule that is measured and not built; they go red when it lands. That
is the device `chord-tie.test.ts` established and it is not a regression.

## WHAT IS LEFT

### 1. ONE BYTE ROW, AND IT IS THE ONE FROM 2026-08-21

`abcts-directives-tune4` — `%%stafftopmargin 30` on a tune WITH a title — differs in the
last bits of the root `height` (`201.76700000000002` against `201.767`). The y-versus-pitch
asymmetry a staff carrying a HEADING BLOCK already has, recorded at `verticalExtent` since
2026-08-11, and **the deferred refactor rather than a new defect**.

### 2. THE `abselem` DECISION IS STILL THE OWNER'S

`scripts/zzrv.ts` reads 10,570 rows differing ONLY by an absent `abselem`/`staffGroup`.
Publishing them means RETAINING THE `Layout`, which measurably killed this suite's workers
once (2026-08-16: 5.6s → 50-120s). A host that wants the drawn element has
`getSelectableArray`. **Do not start it without a decision.**

### 3. THE INLINE `[V:` LINE RULE — MEASURED, NOT BUILT, AND THE OBVIOUS FIX IS WRONG

`tests/inline-voice-line.test.ts`. A `[V:x]` written after music on the same LOGICAL line
opens a new system in abcjs and not here:

    shape                                          abcjs  ours
    CDEF|[V:1]GABc|            (no V: anywhere)        2     1
    V:1 header, then CDEF|[V:1]GABc|                   2     1
    [V:1]CDEF|[V:1]GABc|                               2     1
    CDEF|\  +  [V:1]GABc|                              2     1
    [V:1]CDEF|\  +  [V:1]GABc|                         1     1   ✓
    [V:1]CDEF|  +  [V:1]GABc|                          2     2   ✓

The rule is neither "a repeat switches voice" nor "a continuation suppresses the break" —
the two agreeing rows straddle both. What separates row 4 from row 5 is whether the LOGICAL
LINE ITSELF OPENED with a `[V:1]`.

⚠️ **DO NOT REMOVE `selectVoice`'s EARLY RETURN — IT WAS TRIED AND REVERTED.** It takes all
six shapes to abcjs's answer and takes `abcjs-visual-parsing-03-v-1-f` and
`-09-score-t-b` — two of abcjs's OWN test tunes, both `\`-continued — from byte-exact to
differing.

⚠️ **AND `setCurrentVoice`'s GUARD IS NOT THE MECHANISM — PROBED, NOT ASSUMED.** The
obvious reading is `if (currentVoice.index === … && staffNum === …) return // there was no
change` (`abc_parse_key_voice.js:526-531`). A `console.error` on both arms says abcjs takes
**EARLY RETURN in row 4 and row 5 alike**, the two rows that disagree with each other.

**THE NEXT MOVE:** instrument `abc_parse_music.js`'s inline-field CALLER, not `parseVoice`.
`case "[V:"` returns a fourth element `needsNewLine` whose own `startNewLine()` is
**COMMENTED OUT** (`abc_parse_header.js:400-405`), so the break is raised by whoever reads
that return value. Find the caller before touching anything.

### 4. WHAT THE `AbcElement` SWEEP LEFT

abcjs's `types/index.d.ts` against what both corpora produce, parsed AND rendered. Still
open, reachable from ABC, produced by no corpus tune:

| Field | Where it comes from |
|---|---|
| pitch `style` | a per-PITCH notehead override the engraver reads (`abstract-engraver.js:679`) |
| grace `startBeam`, `startTie`, `endTie`, `style` | a tie or a beam inside `{}` |

⚠️ **AND THREE DECLARED FIELDS ARE DEAD** — `noStem`, `stemConnectsToAbove` and
`beat_division` appear ZERO times in abcjs 6.7.0's source. Do not open those rows.
⚠️ **AND TWO FONTS ARE UNREACHABLE** — a bar's `measurefont`/`repeatfont` are written to an
accumulator abcjs discards one line later (`abc_parse_music.js:305-309`).

### 5. `K:C clef=none`'s ONE STEM — STILL ON NO GATE

⚠️ **THE STEM'S OWN EXPRESSION IS NOT THE OBSTACLE — THREE ATTEMPTS SAY SO.** The drift is
upstream, in the NOTE's x: ours is `48.7950000000000017` where abcjs's is
`48.795000000000003979`. Instrument abcjs's `layoutOneItem` and read the `er`/`extraWidth`
arm; do NOT touch the stem.

### 6. THE `ponytail:` LEDGER — 100 ENTRIES

`grep -rn "ponytail:" src tests scripts`. Its fourth sweep found nothing, which is what sent
the fifth pass at abcjs instead. Several entries added today.

---

## THE HARNESS

| Script | Prints |
|---|---|
| `D=<dir> O=<dir> npx tsx scripts/zzdecs.ts` | our SVG for every `<i>.abc` in a directory; `<dir>/names.json` names the cases |
| `ABCJS_VERSION=6.7.0 node ../abcMusicKit/Tools/abcjs-debug/dump-svg.js --file x.abc --output x.svg` | abcjs's, at the goldens' own `{staffwidth: 670}` |
| `node scripts/harvest-abcjs-goldens.mjs --only <slug>` | regenerate ONE fixture's byte goldens |
| `npx tsx scripts/harvest-abcjs-wrap.ts` | the `wrap` oracle — explanation, lineBreaks AND the SVG |

⚠️ **`ABCJS_VERSION` IS NOT OPTIONAL — `dump-svg.js` DEFAULTS TO 6.6.3.**

⚠️ **AND `zzdecs` RENDERS AT ITS OWN DEFAULT WIDTH.** A control that needs a specific
`staffwidth` has to go through `renderAbc` directly; comparing a `zzdecs` render against a
golden made at another width reads as a defect and is not one.

⚠️ **AND `cd` MATTERS MORE THAN IT LOOKS.** Several probe runs reported clean sweeps that
were really "the reference never rendered", because the shell had reset and `../abcMusicKit`
resolved one level off. Check the reference file EXISTS before reading a column of failures.

**THE SCRATCHPAD abcjs IS AT `/tmp/gp/abcjs`** and currently carries a `ZZCV` probe in
`setCurrentVoice` (§3) plus the `ZZTOPA` one in `incTop`. Copy the reference to `/tmp/gp/abcjs`,
add the probe, then copy `dump-svg.js` INTO `Tools/abcjs-debug/` as `zzprobe-dump.js` with
its `abcjsPath` repointed, and run THAT. ⚠️ **`zz-dump-probe.js` IS ALREADY A TRACKED FILE
THERE** — name your copy something else; that is a DIFFERENT repo and an `add -A` there
would commit it.

The probes worth re-adding, with where they go:

| Env | Site | Answered |
|---|---|---|
| `ZZTOPA` | `incTop`, `set-upper-and-lower-elements.js` | the above ladder, rung by rung |
| `ZZCV` | `setCurrentVoice`, `abc_parse_key_voice.js` | whether a `[V:]` repeat switches — it does NOT |
| `ZZMOVE` | `renderer.moveY` | the page cursor, move by move — **reach for this first on any vertical question** |
| `ZZW` | `absolute-element.js` `addRight` | which children set `abselem.w` |
| `ZZLX` | `layout/voice-elements.js` before `child.setX(x)` | the x, `getMinWidth` and `minspacing` per element |

---

## THE METHOD, UNCHANGED

**Read the named abcjs function FIRST, then instrument to confirm it — neither half works
alone.** Run every command from `/Users/lrettberg/ICMLabs/Code/abcts`. Run `npx tsc
--noEmit` BEFORE every commit. After regenerating ANY golden, **diff it against `HEAD` and
assert that only NEW KEYS appeared** — `+N / -0 / ~0`. Re-ratchet each gate's PASSING list
in the same commit: `svg-bytes`, `lines` (list AND character floor), `corpus-abcjs`'s
`WITHIN` counts, `tune-metrics`, `voices-array`. Commit and push after every landing; never
`--force`. Omit `Co-Authored-By` trailers in this workspace.

# HANDOFF — 2026-08-16

Read `Docs/CHECKPOINT-2026-08-16.md` for the why; this is where every gate stands, what to
do next, and the three traps this session paid for.

---

## THE GATES

| Gate | Now | Report file |
|---|---|---|
| **SVG bytes, in-repo** | **0 of 188** | `/tmp/abcts-svg-bytes-ranked.txt` |
| **SVG bytes, sibling — all five flavours** | **0 of 356** | `/tmp/abcts-svg-bytes-sibling-ranked.txt` |
| **`strTranspose`** | **0 of 59** | `/tmp/abcts-transpose.txt` |
| Harvested geometry | 0 of 177 | `/tmp/abcts-corpus-ranked.txt` |
| Audio / timings / element timings / chord grids / MIDI | 0 of 72 / 38 / 13 / 23 / 3 | — |
| Pixel targets / DOM contract | 0 of 120 / 25 | — |
| **abcjs API surface** | **27 of 64 absent** | `/tmp/abcts-compat-surface.txt` |
| **`tune.lines` characters** | **250,775 of 256,138**, 177 tunes ratcheted | `/tmp/abcts-lines-ranked.txt` |
| **`engraver.selectables`** | **151 of 389**, `selection-clefs` ratcheted | `/tmp/abcts-selection-ranked.txt` |
| **`AbcTune` accessors** | 2 of 291, both written down | `/tmp/abcts-accessors.txt` |

Suite **1798 passing, 2 expected-fail, 1 red**. The red is `content-parity` at 44 of 46 and
**neither diff is ours** — `S7-voices` and now `multi-voice-rest-placement`, both fixtures
edited in `../abcMusicKit` with goldens from 08-08. Checked by `git stash` and by `ls -la`,
not assumed. The two expected-fails are `tests/chord-tie.test.ts`, which asserts what abcjs
does and goes RED when the gap closes.

---

## WHAT TO DO NEXT, IN ORDER

### 0. TWO THINGS MEASURED LATE IN THE SESSION, BOTH CHEAP AND BOTH BLOCKING

- **A `tempo` AND A `part` HAVE NO SOURCE RANGE IN THE MODEL**, so the walk produces
  nothing for them and — because the gate compares row against row — **every entry after
  one is misaligned**. That is the whole of what `selection-tempo` still lacks on the
  element side (its rows 0 and 1 are exact today). The parser change is small: `Q:` reaches
  `builder.voice.setTempoChange(parseTempo(value))` at `parser.ts:3087-3091` with the
  field's range in scope, and `P:` already has `partLabelSourceRange` on the measure. Both
  also close characters on the lines gate, where they are two of the named six.
- **A KEY SIGNATURE'S `accidentals` ARE `{acc, note, verticalPos}` and `note` IS THE NAME
  AT THE POSITION DRAWN** — B♭ major gives `B` and `e`, upper and lower case in one list,
  moving with the clef. Measured, written at the code site in `src/compat/selectables.ts`,
  and deliberately not built.

### 1. Finish the selectables — the other TEN `add` sites

`selection-multiple` (221 rows) and `selection-tempo` (8) are `selectTypes: true`, where
every drawn thing is selectable. We emit the notes and rests; abcjs also adds, at
`write/draw/`:

| Site | `abcelem` |
|---|---|
| `non-music.js:25,41` | the text rows — `{el_type, name, startChar, endChar, text}` |
| `voice.js:20` | `{el_type: "voiceName", startChar: -1, endChar: -1, text}` |
| `brace.js:102` | `{el_type: "brace"\|"bracket", startChar: -1, endChar: -1}` |
| `ending.js:51` | `{el_type: "ending", startChar: -1, endChar: -1}` |
| `triplet.js:14` | `{el_type: "triplet", …}` |
| `tie.js:28` | `{el_type: "slur", startChar, endChar}` |
| `crescendo.js:21`, `dynamics.js:16` | `{el_type: "dynamicDecoration", …, decoration}` |
| `glissando.js:24` | `{el_type: "glissando", …}` |
| `absolute.js:55,62,65` | every element — including `bar`, `tempo`, `part` and the staff-extra clef / key / meter |

**THE ORDER IS ALREADY PROVEN** — `data-index` is byte-exact on 544 rows across both
corpora — so this is about producing each `abcelem`, not about where it goes. Every shape is
in `tests/corpus-selection/golden.json`, measured. `scripts/zzsel.ts` counts what is open by
field; run it with `ONLY=<slug>` or a misaligned case drowns the table.

The staff-extra clef / key / meter need the STAFF's objects (`staff.clef`, `staff.key`,
`staff.meter`), which is the same distinction §3.4 of the checkpoint landed: a standalone
field line is the staff's, an inline one is the stream's.

### 2. The 119 open `tune.lines` rows — the same two defects, now named twice

- **Six element types have no source range in the model** — `tempo` (inline `[Q:]`),
  `clef`, `midi`, `style`, `color`, `part`. They also block `selection-multiple`, so this
  is the highest-leverage item on the list.
- **An element's span stops short of a trailing `-` or `)`** — `[G,D]/4-` is 272…281 in
  abcjs and 272…279 in ours; `ragtime-nightingale` loses 1,273 characters to it alone. It
  is a PARSER change under gates that read source offsets.

### 3. The three things measured and written down rather than built

Each is gated so it cannot be forgotten, and each fails the moment it is fixed:

- **A chord's ties are per pitch** (`tests/chord-tie.test.ts`, two `it.fails`) — the
  RENDERER has the rule and the flattener does not.
- **`%%maxStaves` truncates the CLOCK**, because `makeVoicesArray` walks the DRAWN
  staffgroups.
- **An `&` overlay's `end` row is a whole note late** while every EVENT row matches.

### 4. The WebAudio and editor surface

`CreateSynth`, `SynthController`, `CreateSynthControl`, `SynthSequence`, `TimingCallbacks`,
`Editor`, `EditArea`, and the small ones. IN by the ruling. **For the ones that make sound
the contract is the API surface and the EVENT SEQUENCE — already 0 of 72 — not the
samples**, and that has to be stated rather than fudged.

### 5. The stragglers

`extractMeasures`, `tuneMetrics`, `setGlyph`, the three animation functions, and the tune
object's `lines`-derived accessors (`topText`, `bottomText`, `formatting`, `metaTextInfo`,
`getKeySignature`, `deline`, `makeVoicesArray`, `setupEvents`, `meter`).

`%%wrap` is not in `src/` at all and `visual/wrap.test.js` is 9 cases.

---

## THE THREE TRAPS THIS SESSION PAID FOR

### DO NOT RETAIN A `Layout` — IT KILLS THE SUITE'S WORKERS, AND IT READS LIKE A FLAKE

The selectables need the drawing, and the first cut kept it in each `TuneObject`'s closure.
A `Layout` is the biggest object this library makes and a host keeps every tune a render
returns: the full suite went from **5.6s to 50-120s and failed a DIFFERENT test on every
run** — pixel parity's ranked table, then a byte-exact sibling slug, then another. Nothing
about the failures said "memory"; they read as intermittent value differences. It is laid
out AGAIN inside the lazy accessor now (0.7ms, only when asked) and two consecutive full
runs are clean.

**And a rarer flake does exist underneath it** — one run in two failed `pixel-parity` with
the whole change stashed. Worth its own probe: different test each time across different
FILES is the shape of a worker dying, not of an assertion.

### THE GOLDEN GENERATOR'S FONT-HEIGHT TABLE HAS SEVEN ROWS

`dump-svg.js`'s `fontHeights` covers the seven sizes abcjs's DEFAULTS resolve to; anything
else falls through to `size + 2` where a browser gives about `size × 1.108`. **46 text
elements across both corpora set a size outside those seven.** Our engine reproduces the
fallback on purpose (`GOLDEN_TEXT_HEIGHTS`) so nothing is inconsistent here — but the
goldens and a browser part company on those rows, and that is now written down rather than
being a surprise the next time an oracle disagrees. `dump-svg.js` is in `../abcMusicKit`,
which another lane owns; do not edit it.

### A DISAGREEMENT WITH THE ORACLE IS EVIDENCE ABOUT THE HARNESS TOO

Nine of the eleven rows where abcjs's own suite disagrees with abcjs were OUR text metrics,
and the way to tell was to VARY the suspected input in a scratchpad copy and watch: the
obvious suspect — the `%%titlefont … 22.0` the tune actually sets — moved nothing at all,
and the real one (the 29px it resolves to) reproduced the test file's number to the digit.
**A fixture's own directive is not evidence, any more than its name is.**

### A RATCHET THAT NAMES ROWS CATCHES WHAT A COUNT CANNOT

The script that re-ratchets `tests/lines.test.ts` split the ranked report on whitespace,
which turned `sib/little swallow-tune0` into `sib/little` — a FAILING tune on the passing
list under a name nothing matches. The next run went red on it. The fix is in
`/tmp/relines.mjs`: strip the trailing ` n/m` instead of splitting.

---

## THE HARNESS

Ours, all gated on their own env var: `ABCTS_Y`, `ABCTS_H`, `ABCTS_PL`, `ABCTS_PLR`,
`ABCTS_SP`, `ABCTS_XX`, `ABCTS_PROBE`, `ABCTS_CHECK`, `ABCTS_ABSY`, `ABCTS_TEMPOX`.

Scripts (all run with `npx tsx`, from `/Users/lrettberg/ICMLabs/Code/abcts`):

| Script | Prints |
|---|---|
| `zzsel.ts` | **every open selectable row, counted BY FIELD** — `ONLY=<slug>` for one case |
| `zzr.ts` | an IN-REPO fixture rendered like the byte gate → `/tmp/ours.svg` |
| `zzc.ts` / `zzpm.ts` / `zzs.ts` | a SIBLING fixture with `add_classes` / in PRINT / STACKED |
| `zzacc.ts` | every accessor row that differs from abcjs |
| `zztr.ts` | every `strTranspose` case that differs |
| `zzlines.ts` | every character where `getElementFromChar` differs |
| `zztim.ts` / `zzev.ts` / `zzflat.ts` | our timing rows / parsed durations / flattened MIDI |
| `zzh.ts` | one tune's rendered page height |
| `render-all.ts` (in `tests/`) | **one output SLOT per tune**, the way abcjs's generator asks |

Harvesters (`node scripts/…`): `harvest-abcjs-selection.mjs` is the new one — it evaluates
abcjs's test file for its inputs and RUNS abcjs for the answers.

abcjs, in the SCRATCHPAD COPY at `/tmp/gp/abcjs` — **never `../abcMusicKit`**, which
another lane owns. Run with
`NODE_PATH=$(cd ../abcMusicKit/Tools/abcjs-debug/node_modules && pwd) ABCJS_PATH=/tmp/gp/abcjs node /tmp/gp/<script>`:

| Script | Prints |
|---|---|
| `twin.js` | **every fixture through BOTH abcjs trees, diffed — run this after instrumenting** |
| `seldump.js` / `seldump2.js` | which elements abcjs marks selectable, and at what DEPTH |
| `bbox-stub.js` | `dump-svg.js`'s text-metric stub, extracted so a probe can VARY it |
| `surface.js` | abcjs's whole public surface, walked from the object |
| `accessors.js` | the nine numeric accessors per tune over a directory |
| `lines.js` / `charmap.js` | a tune's element stream / `getElementFromChar` for every char |
| `tim.js` / `flat.js` | `setTiming` rows and `makeVoicesArray` sizes / `setUpAudio` tracks |
| `one.js` / `dur.js` | one tune's meter, tempo, accessors, per-line staff meters / durations |
| `h.js` / `svgdiff.js` | a tune's page height / two tunes' markup diffed |
| `walkbook.js` | `dump-tunebook-svg.js` with `ABCJS_PATH` overridable — STACKED renders |

Goldens are generated from the VENDORED tree, never the scratchpad:
`NODE_PATH=… ABCJS_VERSION=6.7.0 node ../abcMusicKit/Tools/abcjs-debug/dump-svg.js --file <fixture> --output <out>`.
**`ABCJS_VERSION` is not optional — it defaults to 6.6.3.**

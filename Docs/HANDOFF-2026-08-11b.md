# HANDOFF — 2026-08-11b

## The session prompt — paste this

```
We are continuing abcts development in the abcts repo (Code/abcts).

THE GOAL, in Lance's words: abcts exists to build an abcjs-modern whose output — the
SVG FILE and the AUDIO — is 100% BYTE-EQUAL to abcjs 6.7.0. A tolerance is not a
compromise to be balanced against effort; it is A DEFECT THAT HAS NOT BEEN WRITTEN
DOWN YET. Anything you decline to reproduce goes in Docs/ABCJS-DIFFERENCES.md with its
evidence, and its slug goes in svg-bytes.test.ts's DIVERGENT list. That list is EMPTY
and stays empty. Work until byte-equal, without stopping for approval or status.
Checkpoint, hand off and compress as you go so no context is lost.

AND: abcjs BEHAVIOUR is what we are shooting for — we can optimise later. Take abcjs's
arithmetic, its order and its data model even where a better one exists. AS YOU DECIDE
TO USE AN abcjs NON-OPTIMAL SOLUTION, DOCUMENT IT FOR FUTURE REFACTOR: one
`abcjs-debt:` line at the site and an entry in Docs/ABCJS-DEBT.md naming the gate that
goes red if someone "fixes" it. That file is a THIRD CLASS, distinct from
ABCJS-DIFFERENCES.md (what we refuse to reproduce) and from `ponytail:` (our own
shortcuts).

Read Docs/CHECKPOINT-2026-08-11b.md first — the state, §3 WHAT IS LEFT (the STRUCTURAL
rows are now the majority and §3.1 names both remaining ULP threads with their
measurements), §5 THE HARNESS, §6 the rules. Then Docs/HANDOFF-2026-08-11b.md. Then
CHECKPOINT-2026-08-11.md for the twelve landings, its §4.2 structural list with abcjs's
citations, and its §3 — noting that its "a beamed stem must not supply a pitch" was
right about the symptom and wrong about the cause, corrected in -08-11b §2.
Then -08-10d.md for the staff-frame recipe and the unit flip, -08-10c.md for v1's
answer to the round trip, and CLAUDE.md.

RUN EVERY COMMAND FROM /Users/lrettberg/ICMLabs/Code/abcts. `cd` does NOT persist
between tool calls, and the workspace ROOT has its own vitest reach — run from there
and it collects every test in every sibling repo, abcjs's own included.

`npx tsc --noEmit` BEFORE `git commit`, not alongside it. `npx biome check src` is NOT
clean and never was.

AN ASSOCIATION IS A DECISION. `a + b + c` is `(a + b) + c`, and abcjs's grouping is
part of the port. One pair of parentheses in the emitter closed nine fixtures.

COUNT TOKENS BY AXIS, NOT FIXTURES. While the family is ULP the fixture count is the
wrong dial: 85 -> 85 hid a 180-token improvement and 82 -> 82 hid a 48-token one.
/tmp/gp/axis.mjs is the table; rebuild it first (§5).

PLACE, DON'T SHIFT. Wherever the layout moves something into position, ask whether
abcjs adds a STORED offset to a SOLVED coordinate — `child.x = x + this.dx`. Four of
the last session's seven landings are that one question on four different quantities,
and a fifth (the tempo notehead) is named and waiting.

MEASURE, DON'T READ — and when the source is not enough, INSTRUMENT abcjs, in a
SCRATCHPAD COPY, never ../abcMusicKit. §5 lists the five sites worth re-adding.
And do NOT read numbers from a site that runs in a MEASURING pass: `layoutEvent`'s
headX is called eight times per line with x's that never reach the page.

A WASH IS NOT "NO EFFECT", and TWO ERRORS CANCELLING is now FIVE times on this branch.

NEVER REMOVE A SLUG FROM svg-bytes' PASSING LIST — it names all 104 byte-exact
fixtures, and it holds them because seven could not. It has caught four regressions
that the aggregate count hid. Regenerate it (grow only) when a batch lands.

A REFACTOR IN THIS FAMILY CAN MOVE REAL PIXELS, not only ULPs: placing elements while
still SHIFTING their beams put 8.51px of dy on ragtime-nightingale. Re-read
pixel-parity and the harvested table after every step.

NEVER `git checkout -- <file>`. COMMIT A BIG CHANGE THE MOMENT ITS GATES GO GREEN.
```

## Where it stands

**NINE RANKED TABLES. ONE IS OPEN: `svg-bytes`, 67 of 171**, from 94 when this session
opened, with **104 fixtures byte-exact and all 104 RATCHETED** (up from seven). Best 52490,
median 8185.

Everything else is at zero: audio 0/72, note timings 0/38, chord grids 0/23, MIDI files 0/3,
harvested geometry 0/174, pixel targets 0/120; element timings 1/13 (abcjs being
idiosyncratic, NAMED); DOM contract 1 of 25 with 24 slugs ratcheted. Suite **1255/1255**,
`tsc` clean, everything committed and pushed.

The open table splits **33 structural + 13 glyph-x + 8 root + 7 other + 1 glyph-y**. By
TOKEN the two ULP families are now SMALL and comparable: glyph-y **62** across 10 fixtures,
glyph-x **36** across 13 — from 265 across 33 at the session's midpoint. **The structural
rows are now the majority of the table.**

## The loop

```bash
cd /Users/lrettberg/ICMLabs/Code/abcts
npx vitest run                                   # writes every ranked table
head -1 /tmp/abcts-svg-bytes-ranked.txt          # the open one
head -1 /tmp/abcts-pixel-ranked.txt              # must stay 0 of 120
head -1 /tmp/abcts-corpus-ranked.txt             # must stay 0 of 174
```

After ANY geometry change: `npm run baseline`, then **READ the diff's SHAPE** — a new
feature only ADDS, a reorder is a pure PERMUTATION, and REMOVALS in either case mean
something broke **until a corpus-wide COUNT says otherwise**.

## Where to start

**THE STRUCTURAL THIRTY-THREE ARE NOW THE MAJORITY OF THE TABLE**, and each is listed with
abcjs's own citation in `CHECKPOINT-2026-08-11.md` §4.2. The **brace with a header** (three
fixtures, `draw/brace.js:78-98`) is the largest single one and needs no new model. That is
where a session buys the most now: the two arithmetic families are down to 98 tokens
between them.

If you want to finish the arithmetic first, **both remaining threads are named and measured
in §3.1**, and both are the same finding as this session's:

- **THE TEMPO NOTEHEAD**, four fixtures × 2 tokens. abcjs draws it at `calcY(pitch)` where
  `pitch` is the above-ladder's own running `staff.top` (15.796645161290321 on
  `synth-flattener-25`); ours SHIFTS the whole tempo element by a delta off the TEXT
  baseline. `aboveLadder` needs to return the rung's PITCH beside its y — `spend()` already
  has it in hand. **PLACE, DON'T SHIFT.**
- **`visual-transpose-03`'s last token**, the `^^F` head's x. Its accidental is EXACT and
  its head is not, so the residual is in what `headX` is BUILT from — `(x + graceWidth) +
  accidentalWidth` — now that the solve itself is abcjs's. `PlacedGlyph.dx` is the tool:
  construct the offset, do not derive it.

## What this session did, in one line each

`svg-bytes` 94 → 67 of 171 on seven landings, **every one of them the same finding — abcjs's
ARITHMETIC IS PART OF THE PORT**: which number is formed first, which product is taken once,
which offset is stored rather than derived.

- **A GLYPH'S y IS ONE VALUE BEFORE THE OUTLINE SEES IT** — `-3.96 + -11.625 + 84.56` is
  `68.975` where `-3.96 + (-11.625 + 84.56)` is `68.97500000000001`. Nine fixtures, one pair
  of brackets.
- **THE VERTICAL PAGE WALK IS ABCJS'S OWN CURSOR, IN PITCH** — the staff origin as one
  product, `addStaffPadding` as a pitch sum with one multiply, the intra-group separation
  folded into `staff.top`, and the page as one cursor seeded with `padding.top`.
- **A BEAMED STEM RESERVES THE BEAM'S OWN PITCH** — `pitch2: bary` out of `getBarYAt`,
  which interpolates two pitches. The previous checkpoint's "must not supply a pitch" was
  right about the symptom and wrong about the cause.
- **THE ABOVE STACK IS A PITCH WALK, INK TOP INCLUDED** — and on its own it was a WASH that
  cost a byte-exact fixture, because the reciprocal-multiply it replaced had been cancelling
  a latent defect in the ornament's own reserve. All three producers together closed it.
- **THE LINE SOLVE ITERATES ON ABCJS'S SPACING, NOT ON A FACTOR** — `spacing` is one number
  replaced outright each pass, where ours carried a ratio to the 30px base and
  re-multiplied it at every spring. **Eleven fixtures on four lines**, the session's
  largest step.
- **PLACE AN ELEMENT ON THE SOLVED x, DON'T SHIFT IT THERE** — `child.x = x + this.dx`. And
  one offset has to be BUILT rather than derived: a flag's `dx` is `headx + notehead.w -
  0.6`, and `(x + a) - x` is not `a`.
- **THE RATCHET NOW NAMES ALL 104 BYTE-EXACT FIXTURES.** Seven could not defend eighty-nine:
  two fixtures regressed early in this session while the aggregate count improved, and only
  a hand-diff of scratch output caught them. After it was widened it caught two more, in the
  same run that made the change.

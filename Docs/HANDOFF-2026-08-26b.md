# HANDOFF — 2026-08-26b

**Branch `main`. Suite 2,232 passing, NO reds and NO expected-fails. Everything pushed.**

## ▶️ START HERE

**§1 OF THE PREVIOUS HANDOFF IS SPENT — ALL SEVEN ROWS, IN SIX COMMITS.** There is no
measured work list waiting for you. Read `Docs/CHECKPOINT-2026-08-26b.md` §6 for the
territory, and §2 for what the seven rows actually turned out to be.

**THE INSTRUMENT IS THE SWEEP, AND IT PAID AGAIN TODAY** — eight defects, of which only
three were the rows written down. **THE OTHER FIVE CAME OUT OF THE FIXTURES WRITTEN TO GATE
THE FIRST THREE.**

## 🏁 EVERY GATE IN THIS REPO READS ZERO

| Gate | Now |
|---|---|
| **SVG bytes, in-repo** | **0 of 473** — 1 ruled divergent |
| SVG bytes, sibling | 0 of 356 |
| harvested corpus, four axes | **0 of 222** |
| `extractMeasures` | **0 of 268 files, 1,565 measures** |
| `tune.lines`, warnings, parse/render values, audio, MIDI, chord grids, timings, DOM, pixel | 0 |

**THAT IS THE NORMAL CONDITION HERE, NOT A FINISH LINE.** No gate can name the next defect
and none has for weeks.

## WHAT PAID, AND IT IS THE METHOD

**Eight defects, six commits, three new fixtures (49 tunes, all ratcheted).** In order of
what the ROW cost to find:

1. **THE LADDER FIRST, THE FIXTURE SECOND, AND THE FIXTURE IS WHERE THE MONEY IS.** Ten
   control shapes closed six rows of the inline-clef defect. The fifteen-tune FIXTURE then
   opened two more the moment they were a page — a beam direction and a cross-voice clef
   leak — because every ladder shape was one bar and one voice.
2. **A SECOND SWEEP OVER THE SAME TERRITORY.** The nine-shape lyric sweep written to CHECK
   the verse-lane fix found the melisma-divider defect, which could not have differed while
   every verse sat in the wrong lane.
3. **READING THE NAMED FUNCTION.** `startNewLine`'s
   `staves[n].clef ?? multilineVars.clef` is one line and it is a whole class of defect.

## THE RULES THIS SESSION EARNED

⚠️ **A GUARD NOBODY PASSES LOOKS LIKE A RULE THAT DOES NOT APPLY.** The cross-voice clef fix
was correct and moved NOTHING, because the guard read `voice?.clef === undefined` and an
undeclared voice's clef is `null`. That measurement is identical to the one a wrong
hypothesis produces. **Check the guard's TYPE before re-thinking the rule.**

⚠️ **A PROBE THAT NAMES THE SYMPTOM HAS NOT NAMED THE CAUSE.** The previous handoff's
`ZZCLEF` probe correctly reported "the whole measure is in the NEW clef" and pointed at
`clefAtMeasure[from]`. The change was reaching the layout TOO EARLY, and the mid-measure
switch it said to stop looking at was already built and already right.

⚠️ **PUT A NEW WRITE WHERE THE THING ENDS, NOT WHERE IT LOOKS LIKE IT ENDS.**
`closeUnterminatedMeasure` reads like end-of-voice and is every LINE's flush; the trailing
clef stamped there ate a change a later measure was waiting for. A byte-exact rung of the
same ladder is the only thing that caught it.

⚠️ **A SUM CANNOT SEE AN ORDER** — fifth time. The `%%begintext` ULP was two rows folded
into one remainder spent on the wrong side of `staffSeparation`.

⚠️ **A ROW WITHOUT A GATE CANNOT REPORT ITS OWN CLOSURE** — second session running. §1(d),
the 6.74px annotation row, was already exact when I got to it.

⚠️ **A GATE'S TIMEOUT HAS TO TRACK THE CORPUS.** The ranked table crossed vitest's 5s
default the run after the corpus grew. A gate that fails for growing punishes the one thing
this repo wants.

## THE HARNESS

| Script | Prints |
|---|---|
| `D=<dir> O=<dir> npx tsx scripts/zzdecs.ts` | our SVG for `<i>.abc` in a directory (needs `names.json`) |
| `ABCJS_VERSION=6.7.0 node ../abcMusicKit/Tools/abcjs-debug/dump-svg.js --file x.abc --output x.svg` | abcjs's, at the goldens' `{staffwidth: 670}` |
| `node scripts/harvest-abcjs-goldens.mjs --only <slug>` | regenerate ONE fixture's byte goldens |
| `node scripts/harvest-abcjs-extract-measures.mjs` | the `extractMeasures` oracle — **regenerate when a fixture grows** |
| `ABCTS_H=1` / `ABCTS_Y=1` / `ABCTS_PROBE=1` | our height terms, page walk, staff extent |

**A SWEEP RUNNER IS TWENTY LINES AND YOU SHOULD WRITE ONE FIRST.** It renders both engines
over a numbered directory and prints `exact` / `DIFFERS` per row. Two traps it must be built
around, both of which bit before: ⚠️ **`printf '%b'`, never `'%s'`** — the previous session's
third sweep reported 36 of 36 EXACT with every `\n` literal — and ⚠️ **exit if any abcjs SVG
is EMPTY**, because a missing reference reads as a clean column.

⚠️ **AND `git stash` IS THE PROOF IT RAN.** Ten rows came back `exact` on the first sweep of
the day; stashing the fix and re-running turned six `DIFFERS`. Nothing else distinguishes a
real fix from a harness that never rendered.

⚠️ **RUN EVERY COMMAND FROM `/Users/lrettberg/ICMLabs/Code/abcts`.** `cd /tmp` bit again
today — `npx tsx scripts/…` resolved against the wrong root.

⚠️ **`ABCJS_VERSION=6.7.0` IS NOT OPTIONAL — `dump-svg.js` DEFAULTS TO 6.6.3.**

**THE SCRATCHPAD abcjs IS AT `/tmp/gp/abcjs`** and it is cleaned periodically. It now
carries **`ZZMOVEY`** on `Renderer.prototype.moveY` (`src/write/renderer.js`) beside the
probes the previous handoff lists. `ZZMOVEY=1` against `ABCTS_Y=1` prints both engines' page
walk term for term, which located the `%%begintext` ULP in one run.

## WHAT IS LEFT

### 1. THE SWEEP'S UNTOUCHED TERRITORY

`%%MIDI` and audio-facing directives against the DRAWING, `V:` modifiers beyond `style=`,
`%%repeat`, staff-line counts, `%%map`/`%%percmap`, microtones beyond the quarter-tone pair,
and anything at the boundary between two features. **36 shapes is an hour, and today's yield
was one defect per six.**

### 2. THE `ponytail:` LEDGER — 100 ENTRIES

`grep -rn "ponytail:" src tests scripts`. Read each against the OUTPUT, not the code beside
it — that is how both of the previous session's two paid.

### 3. TWO RULED DIVERGENCES, BOTH abcjs PRINTING AN INTERNAL FAILURE INTO THE PAGE

`Docs/ABCJS-DIFFERENCES.md` carries both. **A decision, not a defect.**

### 4. THE `abselem` DECISION IS THE OWNER'S

Unchanged since 2026-08-21. `scripts/zzrv.ts` reads 10,570 rows differing ONLY by an absent
`abselem`/`staffGroup`, and publishing them means RETAINING THE `Layout`, which measurably
killed this suite's workers once. **Do not start it without a decision.**

## THE METHOD, UNCHANGED

Read the named abcjs function FIRST, then instrument to confirm it — neither half works
alone. `npx tsc --noEmit` before every commit. Diff every regenerated golden against `HEAD`
asserting only NEW KEYS appeared. Re-ratchet every gate's PASSING list in the same commit.
Commit and push after every landing. Never `--force`. Omit `Co-Authored-By` trailers here.

⚠️ **ANOTHER AGENT MAY BE IN THIS WORKTREE.** Treat any single commit's file list as
approximate, and never `git checkout` a whole file you did not just write.

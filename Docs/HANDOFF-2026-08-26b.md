# HANDOFF — 2026-08-26b

**Branch `main`. Suite 2,398 passing, NO reds and NO expected-fails. Everything pushed.**

## ▶️ START HERE

**§1 OF THE PREVIOUS HANDOFF IS SPENT, THREE SWEEPS OF NEW TERRITORY WENT WITH IT, AND THE
WARNINGS GATE IS CLOSED. TWENTY-SEVEN DEFECTS, FOURTEEN COMMITS.**

**ALL EIGHT OF THE ROWS THIS HANDOFF ONCE NAMED ARE CLOSED, AND SO IS THE OWNER'S MODE
RULING. THIRTY-SIX DEFECTS, TWENTY-TWO COMMITS.** `Docs/CHECKPOINT-2026-08-26b.md` §8 has
how each closed and §9 the ruling.

⚠️ **SO NO GATE CAN NAME THE NEXT DEFECT AGAIN — which is the normal condition here.** What
paid, in order: writing a FIXTURE (nineteen of the first twenty-seven), then reading a gate
the change was not aimed at (`extractMeasures` and the warnings gate each named defects the
byte gate could not see, on fixtures written FOR the byte gate), then the sweep itself.

⚠️ **THE SWEEP'S YIELD IS FALLING AND THE FIXTURE'S IS NOT.** Three sweeps ago it was one
defect per four shapes; the first sweep today was one per twelve and the second one per
thirty-six. **NINE OF THE SEVENTEEN CAME FROM TURNING A CLOSED ROW INTO A FIXTURE**, and the
last five came from a gate the sweeps were not aimed at — `tests/warnings.test.ts`, which
both byte-exact sweep fixtures moved the moment they were pages.

**SO THE NEXT UNIT IS PROBABLY NOT ANOTHER 36 SHAPES OF THE SAME KIND.** §7.1 names what is
untouched; §6.3 is the pattern that is still paying.

## 🏁 EVERY GATE IN THIS REPO READS ZERO

| Gate | Now |
|---|---|
| **SVG bytes, in-repo** | **0 of 635** — 1 ruled divergent |
| SVG bytes, sibling | 0 of 356 |
| harvested corpus, four axes | **0 of 226** |
| `extractMeasures` | **0 of 270 files, 3,199 of 3,199 rows** |
| **warnings** | **0 of 759** — CLOSED, 49 rows ratcheted from 14 |
| `tune.lines`, parse/render values, audio, MIDI, chord grids, timings, DOM, pixel | 0 |

✅ **EVERY GATE IS AT ZERO AGAIN, INCLUDING THE EIGHT ROWS THIS HANDOFF ONCE NAMED.** All
eight closed — §8 records how, and two of the three families had a MEASURED WRONG ANSWER
recorded beside them first, which is what made the second attempt cheap.

⚠️ **SO NO GATE CAN NAME THE NEXT DEFECT, WHICH IS THE NORMAL CONDITION HERE.**

**THAT IS THE NORMAL CONDITION HERE, NOT A FINISH LINE.** No gate can name the next defect
and none has for weeks.

## WHAT PAID, AND IT IS THE METHOD

**Eight defects, six commits, three new fixtures (49 tunes, all ratcheted).** In order of
what the ROW cost to find:

1. **THE LADDER FIRST, THE FIXTURE SECOND, AND THE FIXTURE IS WHERE THE MONEY IS.** Ten
   control shapes closed six rows of the inline-clef defect. The fifteen-tune FIXTURE then
   opened two more the moment they were a page — a beam direction and a cross-voice clef
   leak — because every ladder shape was one bar and one voice.
2. **A SECOND SWEEP OVER THE SAME TERRITORY.** The nine-shape lyric sweep written to CHECK
   the verse-lane fix found the melisma-divider defect, which could not have differed while
   every verse sat in the wrong lane.
3. **READING THE NAMED FUNCTION.** `startNewLine`'s
   `staves[n].clef ?? multilineVars.clef` is one line and it is a whole class of defect.

## THE RULES THIS SESSION EARNED

⚠️ **A GATE'S RATCHET IS ONLY AS BROAD AS WHAT SOMEBODY ADDED TO IT.** The warnings ratchet
held 14 rows while 39 agreed — twenty-five could have regressed in silence. The gate was
BUILT to say so and its own failure message says so ("these agree — add them to PASSING");
it just had to be read. Same shape as the byte gate holding 4% of what was green.

⚠️ **A RULE THAT IS RIGHT ON THE FIXTURE THAT NAMED IT CAN BE WRONG ON THE ONE BESIDE IT.**
The tune-line-number rule was derived from two fixtures and took the warnings gate from 2 to
9: it counted header LINES where abcjs counts header DIRECTIVE lines, and a five-line `%`
comment preamble is not an offset at all. Derive from two, then check the third.

⚠️ **A DIAGNOSTIC CODE NO FORMATTER MAPS IS A SILENT ONE.** `broken-rhythm-without-note` was
raised, carried and dropped — the parser looked like it warned and the host saw nothing.

⚠️ **A DECLARED 0 IS OFTEN NOT DECLARED AT ALL** — third instance on this branch
(`if (opt.bottom)` for a stem, for a mezzosoprano clef, and now
`if (multilineVars.currentVoice.stafflines)`). The first two were each written up as
belonging to their own site. **It is the rule, not the site.**


⚠️ **A GUARD NOBODY PASSES LOOKS LIKE A RULE THAT DOES NOT APPLY.** The cross-voice clef fix
was correct and moved NOTHING, because the guard read `voice?.clef === undefined` and an
undeclared voice's clef is `null`. That measurement is identical to the one a wrong
hypothesis produces. **Check the guard's TYPE before re-thinking the rule.**

⚠️ **A PROBE THAT NAMES THE SYMPTOM HAS NOT NAMED THE CAUSE.** The previous handoff's
`ZZCLEF` probe correctly reported "the whole measure is in the NEW clef" and pointed at
`clefAtMeasure[from]`. The change was reaching the layout TOO EARLY, and the mid-measure
switch it said to stop looking at was already built and already right.

⚠️ **PUT A NEW WRITE WHERE THE THING ENDS, NOT WHERE IT LOOKS LIKE IT ENDS.**
`closeUnterminatedMeasure` reads like end-of-voice and is every LINE's flush; the trailing
clef stamped there ate a change a later measure was waiting for. A byte-exact rung of the
same ladder is the only thing that caught it.

⚠️ **A SUM CANNOT SEE AN ORDER** — fifth time. The `%%begintext` ULP was two rows folded
into one remainder spent on the wrong side of `staffSeparation`.

⚠️ **A ROW WITHOUT A GATE CANNOT REPORT ITS OWN CLOSURE** — second session running. §1(d),
the 6.74px annotation row, was already exact when I got to it.

⚠️ **A GATE'S TIMEOUT HAS TO TRACK THE CORPUS.** The ranked table crossed vitest's 5s
default the run after the corpus grew. A gate that fails for growing punishes the one thing
this repo wants.

## THE HARNESS

| Script | Prints |
|---|---|
| `D=<dir> O=<dir> npx tsx scripts/zzdecs.ts` | our SVG for `<i>.abc` in a directory (needs `names.json`) |
| `ABCJS_VERSION=6.7.0 node ../abcMusicKit/Tools/abcjs-debug/dump-svg.js --file x.abc --output x.svg` | abcjs's, at the goldens' `{staffwidth: 670}` |
| `node scripts/harvest-abcjs-goldens.mjs --only <slug>` | regenerate ONE fixture's byte goldens |
| `node scripts/harvest-abcjs-extract-measures.mjs` | the `extractMeasures` oracle — **regenerate when a fixture grows** |
| `ABCTS_H=1` / `ABCTS_Y=1` / `ABCTS_PROBE=1` | our height terms, page walk, staff extent |

**THE SWEEP RUNNER IS A SCRIPT NOW** — `npx tsx scripts/zzsweep.ts <dir>`, from the repo
root. Drop `<anything>.abc` files in a directory and it renders both engines and prints
`exact` / `DIFFERS` with the first differing byte. It carries the traps that bit before:
a missing or EMPTY abcjs SVG is a hard exit rather than a row, `ABCJS_VERSION=6.7.0` is
passed rather than defaulted, and `{staffwidth: 670}` is explicit.

⚠️ **STILL WRITE THE SHAPES WITH `printf '%b'`, NOT `'%s'`** — the runner cannot check that
for you, and a sweep whose every `\n` stayed literal once reported 36 of 36 EXACT.

⚠️ **AND PUT A CONTROL THAT MUST DIFFER IN THE SAME RUN AS THE ONES THAT MUST NOT.** `C32|`
is a note longer than a breve — a ruled divergence — so it reads `DIFFERS` forever, on
purpose. That is cheaper than the `git stash` proof and it cannot go wrong:
⚠️ **`git stash` ON A CLEAN TREE STASHES NOTHING, so the `git stash pop` after it pops
SOMEBODY ELSE'S entry.** It dropped a WIP from `geometry/lyric-ink-anchor` — a branch nobody
is on — into `layout.ts` as a conflicted merge. Nothing was lost (git keeps the entry when a
pop conflicts, and it is still `stash@{0}`), but check `git stash list` first.

⚠️ **RUN EVERY COMMAND FROM `/Users/lrettberg/ICMLabs/Code/abcts`.** `cd /tmp` bit again
today — `npx tsx scripts/…` resolved against the wrong root.

⚠️ **`ABCJS_VERSION=6.7.0` IS NOT OPTIONAL — `dump-svg.js` DEFAULTS TO 6.6.3.**

**THE SCRATCHPAD abcjs IS AT `/tmp/gp/abcjs`** and it is cleaned periodically. It now
carries **`ZZMOVEY`** on `Renderer.prototype.moveY` (`src/write/renderer.js`) beside the
probes the previous handoff lists. `ZZMOVEY=1` against `ABCTS_Y=1` prints both engines' page
walk term for term, which located the `%%begintext` ULP in one run.

## WHAT IS LEFT

### 0. THERE IS NO MEASURED WORK LIST — BUILD ONE

Every named row closed. **The two rules that produced the most this session, in order:**

⚠️ **A ROW NAMED IN A FIXTURE CAN REPORT ITS OWN CLOSURE; ONE LEFT OUT OF THE CORPUS
CANNOT.** The byte gate spent half the session at 8 of 622 ON PURPOSE, and every one of those
eight closed — where the two shapes DROPPED from a fixture had to be remembered by prose.

⚠️ **AND READ THE GATE THE CHANGE WAS NOT AIMED AT.** `extractMeasures` is all-or-nothing and
named two defects in opposite directions on a fixture written for the byte gate; the warnings
gate named four more the same way. After every landing, run the whole suite and read the
ranked tables — not just the one you were working on.

### 1. THE SWEEP'S UNTOUCHED TERRITORY

`%%MIDI` and audio-facing directives against the DRAWING, `V:` modifiers beyond `style=`,
`%%repeat`, staff-line counts, `%%map`/`%%percmap`, microtones beyond the quarter-tone pair,
and anything at the boundary between two features. **36 shapes is an hour, and today's yield
was one defect per six.**

### 2. THE `ponytail:` LEDGER — 100 ENTRIES

`grep -rn "ponytail:" src tests scripts`. Read each against the OUTPUT, not the code beside
it — that is how both of the previous session's two paid.

### 3. TWO RULED DIVERGENCES, BOTH abcjs PRINTING AN INTERNAL FAILURE INTO THE PAGE

`Docs/ABCJS-DIFFERENCES.md` carries both. **A decision, not a defect.**

### 4. THE `abselem` DECISION IS THE OWNER'S

Unchanged since 2026-08-21. `scripts/zzrv.ts` reads 10,570 rows differing ONLY by an absent
`abselem`/`staffGroup`, and publishing them means RETAINING THE `Layout`, which measurably
killed this suite's workers once. **Do not start it without a decision.**

## THE METHOD, UNCHANGED

Read the named abcjs function FIRST, then instrument to confirm it — neither half works
alone. `npx tsc --noEmit` before every commit. Diff every regenerated golden against `HEAD`
asserting only NEW KEYS appeared. Re-ratchet every gate's PASSING list in the same commit.
Commit and push after every landing. Never `--force`. Omit `Co-Authored-By` trailers here.

⚠️ **ANOTHER AGENT MAY BE IN THIS WORKTREE.** Treat any single commit's file list as
approximate, and never `git checkout` a whole file you did not just write.

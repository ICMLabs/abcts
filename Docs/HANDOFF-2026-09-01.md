---
title: Handoff 2026-09-01 — browser parity, 231 → 8, and the instruments that got it there
status: live gate 8 of 685 in WebKit; Node suite 2,453 and svg-bytes 685/685 green; everything pushed
supersedes: HANDOFF-2026-08-29.md for the state
---

# Handoff 2026-09-01

## ▶️ START HERE

**A NEW AXIS OPENED AND IT IS THE ONE THE PRODUCT IS JUDGED ON.** Every gate in this repo
renders `src/` under Node against goldens harvested from abcjs **under jsdom**, and
`dump-svg.js` PATCHES `getBBox` with calibrated tables because jsdom has no layout engine.
`golden-widths.ts` reproduces those tables and its own header says reproducing them "makes
our real output diverge from a browser's, which is the point".

So the 691 goldens assert **"abcts matches abcjs GIVEN SYNTHETIC TEXT METRICS"**, and
nothing had ever asked what abcjs draws when it measures for real.

    scripts/zzlive.mjs     abcts and abcjs 6.7.0 in ONE WebKit page, diffed live
    scripts/zzcontrol.mjs  ladders: `size`, `dirs`, `abc` — one variable per rung
    scripts/zzpair.mjs     one fixture, both engines, element by element

    live gate today: 8 of 685    (231 when the axis opened)

⚠️ **AND THERE IS NO STORED GOLDEN THAT COULD REPLACE IT.** Measured (`d4b7022`): abcjs
does NOT render byte-identically in WebKit and Blink — 230 of 691 differ between them.
Single glyph advances agree exactly; multi-character widths and bbox heights do not. **So
"byte parity in a browser" has no single target**, and the only coherent oracle is abcjs
running in the SAME browser, live. Re-harvesting the corpus from a browser is ruled out.

WebKit is the default for a reason beyond availability: Studio's editor is CodeMirror 6 in
a **WKWebView**, so for this stack WebKit is the deployment engine rather than a proxy.

## HOW TO RUN IT

`playwright-core` is **NOT** in this repo's devDeps — six is a deliberate number — and the
browser is not vendored:

    mkdir -p /tmp/gp/pw && cd /tmp/gp/pw && npm init -y && npm i playwright-core@1.61
    npx playwright-core install webkit          # only if the cache was cleaned
    cd <repo> && npm run build                  # the gates load dist/, not src/
    PW=/tmp/gp/pw/node_modules/playwright-core/index.js node scripts/zzlive.mjs

⚠️ **PIN 1.61.** Its `browsers.json` names webkit-2311, the build cached on this Mac. 1.55
wants 2203 and 1.62 wants 2336 — either starts a 90MB download.

⚠️ **`/tmp` IS CLEANED AND IT COST AN HOUR.** The driver, the browser AND every ladder
script vanished mid-session; the scripts had produced nine of the day's fixes and lived only
in a scratchpad. They are in `scripts/` now. **A harness that finds things belongs in the
repo.**

## THE ONE RULE THAT PRODUCED ALMOST EVERY FIX

**abcjs MEASURES THE THING IT IS ABOUT TO DRAW; we measured a STAND-IN.** Ten landings, one
shape: a probe string where the real one was wanted, a generic font, the wrong family, the
flat form of a jazz chord, a font's height where a string's was wanted, one line's height
where the whole block's was wanted.

**AND `h + (n-1) * size * 1.2` IS THE STUB'S `getBBox`, NOT A RULE** — four times: the lyric
lane, the jazz chord's nested tspans, the `%%begintext` block, and `dump-svg.js` itself.
Wherever a multi-line height is COMPUTED from one line, abcjs measured the whole string once
(`getTextSize.calc` over the join) and a browser disagrees in the last bits.

**AND THE OWNER'S STEER — THE ORDER OF CALCULATIONS — PAID TWICE.** abcjs divides by
`spacing.STEP` FIRST and multiplies by a lane count SECOND; building a length and dividing
once at the end is `(h*n)/STEP` against `(h/STEP)*n`, the same number for one lane and not
the same float for more. Landed above (`805a9a1`) and below (`fb3fb17`).

## ⚠️ THE METHOD, AND IT MATTERS MORE THAN ANY FIX

**A LADDER OF CONTROLS IS THE PROOF; A FIXTURE WITH MORE THAN ONE OPEN CAUSE CANNOT RULE
ANYTHING OUT.** Two conclusions were committed to this repo and BOTH were wrong, both
reasoned from `visual-options-01-fonts` — which sets eighteen font directives at once:

- *"the face is not it, measured twice, reverted"* — it WAS it.
- *"the error flips sign with size, so a family cannot explain it"* — on a control every
  rung is positive and there is no sign change at all.

Nine control tunes varying ONE thing identified the two fonts by their INCREMENTS (ours
Helvetica, abcjs Arial) and closed the chord lane to 0.000000 on every rung.

**AND THREE INSTRUMENTS EACH CAUGHT WHAT THE OTHERS COULD NOT:**

| instrument | caught |
|---|---|
| ladder of controls | two written-down conclusions that were false |
| the Node corpus | an over-broad lyric fallback whose own control was byte-perfect |
| the AGGREGATE live gate | a fix that closed its target, kept Node green, and put two OTHER fixtures 23.27px out |

A per-fixture check cannot see a regression in a fixture it does not render.

## WHAT IS LEFT — 8, and most of it is DECISIONS

**1 · THE TEMPO'S SUB-PIXEL X — 3 fixtures, mechanism MEASURED, fix not written.**
A FRACTIONAL x measures one sub-pixel wider. WebKit, `"left"` bold Times 20px:

    x = 0, 100, 166      27.765625
    x = 0.7, 166.7       27.781250      exactly 1/64

abcjs measures the element it JUST DREW (`getTextSize.calc`'s fourth argument), at the
SOLVED x. **Passing an x at the existing site is measured to change NOTHING** — that site is
the prefix builder and its cursor is provisional. **The fix is to advance the tempo at DRAW
time from the solved x — an ORDERING change.** Reproduce with
`G4|[Q:"left" 1/4=170 "right"]A4|`; a bare `Q:` is exact.

**2 · CSSOM SERIALISATION — 1 fixture, THE OWNER'S CALL.** abcjs sets `el.style.transform`
through the DOM and the BROWSER serialises it; we emit the attribute as text. And abcjs
disagrees with ITSELF across browsers — Chrome adds `-webkit-transform-origin-x/y` where
WebKit does not. Matching one browser's serialisation breaks the other by construction. The
mechanism-level answer is to set styles through the DOM as abcjs does, which is
architectural: **we emit SVG text, abcjs builds nodes.**

**3 · A HELD SYLLABLE TAKES THE DEFAULT FONT — measured, fixed, REVERTED (`85356b9`).**
`%%vocalfont Times-Roman 20` over `C4 D4` / `w:laa_ la` draws the held `&nbsp;` at 17 BOLD
where abcjs draws 27 normal. Inheriting the directive closes the control EXACTLY and takes
four gates red, one named *"does NOT realize %%vocalfont — abcjs parses it and never draws
it"*. Scope to the held/empty syllable ALONE and prove against THOSE gates, not the control.

**4 · TWO LARGE FONT FIXTURES** — `misc-06-title-1bold` (10.3px, `$1`/`$0` rich text) and
`options-01-fonts` (3.1px, down from 27.4). Several causes each; use a control.

**5 · TWO SELECTION/TABLATURE FIXTURES**, page heights already byte-exact, differing deep in
the file. Unexamined.

## TRAPS THIS SESSION ADDED

- ⚠️ **`display:none` ZEROES `getBBox`**, and abcjs measures at DRAW time for a boxed font —
  so hiding the harness's slots that way INVENTED two defects and MASKED others.
  `visibility:hidden` still lays out. **A harness that lies does not lie in one direction.**
- ⚠️ **`npx tsc --noEmit | head -3; echo $?` REPORTS `head`'S STATUS.** Two changes that did
  not compile passed that check and were caught by WebKit as runtime `Can't find variable` /
  `Cannot access before initialization`. Write `npx tsc --noEmit && echo OK`.
- ⚠️ **A NARROW PROBE CAN MISS THE EFFECT IT WAS BUILT TO FIND.** The above-lane order fix
  looked inert on the two fixtures the lane was known to be wrong on — both have ONE lane,
  where the two arithmetics agree by construction — and closed a third neither named.
- ⚠️ **AN EMPTY LINE MEASURES ZERO AND STILL TAKES A ROW** (`svg.js:311-312`). A
  `%%begintext` holding one blank line reserved nothing until the live path was guarded on
  every line carrying ink.

## AND ONE THING THAT IS NOT abcts

The bundle is browser-safe now (`5b36bb4`): `const PROBE = process.env.ABCTS_PROBE` at
`layout.ts`'s MODULE SCOPE meant the iife threw while evaluating, so `var ABCTS` hoisted and
the page saw `undefined`. All 25 probe sites read `ENV` from `abcjs-constants.ts`. **Every
build is browser-safe now, not just the iife** — a host bundling `abcts/compat` with a tool
that does not shim `process` was equally dead. `dist/abcts-browser.global.js` is the
`<script>` build; the global is `ABCTS`, deliberately NOT `ABCJS`, so both can load in one
page.

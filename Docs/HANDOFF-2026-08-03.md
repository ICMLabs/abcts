# Handoff — 2026-08-03

The session prompt for the next agent, and the shortest possible statement of where to
start. Full state is in `CHECKPOINT-2026-08-03.md`.

---

## THE PROMPT

```
Continue the vertical parity arc on `geometry/vertical` in the abcts repo
(~/ICMLabs/Code/abcts, git@github.com:ICMLabs/abcts.git).

FIRST: read Docs/CHECKPOINT-2026-08-03.md, then Docs/VERTICAL-ARC.md, then
ARCHITECTURE.md, then CLAUDE.md.

TWO REPOS, BOTH WITH A BRANCH CALLED `main`. `Code/abcts` is ours.
`Code/abcMusicKit` is a read-only sibling holding the vendored abcjs source, the
41-fixture corpus and its goldens — never commit there, and if you instrument it,
restore it and verify `git -C ../abcMusicKit status --short` is empty before you
finish.

THE GOAL IS 100% PARITY WITH ABCJS, bugs and poor rendering choices included. A
test that asserts abcjs is wrong is a test to correct, and two have been.

THE RULE THAT COST THE MOST: MEASURE THE OUTPUT. The source will lie to you —
three times on this branch a careful chain of source reads predicted something
abcjs's own SVG denies, and a grep of the golden settled each in seconds. Read the
source to find the MECHANISM; read the output to find the NUMBER. And an extent
difference names a STAFF, not a mechanism: probe the mechanism separately, always.

THE IDEA THAT EXPLAINS THE ARC: abcjs does not measure what it draws — it DECLARES
a box and reserves that. Notehead, accidental, clef, key and time signature,
tempo, tuplet, dynamic, decoration and tie all reserve declared figures; a BEAM
reserves nothing. Two questions go with it: WHOSE box is it (a volta belongs to
one voice; a chord and an annotation share a lane), and WHEN is it applied (ink,
then lanes, then post-lane — the same box one phase early is a different number).

THERE ARE TWO CORPORA. The 41 fixtures in ../abcMusicKit are the original gate.
`tests/corpus-abcjs/` is 174 tunes harvested from abcjs's OWN test suite, with
goldens generated by running abcjs (`npm run harvest`, `npm run harvest:goldens`).
It found a whole feature that was parsed and never painted — `&` overlay voices —
because nothing in the 41 uses `&`. A GATE IS ONLY AS BROAD AS ITS INPUTS.

INSTRUMENT TO ANSWER A QUESTION. Env-guarded console.log in the vendored abcjs
source, run its own Tools/abcjs-debug/dump-svg.js harness, read the ground truth,
restore. Compare abcjs's staff extent at THREE points and use POST — the
checkpoint says which and why. AND INSTRUMENT OUR SIDE: `ABCTS_PROBE=1` prints
each staff's extent in abcjs pitch, who set it, the lane flags, the top-text block
and every beam.

NEVER RAISE A CEILING TO MAKE A CHANGE PASS. The branch is red on ONE item —
`ragtime-nightingale`'s oy at 1.96 against 0.59 — and that number got worse while
its geometry got better, which the checkpoint explains. Anything else failing is
yours.

START WITH ONE OF THESE, in this order:
  1. `little swallow` — DIAGNOSED. Our lyric reserve is anchored on the drawn
     baseline where abcjs anchors on the INK and subtracts the lane. Port the
     structure, then re-derive the constant.
  2. ragtime's 942 beamed stems against our 940 — two beamed notes differ, and it
     is NOT the beam geometry, which is verified correct.
  3. The harvested corpus's directive tail, led by %%barnumbers (8 fixtures) and
     the font directives (11 between them).

A PASSING GATE IS NOT PARITY. Parity is dy/dx/oy/ox at ZERO on every fixture:
currently 20/29 on all four at once, and 172/174 content-correct on the harvested
corpus.
```

---

## WHERE TO START, WITH THE EVIDENCE ALREADY GATHERED

### 1. `little swallow` — the best-diagnosed item left

abcjs: `staff.bottom = ink − (lyricHeightBelow + 1)`, and the lyric baseline follows the
ink too (`lyric pitch = ink − spacing.vocal / STEP`). Its five ink bottoms are −2.5 /
−3.4747 / −3.0 / −3.4727 / −3.0 and its staff bottoms follow exactly.

**Our ink already agrees** — probed, staves 2 and 4 both bottom out at pitch −3.0. Our
reserve is anchored on the DRAWN BASELINE instead, so two staves that share an ink bottom
get one answer from abcjs and two from us.

The existing constant (`lyricVoiceStep + spacePerStep − lyricInkGap`) was verified against
abcjs on three shapes. **Port the structure, then re-derive it** — do not drop a new
constant into the old form.

### 2. ragtime — 942 beamed stems against 940

Two beamed notes differ. This also throws the beam lists out of alignment from index 195,
so every beam past it is compared against its neighbour. The beam GEOMETRY is verified
correct (292 lines, first 194 exact; `bary` within 0.07 pitch over 940 aligned stems), so
do not re-read `calcYPos` — find the two notes.

### 3. The directive tail

`%%barnumbers` (8 fixtures), the font directives (11), `%%musicspace` (4), `%%text` (4),
`%%sep` (2), `%%setbarnb` (2). Each is small and each moves several fixtures.

---

## TWO THINGS THAT NEED A DECISION, NOT A COMMIT

**The gate hides failures.** All eight per-fixture assertions live in one `it`, so the
first to fail ends it — that hid a stale ceiling three separate times in one session.
Collecting all four axes before asserting is the honest fix, and it will show more red
before it shows less.

**A grace note before a `y` spacer is dropped.** `Rest` has no `graceNotes` field, and the
model says so deliberately: *"a rest does carry decorations — but not ties, slurs, grace
notes or lyrics, none of which apply to silence"*. abcjs disagrees for a SPACER, which is a
layout device rather than silence. Overturning a stated model decision needs a decision.

---

## WHAT NOT TO CHASE

Four gate numbers are artefacts of how the gate PAIRS noteheads, not divergences:
`ragtime-nightingale` dy 58.1 (two swapped heads — the real spread is 5.38),
`vree-grace-notes` dy 11.6 and dx 32.5 (grace emission order; sorted, 0.02 and a uniform
1.99 grace glyph), and `little swallow` dx (the goldens' ASCII-only width table measured
its 73 Chinese characters at 8px each).

`frere-jacques` is CLOSED vertically — dy 0.03, oy −0.02. Its dx and ox are horizontal.

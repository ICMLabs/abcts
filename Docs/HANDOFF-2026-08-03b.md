# Handoff — 2026-08-03b

The session prompt for the next agent, and the shortest statement of where to start.
Full state is in `CHECKPOINT-2026-08-03b.md`.

---

## THE PROMPT

```
Continue the vertical parity arc on `geometry/vertical` in the abcts repo
(~/ICMLabs/Code/abcts, git@github.com:ICMLabs/abcts.git).

FIRST: read Docs/CHECKPOINT-2026-08-03b.md, then Docs/VERTICAL-ARC.md, then
ARCHITECTURE.md, then CLAUDE.md.

TWO REPOS, BOTH WITH A BRANCH CALLED `main`. `Code/abcts` is ours.
`Code/abcMusicKit` is a read-only sibling holding the vendored abcjs source, the
41-fixture corpus and its goldens — never commit there, and if you instrument it,
restore it and verify `git -C ../abcMusicKit status --short` is empty before you
finish.

THE GOAL IS 100% PARITY WITH ABCJS, bugs and poor rendering choices included. A
test that asserts abcjs is wrong is a test to correct, and FOUR have been.

THE RULE THAT COST THE MOST: MEASURE THE OUTPUT. And its sharper form, which cost
a whole session: A COUNT YOU CANNOT RE-DERIVE FROM THE OUTPUT IS NOT A
MEASUREMENT. "942 beamed stems against our 940" was carried forward as the top
lead and named the wrong subsystem entirely; diffing the two SVGs' stems settled
it in one command — 1017 against 1016, and the one missing stem was the TEMPO
mark's note.

WATCH WHAT THE GATE CANNOT SEE. abcjs classes only noteheads, ledgers, stems and
the top staff line. Beams, tempo notes, ties, brackets and bar numbers carry NO
class, so a class-based gate is blind to them — which is how a missing tempo note
sat under a green gate.

THE ALGORITHM IS IN ABCJS — read it, don't infer it from a diff. Four of this
session's nine fixes were ports of a named function (`merge`'s staff assignment,
`getClef`'s prefix match, `setCurrentVoice`'s line scan, the backslash
preprocessing) and none could have been guessed from the output. But finish with
a probe: `parseVoice` ends `return setCurrentVoice(id)` unconditionally, which
reads as "every [V:x] repositions the line", and it does not — a [V:x] naming
the voice already current is a no-op, and only instrumenting shows it.

THE IDEA THAT EXPLAINS THE ARC: abcjs does not measure what it draws — it DECLARES
a box and reserves that. Two questions go with it: WHOSE box is it, and WHEN is it
applied (ink, then lanes, then post-lane). And a third, new this session: IS IT
MEASURED TWICE? The lyric bug was one quantity computed in two places that had
drifted apart in their inputs — the formula was never wrong.

SPLIT A NUMBER BEFORE CHASING IT. A harvested fixture was 45.19px out and read as
one bug; a controlled pair — the same tune with and without its %%begintext block
— split it into 11.43 of clef and 33.76 of free text, two unrelated defects.

NEVER RAISE A CEILING TO MAKE A CHANGE PASS. The branch is red on ONE item —
`ragtime-nightingale`'s oy, now 1.49 against 0.59. Anything else failing is yours.

START HERE, EVERY TIME:

  npx vitest run tests/corpus-abcjs-ranked.test.ts && cat /tmp/abcts-corpus-ranked.txt

Six of this session's nine fixes came off that table and none off the ratchet's
counts, which say a number moved but not what to fix. Read the SHAPE: dy 0.0
beside a large dx is horizontal and not this arc's; a large |oy| with dy near
zero is a rigid vertical shift, one term and usually one cause.

Then, in this order:
  1. The two VERTICAL entries at the top — `visual-tablature-17-stretchlast`
     (dy 300.3) and `visual-options-01-fonts` (oy -153.5).
  2. ragtime's remaining five staves are HORIZONTAL in origin — the checkpoint
     shows the beam x spans that prove it. Do not chase them on this axis.
  3. abcjs BEAMS THROUGH a short unspaced rest, ours breaks. Fully diagnosed,
     including why the half-measure cannot work — but read the scope warning:
     four beams in 599 and no gate in this repo can see the fix.

A PASSING GATE IS NOT PARITY. Parity is dy/dx/oy/ox at ZERO on every fixture:
currently 21/29 on all four at once, 172/174 content-correct on the harvested
corpus and 86/97/106/130 of it within 0.05/1/5/25px — 86 of the 174 are still
off some axis.
```

---

## WHERE TO START, WITH THE EVIDENCE ALREADY GATHERED

### 1. The ranked table

`tests/corpus-abcjs-ranked.test.ts` — committed this session, on the `staff-spacing`
pattern. What stands at the top and what its shape says is tabulated in the checkpoint.
The short version: the two VERTICAL items are `visual-tablature-17-stretchlast` (`dy`
300.3) and `visual-options-01-fonts` (`oy` −153.5, 18 font directives plus
`%%header`/`%%footer`). Four `visual-transpose-*` share ONE horizontal cause and two more
sit at a rigid `ox` of 75.84 — cheap, but not this axis.

### 2. ragtime — do NOT chase its last five staves vertically

Staves 8, 16, 24, 34 and 42 are all curve reserves reading a beam-interpolated stem end,
and both engines compute that the same way. What differs is the beam's x SPAN — 104.99
against abcjs's 96.72 on staff 8's beam, with identical `startY`/`endY` — so the interior
sample lands elsewhere. The numbers are in the checkpoint. They close when the horizontal
does.

### 3. Beaming across a rest

Fully diagnosed: `tune-builder.js:172-203`, the rule, the bar, and why transparent-but-not-
a-member cannot work (the rest's own pitch is 11 on a multi-voice stems-up staff and it is
that 11 which lifts `beam.max`). Also weigh it: four beams in 599 across the 41 fixtures,
and no gate here can see the fix — the pixel gate measures noteheads, which do not move.

---

## STILL NEEDING A DECISION, NOT A COMMIT

**The gate hides failures.** All eight per-fixture assertions live in one `it`, so the first
to fail ends it. Collecting all four axes before asserting is the honest fix, and it will
show more red before it shows less.

**Mid-tune free text is dropped.** A `%%begintext` between two music lines is not drawn:
it must not be marked as trailing, since abcjs's justification rule is per LINE and doing
so justifies the wrong one, and placing it properly needs free text to be a LINE rather
than a property of the tune. `S2-fields`'s BeginText tune is 59px short because of it.

**A grace note before a `y` spacer is dropped.** `Rest` has no `graceNotes` field and the
model says so deliberately. abcjs disagrees for a SPACER, which is a layout device rather
than silence. Overturning a stated model decision needs a decision.

---

## WHAT NOT TO CHASE

`ragtime-nightingale` dy 58.1 is two mis-paired heads at i=1498/1500 (+29.12 / −29.02) over
a real spread of ~5px, and they move the mean by nothing. `vree-grace-notes` dy 11.6 and dx
32.5 are grace emission order. `little swallow` dx 23.97 and ox −5.74 are the goldens'
ASCII-only width table measuring its 73 Chinese characters at 8px each — its vertical axes
are now 0.32 and 0.16 and there is nothing left there.

`abcjs-visual-misc-01-barnumbers-1` is a MULTI-MEASURE REST (`Z24`), not a bar-number
fixture, and its 0.00 spreads are an artefact of having one paired notehead.

`ragtime-nightingale`'s staves 8, 16, 24, 34 and 42 — horizontal, see above.

# HANDOFF — 2026-08-11

## The session prompt — paste this

```
We are continuing abcts development in the abcts repo (Code/abcts).

THE GOAL, in Lance's words: abcts exists to build an abcjs-modern whose output — the
SVG FILE and the AUDIO — is 100% BYTE-EQUAL to abcjs 6.7.0. A tolerance is not a
compromise to be balanced against effort; it is A DEFECT THAT HAS NOT BEEN WRITTEN
DOWN YET. Anything you decline to reproduce goes in Docs/ABCJS-DIFFERENCES.md with its
evidence, and its slug goes in svg-bytes.test.ts's DIVERGENT list. That list is EMPTY
and stays empty. Work until byte-equal, without stopping for approval or status.
Checkpoint, hand off and compress as you go so no context is lost.

AND: abcjs BEHAVIOUR is what we are shooting for — we can optimise later. Take abcjs's
arithmetic, its order and its data model even where a better one exists. AS YOU DECIDE
TO USE AN abcjs NON-OPTIMAL SOLUTION, DOCUMENT IT FOR FUTURE REFACTOR: one
`abcjs-debt:` line at the site and an entry in Docs/ABCJS-DEBT.md naming the gate that
goes red if someone "fixes" it. That file is a THIRD CLASS, distinct from
ABCJS-DIFFERENCES.md (what we refuse to reproduce) and from `ponytail:` (our own
shortcuts).

Read Docs/CHECKPOINT-2026-08-11.md first — the state, the landings, §3 (the pitch
extent, which is HALF DONE and carries a NEGATIVE RESULT: fixing it site by site makes
it worse), §4 WHAT IS LEFT with the correct classifier, and §5 THE HARNESS. Then
Docs/HANDOFF-2026-08-11.md. Then CHECKPOINT-2026-08-10d.md for the staff-frame recipe
and the unit flip, -08-10c.md for v1's answer to the round trip, and CLAUDE.md.

RUN EVERY COMMAND FROM /Users/lrettberg/ICMLabs/Code/abcts. `cd` does NOT persist
between tool calls, and the workspace ROOT has its own vitest reach — run from there
and it collects every test in every sibling repo, abcjs's own included, and prints a
wall of failures that are nothing to do with this one.

`npx tsc --noEmit` BEFORE `git commit`, not alongside it. `npx biome check src` is NOT
clean and never was.

MEASURE, DON'T READ — and when the source is not enough, INSTRUMENT abcjs. The DUMP
LIES about staff.top: dump-elements.js publishes it BEFORE setUpperAndLowerElements
mutates it, and reasoning off it cost half a session. §5 has the scratchpad-copy recipe
that then answered the page walk in ONE run. Never instrument ../abcMusicKit itself;
another agent works there.

CLASSIFY BEFORE YOU CHOOSE. The ranked table's shape is not visible by reading it — a
crude test called 57 rows structural where the correct one calls 19, and a whole
stretch went at the minority family. §4 has the six-line classifier. Run it first.

A GATE BUILT ON OUR OWN MARKUP FAILS WHEN WE SUCCEED. Five times now. When a gate goes
red on a change that made the markup CLOSER to abcjs's, ask what the gate keys on — and
widen the REPRESENTATION rather than keying on something else.

NEVER REMOVE A SLUG FROM A PASSING LIST. THE COUNT CAN IMPROVE WHILE SOMETHING
REGRESSES.

NEVER `git checkout -- <file>`. It discarded a finished, measured refactor once. Use
`git stash` / `git diff` to inspect, and COMMIT A BIG CHANGE THE MOMENT ITS GATES GO
GREEN.
```

## Where it stands

**NINE RANKED TABLES. ONE IS OPEN: `svg-bytes`, 94 of 171**, with SEVEN byte-exact slugs
RATCHETED and **77 fixtures byte-exact**, best 52498 and median 7221. `visual-wrap-05` — the
table's HEAD at 46104 when this session opened — is one of them.

Everything else is at zero: audio 0/72, note timings 0/38, chord grids 0/23, MIDI files 0/3,
harvested geometry 0/174, pixel targets 0/120; element timings 1/13 (abcjs being
idiosyncratic, NAMED); DOM contract 1 of 25 with 24 slugs ratcheted. Suite **1158/1158**,
`tsc` clean, everything committed and pushed.

The open table splits **25 root + 50 ULP + 19 structural**. Root elements are
**146 byte-exact / 22 ULP-only / 3 structural**.

## The loop

```bash
cd /Users/lrettberg/ICMLabs/Code/abcts
npx vitest run                                   # writes every ranked table
head -1 /tmp/abcts-svg-bytes-ranked.txt          # the open one
head -1 /tmp/abcts-pixel-ranked.txt              # must stay 0 of 120
head -1 /tmp/abcts-corpus-ranked.txt             # must stay 0 of 174
```

After ANY geometry change: `npm run baseline`, then **READ the diff's SHAPE** — a new
feature only ADDS, a reorder is a pure PERMUTATION, and REMOVALS in either case mean
something broke **until a corpus-wide COUNT says otherwise**. That count is what proved the
ledger rewrite right when it deleted 184 baseline rows: `data-name="ledger"` across all 171
fixtures, 171 match / 0 differ.

## Three probes worth rebuilding rather than hunting for

- **`/tmp/gp/r.mjs`** — render ONE fixture through our engine at `{ staffwidth: 670 }` and
  print the SVG. Add `add_classes: true` when comparing against `dump-svg.js --add-classes`,
  and take it out again before byte-comparing (the goldens carry no classes).
- **`/tmp/gp/h.mjs`** — walk every fixture and classify the ROOT element as byte-exact /
  ULP-only / structural. 146 / 22 / 3 today.
- **`/tmp/gp/walk.js` + the instrumented scratchpad abcjs** — §5 of the checkpoint. The one
  that ended a whole line of guessing. **Build it before theorising about a term list.**

And the SHAPE worth copying from `/tmp/gp/led.mjs`: count ONE `data-name` across all 171
fixtures in BOTH engines. It turns "is this removal correct?" from an argument into a number.

## Where to start

**§4.1, the arithmetic family — 75 of the 94 rows**, and the only architectural one left.
Its first thread is named and bounded: **`anchorLyrics` is not abcjs's placement**, and until
it is, the lyric lane's own pitch (`lyricLanePitch`, already computed and correct to the last
digit) cannot be spent. §3 has the measurement, the fixture, and why spending it today trades
a ULP for a position error.

If you want a structural win first, the **brace with a header** (§4.2, three fixtures,
`draw/brace.js:78-98`) is the largest single one and needs no new model.

## What this session did, in one line each

`svg-bytes` 117 → 94 of 171 on twenty-seven landings. The ones whose LESSON transfers are in
§2 and §6 of the checkpoint; the three that will save the most time are:

- **A SUM CANNOT SEE AN ORDER**, and **TWO ERRORS CAN CANCEL** — the top-text block's total
  was right to the pixel and its every row 7.56px out, because a leading gap is a ROW and the
  block's ink overshoot happened to be the same 7.56.
- **A BETTER FORMULA IS STILL A DIFFERENT FORMULA** — `Math.hypot` over
  `Math.sqrt(dx*dx+dy*dy)` cost five fixtures.
- **A ULP IS CHEAPER THAN A POSITION ERROR** — the lyric lane is abcjs's, measured, and
  deliberately not spent.

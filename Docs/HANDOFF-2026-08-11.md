# HANDOFF — 2026-08-11

## The session prompt

```
We are continuing abcts development in the abcts repo (Code/abcts).

THE GOAL: abcts exists to build an abcjs-modern whose output — the SVG FILE and the
AUDIO — is 100% BYTE-EQUAL to abcjs 6.7.0. A tolerance is not a compromise to be
balanced against effort; it is A DEFECT THAT HAS NOT BEEN WRITTEN DOWN YET. Anything
you decline to reproduce goes in Docs/ABCJS-DIFFERENCES.md with its evidence, and its
slug goes in svg-bytes.test.ts's DIVERGENT list. That list is EMPTY and stays empty.
Work until byte-equal, without stopping for approval or status. Checkpoint, hand off
and compress as you go so no context is lost.

Read Docs/CHECKPOINT-2026-08-11.md first — the state, the landings, and above all §3,
which is a NEGATIVE result: the remaining ULP tail is ONE architectural defect and
fixing it site by site makes it worse. Its §4 is your work list.
Then Docs/HANDOFF-2026-08-11.md (this file), then CHECKPOINT-2026-08-10d.md for the
staff-frame recipe and the unit flip, -08-10c.md for THE HARNESS and v1's answer to
the round trip, and CLAUDE.md.

RUN EVERY COMMAND FROM /Users/lrettberg/ICMLabs/Code/abcts. `cd` does NOT persist
between tool calls, and the workspace ROOT has its own vitest reach — run from there
and it collects every test in every sibling repo, abcjs's own included, and prints a
wall of failures that are nothing to do with this one. Put the absolute
`cd /Users/lrettberg/ICMLabs/Code/abcts &&` in front of EVERY command.

`npx tsc --noEmit` BEFORE `git commit`, not alongside it. `npx biome check src` is NOT
clean and never was.

MEASURE, DON'T READ. RUN ABCJS ITSELF:
  ABCJS_VERSION=6.7.0 node dump-svg.js --add-classes --file x.abc --output x.svg
ABCJS_VERSION and --add-classes are BOTH non-optional.

A GATE BUILT ON OUR OWN MARKUP FAILS WHEN WE SUCCEED. When a gate goes red on a change
that made the markup CLOSER to abcjs's, ask what the gate keys on before touching the
code.

NEVER REMOVE A SLUG FROM A PASSING LIST. THE COUNT CAN IMPROVE WHILE SOMETHING
REGRESSES.
```

## Where it stands

**NINE RANKED TABLES. ONE IS OPEN: `svg-bytes`, 100 of 171** with SEVEN byte-exact slugs
RATCHETED and **71 fixtures byte-exact**, best 52498 and median 7221. Everything else is at
zero. Suite 1158/1158, `tsc` clean, pushed.

## The loop

```bash
cd /Users/lrettberg/ICMLabs/Code/abcts
npx vitest run                                   # writes every ranked table
head -1 /tmp/abcts-svg-bytes-ranked.txt          # the open one
head -1 /tmp/abcts-pixel-ranked.txt              # must stay 0 of 120
head -1 /tmp/abcts-corpus-ranked.txt             # must stay 0 of 174
grep "^      want" /tmp/abcts-svg-bytes-ranked.txt | sed 's/^      want …//' \
  | cut -c1-40 | sort | uniq -c | sort -rn | head    # the family histogram
```

After ANY geometry change: `npm run baseline`, then **READ the diff's SHAPE** — a new
feature only ADDS, a reorder is a pure PERMUTATION, and REMOVALS in either case mean
something broke until a corpus-wide COUNT says otherwise.

## Two things that cost time this session

**NEVER `git checkout -- <file>`.** It discarded a finished, measured refactor on the
previous session. Use `git stash` / `git diff` to inspect, and COMMIT A BIG CHANGE THE
MOMENT ITS GATES GO GREEN.

**A local "port" of a pitch expression can make the ULP WORSE.** See CHECKPOINT §3 before
touching any `reserve:`. The rule is: convert once where the value is DRAWN, and leave the
value alone where it feeds an EXTENT — until the extent itself carries pitch.

## The classifier probes

Both are recreated in three lines and are worth rebuilding rather than hunting for:

- `/tmp/gp/r.mjs` — render ONE fixture through our engine at `{ staffwidth: 670 }` and print
  the SVG. Add `add_classes: true` when comparing against `dump-svg.js --add-classes`, and
  take it out again before byte-comparing (the goldens carry no classes).
- `/tmp/gp/h.mjs` — walk every fixture and classify the ROOT element as byte-exact /
  ULP-only / structural. The split is what separates real vertical defects from arithmetic
  order.
- `/tmp/gp/led.mjs` — the shape worth copying: count ONE `data-name` across all 171 fixtures
  in both engines. That is what proved the ledger removals correct.

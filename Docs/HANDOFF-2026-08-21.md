# HANDOFF — 2026-08-21

**Branch `main`. Suite 1,883 tests, 68 files, 2 expected-fail, NO reds. Everything pushed
(`b276739`).**

**Every ranked table in the repo reads zero, and so does the one built today.** This file
supersedes `HANDOFF-2026-08-20.md` entirely; read that one only for the synth-behaviour
notes in its §6 and the `harvest-abcjs-goldens.mjs` trap.

Twenty-nine commits. What they amount to is ONE gate and TWENTY-THREE defects it found,
none of which any other gate in the repo could state — because every other one was already
at zero and had been for a day.

**START HERE (2026-08-22):** §1 is a GATE now and reads **0 of 11,048**; §2's ledger has
been swept FOUR times and paid thirteen defects; the fifth and sixth passes enumerated abcjs's
own DIRECTIVE SWITCH instead and paid sixteen more (§2c). The suite has **NO expected-fails left**. The one
thing still owed is §1's OWNER DECISION.

**START HERE:** WHAT IS LEFT §1 is now a GATE and it reads **0 of 11,004** — the overlay
pad closed both ways (§1b) and `abcLine.nonMusic` was built (§1c). The only thing left on
that list is the OWNER DECISION it declares: `abselem`/`staffGroup`, which costs a retained
`Layout`. §2 and §3 are unexamined.

---

## THE GATES

| Gate | Now |
|---|---|
| abcjs API surface | 0 of 64 absent |
| SVG bytes, in-repo | 0 of 207 fixtures |
| SVG bytes, sibling | 0 of 356 tunes |
| `tune.lines` characters | 255,684 of 255,684, 295 of 295 |
| `parseOnly` field shape | 1,617 of 1,617 rows, 0 of 331 tunes |
| **`parseOnly` VALUES** (NEW today) | **11,004 of 11,004 rows — lines, staves AND elements** |
| **RENDERED values** (NEW 2026-08-21b) | **0 of 11,004 rows, 319 tunes** — see §1 |
| `tune.deline` | 1,646 of 1,646 rows, 0 of 646 cases |
| `synth.sequence` | 4,811 of 4,811, 0 of 179 |
| `makeVoicesArray` | 4,220 of 4,220 |
| `tune.setupEvents` | 3,366 of 3,366 |
| `TimingCallbacks` | 4,696 of 4,696 |
| `tuneMetrics` | 219 of 219 sections, 0 of 225 tunes |
| `Editor` | 196 of 196 steps |
| `setGlyph` | 6 of 6 renders byte-exact |
| `CreateSynth` / `SynthController` / `CreateSynthControl` | 196 / 184 / 84 steps |
| `engraver.selectables` | 389 of 389 |
| `extractMeasures` | 1,721 of 1,721 |
| corpus / pixel / DOM / warnings / animation / audio / MIDI / chord grids | 0 |

⚠️ **A GATE'S REPORT FILE OUTLIVES ITS RUN.** `/tmp/abcts-*.txt` is stale until you re-run
the test that writes it.

⚠️ **AND A RED FULL-SUITE RUN IS NOT NECESSARILY RED.** vitest's workers die under memory
pressure: the run comes back `Test Files 5 failed | 63 passed`, the failing set is DIFFERENT
every time, **there is no assertion text at all**, and the duration is five times normal.
Seen again today at 122s. Re-run before believing a red — and READ IT: a real failure names
an assertion.

---

## WHAT IS LEFT

### 1. THE RENDERED TUNE'S OBJECT — A GATE NOW, AND IT READS ZERO

`tests/render-values.test.ts` is `parse-values`' own reduction against a RENDERED tune,
319 tunes and **11,004 rows, 0 differing**, oracle harvested by running abcjs
(`scripts/harvest-abcjs-render-values.ts`). It replaces `scripts/zzrv.ts` as the measure;
the probe stays because it counts the one thing the gate declares away.

⚠️ **AND IT DECLARES EXACTLY ONE ABSENCE: `abselem` / `staffGroup`.** Every drawn element
carries a back-pointer to the laid-out element (`draw/absolute.js:72`) and every drawn line
its staff group; publishing them means RETAINING THE `Layout`, **which measurably killed
this suite's workers once** (2026-08-16: 5.6s → 50-120s, a different test failing each
run). `deline` sidesteps it by modelling the PRESENCE of `abselem` as a boolean — which is
how it passes at 0 of 646 without the field existing — and a host that wants the drawn
element has `getSelectableArray`. **THIS IS AN OWNER DECISION, NOT A BUG FIX. Do not start
it without one.** The two keys are stripped in the gate, where the decision is visible, and
`npx tsx scripts/zzrv.ts` still counts the 10,049 rows it costs.

The gate opened at 60 rows over two classes, and both are closed — §1b and §1c.

### 1b. THE OVERLAY PAD — IDENTITY *AND* VALUE (2026-08-21b)

`tests/overlay-pad.test.ts` gates it, and fails on either half alone — checked by reverting
each in turn.

⚠️ **THE IDENTITY WAS A JOIN NOBODY HAD MADE, AND ITS HOOK WAS ALREADY THERE.**
`core/overlays.ts` runs on BOTH sides and carries an opaque `ref` that its back-fill copies
onto the pad it makes as `pad`. Tagging each PROJECTED element with its own event before the
pass therefore makes every pad name the event it MIRRORS — the same event the model's pad
names through `overlayMirrors` — so `byEvent` can key the model pad at the PUBLISHED element
and `abcelemOf`'s synthesized third object is dead for it. A WHOLE-MEASURE pad has no mirror
and joins by the BARLINE's span instead.

⚠️ **AND THE TAGS COME OFF BY WALKING THE LINES, NOT `byEvent`** — the ending-mark sweep
shallow-COPIES the last voice's elements, so the published object is not always the one
`byEvent` holds. That same copy is why a LAYER'S OWN NOTES were unstamped too, 9 of the 40
rows: re-pointing the index at whatever was PUBLISHED closes both.

⚠️ **AND `bd67ae6`'s VALUE FIX WAS RIGHT FOR ONE LINE AND WRONG FOR THE OTHER.** The note it
left said abcjs's rendered `flattener-21` has NO `stem` element in any of its three voices;
dumping that tune's `tune.lines` denies it — `L3/s0/v1` reads `STEM:down r bar n bar`, and
abcjs stamps that rest `averagepitch: 3`. `resolveOverlays` splices `{stem, down}` onto the
voice it CREATES and back-fills the earlier lines with bars and invisible rests alone, while
`createABCVoice` resets `stemdir` per LINE — so the line the layer SINGS on reads 3 and the
lines it only pads take the default 7. `layerSingsOn` is that split, and the byte gate is
unmoved by it. **A SOURCE READ THAT PREDICTS AN ABSENCE IS STILL A PREDICTION: DUMP THE
OBJECT.**

### 1c. `abcLine.nonMusic` — AN ENTIRE FIELD THAT WAS NOT BUILT (2026-08-21b)

`constructTuneElements` hangs a `Subtitle`, a `FreeText` or a `Separator` on every nonMusic
LINE at engrave time (`engraver-controller.js:229-247`) and a host reads `line.nonMusic`;
ours had none — an `abcjs-debt:` note in `compat/lines.ts` had said so for days.

The rows are the layout's OWN moves and texts, recorded by `appendFreeText` as it spends
them and joined back by block IDENTITY (`Layout.nonMusicRows`, keyed by `FreeTextBlock`) —
the same shape of join as the overlay pad's, and the reason nothing is re-derived and no
`Layout` is retained. Four rules came out of it, each a read of a named function:

- **A NONMUSIC ROW IS NOT `addTextIf`'s SHAPE.** `Subtitle` and `FreeText` push a literal of
  their own, so the KEY ORDER differs from a top-block row's — and **the class is
  UNCONDITIONAL** (`'text subtitle'`, `'defined-text'`) where `TopText` gates every one of
  its own on `shouldAddClasses`.
- **AN ABSENT SPAN IS ABSENT, NOT `-2`.** A `%%center` has no `info` at all, so
  `JSON.stringify` drops both keys; the `-2` is `addTextIf`'s default and belongs to the top
  block alone.
- **THE ROW'S TEXT IS THE WHOLE BLOCK, NEWLINES AND ALL** — and it is the same string the
  row's SELECTABLE carries (`draw/non-music.js:29`), so ours passed the first line to both.
  `freeTextOf` is the one join now, in `core/model.ts`, where `tune.lines` and the layout
  both reach it.
- **A SUBTITLE ABOVE THE FIRST NON-SUBTITLE LINE GETS NOTHING**, because the title block
  already accounted for it — `hasSeenNonSubtitle` gates that arm alone.

### 2. THE `ponytail:` LEDGER — SWEPT ONCE, AND THE SWEEP PAID (2026-08-22)

`grep -rn "ponytail:" src tests scripts` is 89 entries and most are architectural. **SIX of
them named a shape and said no fixture in either corpus writes one**, which made each a
PREDICTION rather than a measurement. `tests/corpus-abcjs/fixtures/abcts-ledger-gaps.abc` is
the measurement — one tune per shape, goldens by running abcjs — and **three were real
defects, none visible to any gate in the repo**:

- **`%%scale` WAS NOT MODELLED.** The tune is laid out LARGER and the SVG CSS-scaled down.
  ⚠️ **AND PRINT AND THE SCALE WERE ONE FLAG** — `spacing.top` and the eleven-inch floor are
  `isPrint`, the transform and its divisions are the SCALE — so a screen `%%scale 0.8` took
  print's 30.24px lead and came out 1320px tall. ⚠️ **AND THE CALLER HAS ALREADY DIVIDED BY
  PRINT'S 0.75**, so only a DIRECTIVE divides the host width here: `x * 0.75 / 0.75` is not
  `x` in doubles, and dividing unconditionally put all 121 `-print` fixtures 358px wide.
- **AN UNPARSEABLE TOKEN ENDS A CHORD WHERE IT STANDS** and is not consumed
  (`abc_parse_music.js:472-489`), so `[C"Am"E]` is a chord of one, an annotated `E` and a
  leftover `]` — **and a stray `]` is an INVISIBLE BARLINE** (`abc_tokenizer.js:161-173`).
- **A MICROTONE INSIDE A CHORD IS READ EXACTLY AS ONE OUTSIDE IT**, `getCoreNote` being the
  same function on both paths — **and the quarter tone is a property of the HEAD**, where
  `microtoneCents` is per event, so the glyph came out a full sharp.

`transpose=` on `K:` was a fourth (the staff clef reports it) and the two audio shapes were
the fifth and sixth — see §2b. All six tunes are byte-exact and ratcheted.

**THE PATTERN IS THE FINDING**: a `ponytail:` saying "the corpus never varies this" is a
prediction, and writing the fixture is how it becomes a measurement.

**SO IT WAS RUN AGAIN, AND PAID BETTER.** `abcts-ledger-gaps-2.abc` writes eight more such
shapes and **FIVE were defects**:

- **A KEY SIGNATURE ON A TENOR CLEF SAT AN OCTAVE HIGH.** `addPosToKey` is a TABLE, not a
  shift — three brackets on the clef's `verticalPos` with per-accidental exceptions inside
  them, one of which (`mid === -8`, `f`/`g` sharp) IS the tenor
  (`abc_parse_key_voice.js:104-160`). ⚠️ The exceptions are keyed on the WRITTEN NAME, case
  included, which is fixed by the TREBLE position — so the test is on that number and no
  letter's case has to travel through the layout. The fold it replaced agrees with abcjs on
  treble, bass and alto, which is every clef either corpus writes a signature on.
- **A CHORD'S DOTS MAY COINCIDE** — `dotadjusty = 1 - abs(pitch) % 2`, per head, with no
  memory of the heads before it. Ours bumped the second up a space: better engraving, and a
  different picture. The collision set is DELETED rather than guarded.
- **A BELOW ANNOTATION HANGS OFF THE LYRIC LANE, NOT THE INK** — abcjs's below chain is
  lyric, then chord, then dynamics. ⚠️ **AND THE RESERVE IS OFF THE INK WHILE THE BASELINE
  IS OFF THE LANE**: adding the block below an already-spent lane counts it twice and grew
  the page by exactly one lyric block.
- **A PERCUSSION CHORD RESOLVES ITS HEAD GLYPH PER PITCH**, and ⚠️ **the STEM rule reads the
  HIGHEST head's glyph**, because abcjs reassigns `noteHead` per pitch and tests it after
  the loop.
- **A MID-MEASURE `[K:… clef=]` IS NOT THE LINE'S CLEF.** `startNewLine` copies
  `multilineVars.clef` when the line opens, so an inline change written after the line's
  first note leaves `staff.clef` alone, draws where it stands, and reaches the NEXT line's
  staff. Ours took it for the whole line and drew ONE clef, at the head, in the wrong shape.
  The projection, the drawing and the `%%MIDI drummap` stamp all take the split now — that
  last one was another `ponytail:` naming this shape exactly.

**AND A THIRD PASS PAID THE SAME.** `abcts-ledger-gaps-3.abc` writes eight more; three
needed no work and **five were defects**:

- **`%%abc-copyright` AND ITS SIBLINGS DREW NOTHING** — `%%abc-<sub>` is
  `metaText['abc-'+sub]` as a PLAIN STRING and three of the five draw a row at the foot of
  the tune after `History:`. ⚠️ The span's `restOfString` is what follows `%%abc`, so the
  `abc` counts ONCE.
- **A SLUR CLOSING ON ONE HEAD OF A CHORD NEVER PAIRED.** A head close is numbered
  `chordPos * 100 + 1` and, finding its own position empty, **TAKES ANY OTHER OPEN** — so
  `(F [GB)d]` is a real curve where `[(CE)G]` is two halves. Ours made a half of every
  chord-internal mark, which is right for the second and wrong for the first.
- **A CURVE CROSSING A SYSTEM IT DOES NOT END ON GOT NO ARC.** `TieElem` is per LINE, so
  the crossed line has NEITHER anchor: `startx + 10` to `lineEndX`, at abcjs's own
  `startY = endY = above ? 14 : 0` under *"the slur covers the entire line"*.
- **THE BELOW-ANNOTATION LANE ROUND-TRIPPED THROUGH PIXELS** — one ULP of the root height,
  closed by `PlacedText.reserveBottomPitch`, the mirror of `reserveTopPitch`.

Twenty-two tunes across the three fixtures, all byte-exact and ratcheted; the byte gate is
**0 of 229**, parse-values and render-values **0 of 11,198**. `grep -rn "ponytail:" src
tests scripts` is 92 entries and half a dozen still read as predictions — the sweep has paid
**thirteen defects** in three passes and has not stopped paying.

### 2b. THE FLATTENER'S CHORD TIES — CLOSED, AND THE LAST EXPECTED-FAIL WITH THEM

`tests/chord-tie.test.ts` was written on 2026-08-15 asserting what abcjs does and marked
`.fails` so it would go RED the moment the gap closed. It has. abcjs resolves ties on the
PITCH (`abc_midi_flattener.js:287-325`) where ours had one duration and one `tiedOver` flag
per EVENT, inside `if (event.type === 'note')` — so a chord's tie did nothing at all.
`VoiceItem.tieExtra` / `.tieSilenced` are the per-head halves. ⚠️ **Only the note path moves
a duration, so only it zeroes the clock.**

⚠️ **AND ONE SHAPE IS MEASURED AND DELIBERATELY NOT PORTED**: `(p:q:r` with `r < p` NEVER
ENDS in abcjs — the group is one note, that note carries both `startTriplet` and
`endTriplet`, `if (elem.startTriplet)` wins the if/else, and the arm that clears
`tripletMultiplier` never runs, so every element to the end of the voice is scaled. Both
engines' numbers are at the site in `src/audio/flatten.ts`; porting it means moving where
tuplets are resolved.

### 2c. AND THE FOURTH SWEEP FOUND NOTHING, WHICH IS WHY THE FIFTH READ abcjs INSTEAD

`abcts-ledger-gaps-4.abc` writes six more predicted shapes and **every one was byte-exact on
the first run** — six entries became measurements and no defect. That is the pattern's other
outcome, and it is the signal that the LEDGER is spent as a source.

**SO THE NEXT ENUMERATION CAME FROM abcjs's OWN SWITCH.** `abc_parse_directive.js` names
**41 directives this parser never mentioned**; rendering one tune through abcjs with and
without each says **only TEN move its output** — the other 31 are inert in that shape.
`abcts-directives.abc` is those ten, and NINE are byte-exact now:

- **THE FOUR PAGE MARGINS.** ⚠️ **A MARGIN IS USED RAW AND A SPACING TAKES THE `4 / 3`** —
  `%%topmargin 40` is forty PIXELS and `%%vocalspace 30` is forty. ⚠️ **AND `(w - m) - m` IS
  NOT `w - 2m`**: giving the right margin its own term moved a notehead on all 47 `-print`
  fixtures at once, so `pageSides()` keeps the `2 * m` form wherever the two are equal.
- **`%%titlespace` / `%%vocalspace` / `%%stafftopmargin`**, the last two defaulting to ZERO.
  ⚠️ `%%vocalspace` does TWO things — grows the lyric lane AND drops the baseline by the
  same amount, so the page total does not move. ⚠️ `%%stafftopmargin` must not reach the
  music-only extent that places a heading, or the staff lands 40px below the title.
- **`%%titleleft`** (anchor and x, title and subtitles), **`%%bagpipes`** (`stemdir` per
  LINE, a voice convention), **`%%newpage`** (a LINE nothing draws, whose whole cost is the
  61.33px any non-music line before the first staff spends).

⚠️ **AND ONE ROW IS OPEN WITH ITS CAUSE NAMED**: `%%stafftopmargin` on a titled tune differs
in the last bits of the root height, and the SAME tune without a title is byte-identical in
both engines — so the residue is the y-versus-pitch asymmetry of a staff carrying a heading,
not the directive. Two formulations of the margin produce the identical double.

**AND THE ENUMERATION HAD TO BE RUN TWICE, WITH THE RIGHT SHAPES.** The first pass measured
all 41 on ONE plain tune; the other 31 were inert IN THAT SHAPE, which is not the same as
inert. A tailored control per directive — a subtitle, a composer, a `W:`, four bars, a beam,
a grace group, print — moved **seven more**, and `abcts-directives-2.abc` gates six of them
(`%%topspace` is print-only, so no screen golden can reach it):

- **three more `spacing.*` overrides**, and the parser takes abcjs's WHOLE
  `oneParameterMeasurement` list now rather than a hand-picked seven. ⚠️ `%%wordsspace` had
  to become a MOVE rather than an empty row — its default is zero, and a zero add must not
  reach the page walk twice, so the row was written and never spent.
- **`%%flatbeams`** is `calcSlant`'s first line; **`%%graceslurs 0`** is the third term of
  the `if` that builds a grace group's curve, and UNSET IS TRUE.
- **`%%barsperstaff N` IS A PARSE-TIME REWRITE** — `while (wrapMusicLines(...))`, which our
  model says with `startsSystem`. ⚠️ **AND THE LINE IT CUTS OUT REPRINTS THE METER**: the
  wrap copies the line whole, so the new one keeps a `staff.meter` an ordinary later line
  does not have — 21.8px, and two `staff-extra time-signature` groups against our one.
  ⚠️ **AND SUCH A LINE IS NOT A SOURCE LINE**, so the span tiling carries across it;
  carrying on every projected line instead took 30 notes of another fixture back a
  character, because a NOTE closes over its trailing newline and the guard cannot tell a
  width-wrap from a source line.

**A "SAME" IS ONLY AS GOOD AS THE SHAPE THAT ASKED.** Seventeen of the 41 move abcjs's
output; the first control could reach ten.

**THE PATTERN GENERALISES: ENUMERATE THE REFERENCE, NOT THE NOTES.** The directive switch
was one list; `index.js`'s API surface was another (0 of 64 absent); abcjs's own test files
are a third. A list of what the reference HAS beats a list of what we wrote down.

### 3. THE NEXT ENUMERATION QUESTION IS STILL "WHICH BEHAVIOUR"

Both patterns paid again today, and both are cheap:

- **WIDEN WHAT A GATE LOOKS AT.** `parse-values` exists because `parse-only` compares field
  NAMES; it opened at 1,249 and named fourteen defects. Then **its own walk was widened**
  past `staff.voices` and opened again at 12, for eight more.
- **WRITE A FIXTURE FOR A SHAPE THE CORPUS DOES NOT HAVE.** `abcts-model-gaps.abc` gates the
  three shapes the previous handoff had listed as unwritten; two needed no work, the third
  was two different questions, and the fixture then opened `parseOnly` and `deline`.

---

## WHAT CLOSED, AND THE RULE EACH ONE PAID FOR

### 1. THE BYTE GATE — 196 → 0 of 207, CLOSED BOTH WAYS

The two slur tunes `HANDOFF-2026-08-20.md` §1 left. ⚠️ **ITS DIAGNOSIS WAS WRONG WHILE ITS
MEASUREMENT WAS RIGHT** — it read abcjs's `startLimitX` and `lineEndX` off a probe correctly
and concluded ours took a different arm; ours already produced 60.846 and 117.27 to the
digit. **A NUMBER READ OFF THE ORACLE IS NOT A DIFFERENCE UNTIL YOU READ THE SAME NUMBER OFF
YOURSELF.** The real causes: a grace group walked TWICE where `addGraceNotes` walks it once,
a missing 3px grace inset (`calcX`'s only special case), a missing reserve, and an unpaired
half ordered as though it were a carried one.

### 2. `parse-values` — A NEW GATE, 1,249 → 0 of 11,004

One row per LINE, per STAFF and per ELEMENT of a `parseOnly` tune, keyed
`L{n}` / `L{n}/s{n}` / `L{n}/s{n}/v{n}/{i}`, value = the object canonicalised to JSON.
319 tunes. Oracle by RUNNING abcjs. **EXACT, not a floor.**

The twenty-two defects, grouped by the rule they paid for:

**A FIELD CAN BE RIGHT WHERE IT IS DRAWN AND ABSENT WHERE IT IS PUBLISHED.**
`verticalPos` was `pitch` on every note of every non-treble clef in both corpora (1,220
rows) — the PARSER writes `p.pitch - workingClef.verticalPos` as the element is appended
(`tune-builder.js:917-928`), and ours held it back with the engraver's fields under a note
calling it "a placeholder until the drawing is walked". A whole VERSE was missing from
`el.lyric` (133). Same-position annotations join with `\n`. The voice titles are in
DECLARATION order where the voices are in `%%score` order, **and abcjs pairs them by index**
— so it draws each voice under the other's name, which we already reproduced in the
DRAWING and not in the object.

**AND IT IS NOT ALWAYS COSMETIC.** `~` is `irishroll`, `M` is `mordent`, `P` is
`pralltriller` — a shorthand is NOT its long form, and `abc_midi_flattener.js:404-419` has
no `irishroll` case at all, so `~` sounds PLAIN where `R` is rolled. 210 byte-exact
fixtures, the pixel gate and the DOM contract were all silent.

**A RULE THAT FIRES IN TWO PLACES IS TWO RULES UNTIL THE SOURCE SAYS OTHERWISE.** There are
two whole-rest rules — the PARSER's `duration === 1 && durationOfMeasure <= 1` and the
ENGRAVER's `measureLength === duration` — and gating ours on `engraved` alone took the count
the WRONG WAY, 8 differing to 35. There are two `addEndBeam` paths for a chord. There are
two whole-note-rest concatenation orders for the naturals (FIRST in the stream, LAST on the
staff), and neither half was built.

**AND WHAT THE ENGRAVER DOES IS NOT WHAT THE PARSER DID.** A chord's pitches are sorted by
`createNote`, not by the parser (63 rows); `rest.type` is rewritten to `whole` by the
engraver as well as by the parser; `el_type` is renamed. `engraved` is the flag.

### 3. THE RULES, AND THEY COST SOMETHING TO LEARN

⚠️ **A ROW COUNT GOING DOWN IS NOT A RULE BEING RIGHT.** Two readings of the chord beam
break were taken off the corpus and both were wrong — **and the second improved the corpus
by 44 rows while being wrong.** What caught it was the SAME SOURCE answering differently at
`L:1/4` and `L:1/8`. An instrumented abcjs then named the cause in one run: `addEndBeam` on
a chord tests `el.duration` BEFORE `* chordDuration` — the first note's, not the chord's.
The meter rule had the same shape: preferring the leading `M:` fixed one fixture and broke
another, because the split is standalone-versus-inline.

⚠️ **PRINT THE VALUE THE CALLER PASSED, NOT THE ONE THE CALLEE HAS LEFT.** Two readings of
the key naturals came from a probe placed BELOW `createStaff`'s own delete
(`tune-builder.js:1002-1005`), so every staff reported `nat=undefined` and "whichever path
takes them first wins" looked MEASURED. It is false: `appendStartingElement` deletes the
property on `fixKey`'s SHALLOW COPY, so the list still reaches the next line.

⚠️ **A DERIVED FLAG MUST BE RECORDED WHERE IT IS DECIDED IF THE DECISION CONSUMES ITS OWN
INPUT.** The parser's whole-rest rule rewrites the duration to the measure's, so
`notatedDuration` can no longer answer "was this written as a whole note". `Rest.wholeRest`.

⚠️ **A WALK MEASURES WHAT IT DESCENDS INTO, AND NOTHING IT PASSES OVER.** The first cut of
`valuesOfTune` descended `line.staff[].voices[]` and emitted only what it found at the
bottom — so a staff's `key`, `meter`, `title`, `brace` and fonts and a line's `text`,
`separator` and `subtitle` were never compared. **Exactly the hole the gate was built to
close in `parse-only`, one level up, and I built it in again.**

⚠️ **A SHARED SCRIPT, NEVER A COPY.** `scripts/zzpv.ts` held its own `canon`/`rows` pair and
reported 9,727 while the gate saw 11,004 — the same trap `synth-controller.test.ts` paid for.

### 4. THE SMALLER ONES, EACH A READ OF A NAMED FUNCTION

A `/` marks the grace note it PRECEDES and `{A/B}` has none (that `/` is `A`'s length);
a space after a grace ends its beam; the running fonts are seeded with abcjs's DEFAULTS and
not with what the source set; a line's font snapshot is taken PAST its leading inline
headers; an explicit `stafflines=5` is not the default; `%%sep`'s span is the line's start
plus the length of a DIFFERENT string; a `%%begintext` block is measured from its CLOSING
line and every line contributes its own newline; a subtitle is RICH TEXT.

---

## THE HARNESS

| Script | Prints |
|---|---|
| `npx tsx scripts/zzpv.ts` | the value table, CLASSIFIED BY WHICH KEY DIFFERS; `K=<class>` filters to one and shows a full example |
| `npx tsx scripts/zzrv.ts` | the same against a RENDERED tune, with the marker-only rows counted separately |
| `npx tsx scripts/zzbeam.ts` | the beam ladder — 11 shapes × BOTH note lengths, self-scoring (`ok` / `DIFF`) |
| `SLUG=… npx tsx scripts/zzdel.ts` | every `deline` row that differs for one case |
| `npx tsx scripts/zzpo.ts` | every `parseOnly` FIELD-NAME divergence, classified |
| `F=… npx tsx scripts/zzrag.ts` | every `tuneMetrics` number that differs for one tune |
| `npx tsx scripts/zzslur.ts` | slur marks on all three levels |

abcjs-side probes live in `/tmp/gp/abcjs` and are instrumentation of the reference itself,
each gated on its own env var. Added today: **`ZZBEAM`** at `appendElement`, **`ZZCHORD`**
in the chord duration block, **`ZZKEY`** at `addPosToKey`, **`ZZNAT`** at
`appendStartingElement` and `createStaff`, **`ZZSEP`** at `addSeparator`.

⚠️ **AND BRACE THE `if`.** Four probes once stole an unbraced `if`'s body. `/tmp/gp/twin.js`
renders every fixture through BOTH trees and diffs, but it cannot see a guard no fixture
reaches — so gate the probe AND brace the branch.

⚠️ **`/tmp` IS CLEANED.** The scratchpad abcjs comes back with `src/` intact and every
top-level file gone, which reads as `Cannot find module` rather than as a missing tree.

---

## THE METHOD, UNCHANGED

**Read the named abcjs function FIRST, then instrument to confirm it — neither half works
alone.** Every fix after Lance's reminder came that way, and the three that did not cost a
revert each. Run every command from `/Users/lrettberg/ICMLabs/Code/abcts`. Run
`npx tsc --noEmit` BEFORE every commit — vitest transpiles without typechecking and will
not tell you. Commit and push after every landing; do not batch. Re-ratchet each gate's
PASSING list in the same commit. Never push `--force`; if a push is REJECTED, stop and
report — a `Connection refused` on port 22 is the network and may simply be retried, which
happened once today. Omit `Co-Authored-By` trailers in this workspace.

# Handoff — 2026-08-08e

State and the audio arc's thirteen findings are in `CHECKPOINT-2026-08-08e.md`.
`-08-08d.md` keeps the 6.7.0 flip and **the reasoning behind the optimisation pass, which
is now the live phase — read it, do not re-argue it**. `-08-08c.md` keeps the audio arc's
first findings; `-08-08b.md` keeps 147–150 and the geometric tail; `-08-08.md` keeps the
ARC DECISION; `-08-07b.md` keeps 134–146 and the method; `-08-06.md` keeps **THE HARNESS**.
Earlier ledgers: `-08-06b.md` (106–124), `-08-05c.md` (90–103 and the `ENGRAVE` triage),
`-08-05b.md` (71–89), `-08-04c.md` (51–70 and the ladder method), `-08-04b.md` (41–50),
`-08-03d.md` (16–40).

---

## THE PROMPT

```
Continue abcts toward 100% abcjs parity (~/ICMLabs/Code/abcts, branch `main`,
git@github.com:ICMLabs/abcts.git). Pushed and green at 989/989.

RUN EVERY COMMAND FROM /Users/lrettberg/ICMLabs/Code/abcts. `cd` does NOT persist
between tool calls, and the workspace ROOT has its own vitest reach — run from
there and it collects every test in every sibling repo, abcjs's own included, and
prints a wall of failures that are nothing to do with this one. Put the absolute
`cd /Users/lrettberg/ICMLabs/Code/abcts &&` in front of EVERY command.

THE TARGET IS abcjs 6.7.0. It was 6.6.3 until 2026-08-08, so EVERY citation
written before that date names a 6.6.3 line number — both trees are vendored side
by side, so a stale one can be CHECKED rather than guessed at, and the sibling
`dump-svg.js` takes ABCJS_VERSION.

THE STANDING ORDER: 100% parity with abcjs on EVERY tune. GONZATO IS DEFERRED by
decision. GEOMETRY IS DONE. **AUDIO IS DONE — 0 of 54.** Work until the order is
reached without stopping for approval or status. Checkpoint, hand off and compress
as you go so no context is lost.

FIRST: read Docs/CHECKPOINT-2026-08-08e.md — the state, the audio arc's thirteen
findings, and WHAT IS LEFT, which is your job. Then -08-08d.md for WHY THE
OPTIMISATION PASS WAS DEFERRED and on what terms (do not re-argue it), -08-08c.md,
-08-08b.md (147-150), -08-07b.md (134-146 and the method), -08-06.md for THE
HARNESS, ARCHITECTURE.md, CLAUDE.md.

START HERE, EVERY TIME:
  npx vitest run tests/audio-ranked.test.ts && cat /tmp/abcts-audio-ranked.txt
  npx vitest run tests/pixel-parity.test.ts && cat /tmp/abcts-pixel-ranked.txt
  npx vitest run tests/corpus-abcjs-ranked.test.ts && cat /tmp/abcts-corpus-ranked.txt

ALL THREE TABLES ARE REGRESSION NETS NOW — 0 of 54, 0 of 174, and 1 of 120, the
one being `extra-class`, recorded and named. **So no gate can name the next
defect.** That has happened twice before and the answer both times was to BUILD A
GATE that expresses an axis none of the others can. Ask what the gates' DATA
MODELS leave out, not whether a number is small.

THE PHASE IS THE OPTIMISATION PASS, and it is STRUCTURAL ONLY, NEVER FOR SPEED:
220 tunes render in 151ms and the dominant cost — nine layout passes per line —
is MANDATED by finding 104. `tests/bench.test.ts` holds the before-number. THE
INVARIANT IS THAT NO BASELINE MAY MOVE: if one does, that is a behaviour change,
revert it rather than re-record it. And above all do not "clean up" the comments —
layout.ts is 49% comment and those comments ARE the finding ledger. The test for
any deletion: could a future session re-derive this finding without the comment?

MEASURE, DO NOT REASON. The probe runner is
/tmp/abcts-probe/abcjs-probe/flat.js — a jsdom shim plus
`visualObj[0].setUpAudio({})`. `DUMP=1 node flat.js <abc>` prints abcjs's whole
answer; ABCJS_PROBE=1 prints whatever the instrumented scratchpad copy logs.
Rebuild it from CHECKPOINT-2026-08-06.md's HARNESS section if /tmp has been
cleared, and point the copy at abcjs-6.7.0.

READ ABCJS'S CODE. IT HAS THE ANSWERS. And LOOK IN THIS REPO FIRST: two of the
audio arc's thirteen findings were rules already implemented here that I started
to write a second time, and three more were inputs the parser was silently
dropping with a `ponytail:` marker sitting on the line.

RATCHET AND NEVER SHRINK. All 54 audio slugs are in `PASSING`; the geometry
ceilings are the same rule.

`npx tsc --noEmit` BEFORE `git commit`, not alongside it.
`npx biome check src` is NOT clean and never was; diff against a stash.
```

---

## WHERE TO START

```
  0 of 54 audio cases differ         PASSING is all 54
  0 of 174 harvested fixtures        against the 6.7.0 oracle
  1 of 120 pixel targets             `extra-class`, recorded
989 / 989 tests
```

### 1. THE OPTIMISATION PASS — the phase boundary, reached

Terms in `CHECKPOINT-2026-08-08d.md`'s `⏳` section, unchanged. Order: harvest the
`ponytail:` ledger and triage; split `layout.ts` along its seams mechanically; close the
`ENGRAVE` bare-literal table; then the module-level mutables *if* they are a real hazard.
NO BASELINE MAY MOVE.

### 2. `extra-class` — the one open parity target

`oy = -3.88`, one PITCH, uniform. `!class=alice!A!class=bob!!>!T[dfa]` — a chord carrying
BOTH an accent and a trill, which nothing else in either corpus does. The residual is the
two-decoration STACK on a chord. Build a LADDER of controls: one decoration on a chord, two
on a note, two on a chord.

### 3. THE NEXT ORACLE, if a new arc is wanted

`timing.test.js`'s `setTiming` gates the audio↔geometry JOIN, which nothing does today —
its rows carry `line`, `left` and `endX` beside the pitches. `midi.test.js`'s FILE writer is
a second surface on top of the event list. Both harvest the way `flattener.test.js` did:
EVALUATE the test file with `describe`/`it`/the assert helper replaced. See
`npm run harvest:audio`.

### 4. The small named divergences

Listed in the checkpoint's **WHAT IS LEFT §4** — full-line `I:` in strict, `(p:q:r` with
`p !== r`, overlay track numbering across staves, host-supplied drum options. None has a
case behind it; each wants a control tune before it is touched.

---

## WHAT NOT TO CHASE

- **The three ranked tables.** Regression nets.
- **Speed.** 220 tunes in 151ms; the nine-pass solve is mandated by finding 104.
- **`create-synth.js`, `synth-controller.js`, soundfonts, WebAudio.** Host playback.
- **The dynamic glyph's last 0.37px.** Outline, proven by its sign.
- **`scripts.stopped`.** The one abcjs glyph still unmapped; no control built.
- **GONZATO.** Deferred by decision.

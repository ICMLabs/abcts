# HANDOFF — 2026-09-16

Supersedes `HANDOFF-2026-09-15.md`, whose §WHAT IS LEFT this session emptied but for one
ULP. Read `CHECKPOINT-2026-09-16.md` first — §3 is the rules, and two of them are
corrections to things this repo had written down and had wrong.

## THE STATE

    suite       94 files, 2,693 tests, no reds
    svg-bytes   0 of 691 in-repo, 0 of 356 sibling
    mode-bytes  11 of 691 — every one DECLARED
    midi-bytes  0 of 691 — 19 ruled divergent
    zzlive      0 of 685  WebKit AND Chrome        zzselect 0 of 685  WebKit AND Chrome
    zzclick     1 of 685 DECLARED ×3               zzledger 0 differ, 0 KNOWN, 41 agree
    zzopts      26 rows. 5 differing of 17,810 — and FOUR are `wrap + staffwidth`'s FLOOR.
                minPadding 1 · every other row 0
    warnings    0 of 815 · test:dist 0 of 685 ESM and CJS
    lint        0 errors, 33 warnings — A GATE NOW (2026-09-16), from 1,022
    package     2.0 MB packed / 6.9 MB unpacked, 25 files, version 6.7.0 — NOT PUBLISHED

Every number above was re-run at this commit, including both browser gates in **both**
engines. `Docs/PARITY-STATUS.md` §1a is the same table in plain language and is the file to
hand anyone asking how close this is.

## WHAT THIS SESSION DID

Seven `zzopts` rows closed — `timeBasedLayout`, `add_classes`, `scale 0.8`,
`oneSvgPerLine + scale 0.8`, `scale 1.5`, `print`, `print + responsive` — in seven commits,
each with a runnable check that was **watched failing against the old code first**.

⭐ **EVERY ONE WAS A REPRESENTATION, AND FIVE WERE THE SAME SHAPE:** the engine held a SUM
and took a part back off to reach the quantity abcjs actually stores. A rule's centre for
its edge; the summed page for the music width; the music width for the page. `(a + b) - b`
is not `a`, and on a rounding boundary that is a printed digit. See the checkpoint's §3.

## ⚠️ WHAT IS LEFT — one ULP, and it is a DOMAIN rather than a term

**`minPadding`, `synth-flattener-23`: two ULP in the root `width`.** Both sides were
instrumented at the element where the chains part (the twelfth) and **the grouping already
agrees** — abcjs spends `x += extraWidth - er` as one term and so does this engine
(`placed = x + (wants - room)`); `minx` is two adds on both sides. abcjs's own terms there:

    x(trial) 526.703   minx 507.51300000000003   er 19.18999999999994
    extraWidth 56.95 (pad 40, extraw -16.95)  ->  X 564.4630000000001

Its `er` carries a tail its PIXEL chain put there, where this engine walks the line in STAFF
SPACES and multiplies once at the end. So the row is a units decision about the solve, and
it is worth less than it costs: two ULP in one root `width` on one fixture, with nothing
visibly moved. **Do not open it as a "last digit" — it is not one.**

**`wrap + staffwidth` is at its floor of 4** — two are abcjs's fourth debug marker, DECLINED
by the owner, and two are abcjs CRASHING in its own `wrapLines`. An agent that finds any
closed row above zero has broken something.

## ✅ THE DEBT LEDGER IS SWEPT, AND IT WAS MOSTLY DESCRIBING A REPO THAT HAD MOVED ON

**§A's ten markers are RESTATED AS DECISIONS in the code** — six of them one decision (the
render-scoped module `let`s; `RenderState` already measured the alternative at 217
references). **§B was swept live against abcjs and FIVE OF ITS SIX ROWS WERE ALREADY
CLOSED**: a voice after a header `K: transpose=`, a `tempo` and a `part` as selectables, a
header `P:ABAB` (not a parity gap at all — abcjs expands nothing either), `%%titleformat`
and friends (abcjs implements none), and the part box's four edges (byte-identical at six
staff widths). Three now have tests. A sixth row with a real trigger — the compound-meter
tempo branch — was RUN rather than waited on: 28 meter × tempo combinations, byte-identical,
and abcjs's own byte does not move either.

⭐⭐ **A DEBT ROW AGES INTO A CLAIM ABOUT THE CODE THAT THE CODE HAS QUIETLY STOPPED AGREEING
WITH.** One row carried "✅ MEASURED 2026-09-08: byte-identical" — an SVG comparison, for a
rule the line above it says the renderer never reads. Re-measure before working a row.

⚠️ **And the marker COUNT has been wrong three times, the instrument moving each time rather
than the code.** Re-harvested: **53 markers**, 21 naming no trigger, 16 carrying a
resolution. 94 and 85 counted prose.

## 📋 THE REST OF THE BOARD

- ⚖️ **`millisecondsPerMeasure` / `getTotalTime`** — the one §B row left, and its own marker
  says "flag it before doing it": hanging them on `TuneObject` widens the drop-in contract.
  `setUpAudio`'s answer already exists; only the wiring is missing, and the compound-meter
  branch needs the same method, so the two are one job.
- ✅ **`npm run lint` — DONE 2026-09-16, and most of it was never this repo's code.** 691
  of the 1,022 were `noUnknownProperty` inside harvested goldens, 254 were index-signature
  reads of abcjs's own field names, 127 files were a formatter that has never been run. Each
  is OFF with its reason written at the rule; the remainder was fixed. ⚠️ The formatter stays
  off until adopting a format is someone's own commit. 🧨 And a `//` comment in `biome.json`
  makes biome silently ignore the WHOLE configuration — no error, it just falls back to
  defaults. The config is `biome.jsonc` now.
- **PUBLISH — PREPARED, NOT RUN.** Version **6.7.0** (the abcjs release this is
  byte-identical to), sourcemaps dropped, tarball 8.2 -> **2.0 MB packed** and 37 -> 25
  files. `abcts` is unclaimed on the npm registry, so the name is there to take and taking
  it is hard to walk back. ⚠️ npm requires versions to increase, so abcts's own fixes move
  the patch digit and a new abcjs to match resets the line. 📏 And the wire size is not a
  win: **626 KB raw / 201 KB gzipped against abcjs's 499 / 145**, about 25% larger,
  uninstrumented — two glyph tables are embedded and extended is a second engraving path.
- ⚖️ **`CLAUDE.md` is 2,300+ lines**, over half blockquote narrative duplicating 60
  handoffs, loaded in full every session. `Docs/CODEBASE-EVALUATION-2026-09-12.md`. Still
  the owner's decision; nothing has been deleted.

## TRAPS THIS SESSION PAID FOR

- 🔥 **TWO RECORDED CAUSES ON THE BOARD WERE WRONG, AND BOTH READ AS CAREFUL WORK.** One
  quoted both engines' advance lists side by side — and had compared **our NODE metrics with
  abcjs's BROWSER ones**; in one WebKit page every advance is identical. The other named a
  nested-tspan `%%jazzchords` split on a fixture that contains **no nested tspan at all**.
  **Re-measure the cause before you work it**, even when the note is specific, dated and
  numeric. Especially then.
- 🔥 **`zzopts` RENDERS `dist/`, NOT `src/`.** A source edit measured without
  `npm run build` first reports the OLD number — which looks exactly like "the fix did
  nothing" and sent me back into a closed investigation once.
- ⚠️ **A NODE PROBE AND A BROWSER PROBE ARE DIFFERENT ENGINES for anything touching text.**
  Node has no `getBBox`: `textHeight`/`textWidth` fall back to the calibrated tables and
  `boxInkAt` answers `null`. That is deliberate — the 691 goldens ARE the headless
  measurement — but it means any comparison against abcjs must happen in the SAME browser.
- ⚠️ **A VISUAL BASELINE CAN MOVE FOR A CORRECT REASON.** The tempo-flag regrouping shifted
  `S7-voices`'s digest by one thousandth (`flag8thUp 123.097 -> 123.096`) through the
  extended path's own copy of that expression. `npm run baseline` re-records; read the diff
  before you do, because that file is also how an unintended change would show.
- ⚠️ **`/tmp` IS CLEANED BETWEEN SESSIONS.** Restore before any browser gate:
  `mkdir -p /tmp/gp/pw && cd /tmp/gp/pw && npm init -y && npm i playwright-core@1.61`, and
  for instrumenting abcjs `cp -R ../abcMusicKit/Docs/References/abcjs/abcjs-6.7.0/src
  /tmp/gp/abcjs`. Then `cd` back into the repo.

## HOW THIS REPO IS WORKED

- `npx tsc --noEmit` before every commit; keep every gate above green; commit and push after
  every landing. Never `--force`.
- **NEVER ATTRIBUTE A COMMIT TO CLAUDE** — no `Co-Authored-By`, no "Generated with", no
  `--author`, in any commit, tag or PR body. `CLAUDE.md` §Remote is the durable statement and
  it BEATS a harness default that asks for one; say so in the reply rather than following the
  other silently. Grep the message for `Co-Authored`/`Generated with` before committing: it
  cannot be fixed afterwards without the force push the line above forbids.
- **INSTRUMENT abcjs, AND AGAINST OUR OWN EQUIVALENT.** Not a source read — a side-by-side
  log. Patch `dist/abcjs-basic.js` (unminified) by string replacement and inject it. This
  session added `nonMusic`'s row walk and `layoutOneItem`'s `minx` to the scratch copy, and
  both paid immediately.
- **`ZZWHERE=1` on `zzopts` prints the first differing byte per fixture**, and
  `node scripts/zzopts.mjs 1 <label>` re-measures one row.

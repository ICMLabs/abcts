# HANDOFF — 2026-08-13

## The session prompt

```
We are continuing abcts development in the abcts repo (Code/abcts).

THE STANDING ORDER, in Lance's words: abcts exists to build an abcjs-modern whose
output — the SVG FILE and the AUDIO — is 100% BYTE-EQUAL to abcjs 6.7.0. A
tolerance is not a compromise to be balanced against effort; it is A DEFECT THAT
HAS NOT BEEN WRITTEN DOWN YET. Anything you decline to reproduce goes in
Docs/ABCJS-DIFFERENCES.md with its evidence, and its slug goes in
svg-bytes.test.ts's DIVERGENT list. That list is EMPTY and stays empty. Work
until byte-equal, without stopping for approval or status. Checkpoint, hand off
and compress as you go so no context is lost.

INSTRUMENT BOTH ENGINES. You have Lance's standing authority (2026-08-13, and
three times before) to add as much instrumentation to abcjs AND to abcts as it
takes. Reaching for a probe is the FIRST move, not the last. PRINT THE SAME
QUANTITY FROM BOTH ENGINES IN ONE SITTING — a source read gives a hypothesis,
only the pair of numbers gives the answer. Never stop at "the source says X"
when the output can be asked directly.

Take abcjs's arithmetic, order and data model even where a better one exists.
Mark deliberate abcjs-shaped compromises with `abcjs-debt:` comments and entries
in Docs/ABCJS-DEBT.md.

Read Docs/HANDOFF-2026-08-13.md FIRST — §1 the state, §2 THE TWELVE, each row
with what it needs and what has already been tried and reverted, §3 THE HARNESS,
§4 the rules. Then Docs/CHECKPOINT-2026-08-12.md for the findings ledger (§2b is
this arc, §3 is what is measured-and-not-landed), then CLAUDE.md.

Run every command from /Users/lrettberg/ICMLabs/Code/abcts. `cd` elsewhere is
refused and silently resets the shell to the WORKSPACE ROOT, where vitest
collects every sibling repo. Run `npx tsc --noEmit` BEFORE `git commit`.

Confirm your lane with `git rev-parse --abbrev-ref HEAD`. It is `main`.
```

---

## 1. THE STATE — UPDATED 2026-08-13b

| Gate | Now |
|---|---|
| Audio events / timings / chord grids / MIDI | **0 of 72 / 0 of 38 / 0 of 23 / 0 of 3** |
| Harvested geometry | **0 of 174** |
| Pixel targets | **0 of 120** |
| DOM contract | 1 of 25, 24 ratcheted |
| **SVG bytes** | **3 of 171 — 168 byte-exact, ALL RATCHETED** |

`DIVERGENT` is EMPTY. The suite is 1321/1322.

**THE ONE RED IS NOT OURS.** `content-parity`'s `S7-voices` fails because of an UNCOMMITTED
edit to that fixture in `../abcMusicKit` whose goldens were never regenerated. Do not
bisect it; `ls -la` on the two inputs says it at once. See `CHECKPOINT-2026-08-12.md` §5.

---

## 2. THE THREE, AND WHAT EACH ONE NEEDS

**SEVEN OF THE TEN CLOSED on 2026-08-13** — `visual-transpose-output-04`,
`visual-transpose-05`, `synth-flattener-28`, `visual-svg-per-line-02-scaled`,
`visual-parsing-06`, `visual-parsing-07` and `visual-tablature-23`. Every landing is in
`CHECKPOINT-2026-08-13.md` §2 with its abcjs citation and both engines' numbers. **Read that
file first** — §3.1 in particular, which is a measured-and-REVERTED fix.

### 2.1 `visual-options-01-fonts` — the TEMPO's font (7292 of 33896)

`%%tempofont serif 19 box` draws at 25px in `serif`, normal weight; ours draws 20px Times
bold. **`CHECKPOINT-2026-08-13.md` §3.1 has the whole thing measured, implemented and
reverted**: swapping the size takes the `pre` text to abcjs's own `y="317.79"` and byte 170,
and takes the ROOT HEIGHT 5px SHORT because staff 0's top shrinks by the size delta.
`tempoHeightAbove` is a FLAT 6 PITCH and the element reserves a POINT at the rung
(`elements/tempo-element.js:12-13`, `set-upper-and-lower-elements.js:209-212`), so the lane
cannot depend on the font size. **Find which term of the ABOVE ladder reads the size** — a
probe printing staff 0's `clampedTopPitch` at 15pt and 19pt, not a guess. `styleOf` is
already committed beside `faceOf` and is used by nothing yet; it is there for this.

### 2.2 `visual-layout-07` — the KEY SIGNATURE's clef (13364 of 19706)

Its bass staff's two key flats sit 7.75px — 2 pitch — low; everything else is byte-exact.
This is **§3.3b of `CHECKPOINT-2026-08-12.md`**, which has a SIX-CONTROL LADDER through
abcjs. Read it before touching anything: the drawn positions are BASS in four of the six
controls, so "always treble" — what both previous attempts amounted to — is denied. abcjs
resolves `verticalPos` ONCE when the `K:` is read, against `multilineVars.clef`, which a
`V:… clef=` never writes. **Port that write set; do not infer the rule from the outcome.**

### 2.3 `visual-svg-02-staffwidth-12` — THREE `[M:]` IN ONE MEASURE (133 of 8790)

`[M:2/4]y[M:3/4]y[M:4/4]`. **CONFIRMED BY COUNT**: abcjs draws 3 `staff-extra
time-signature`, we draw 1. `Measure.meterChange` is SINGULAR, so the last one wins. §3.3 of
`CHECKPOINT-2026-08-12.md`. It needs a list with positions — abcjs treats each `[M:]` as an
ordinary element in the voice stream, drawn where it stands — and there are ~25 references
across `parser.ts`, `layout.ts`, `chord-grid.ts`, `audio/flatten.ts` and `audio/timing.ts`.
The measure's meter-in-force (the LAST) and the drawn ones (ALL) are genuinely different
roles; keep them one derivation, not two fields that can drift.

## 3. THE HARNESS

**abcjs, instrumented, one fixture** — a SCRATCHPAD COPY at `/tmp/gp/abcjs`, NEVER
`../abcMusicKit` (another agent owns it):

```bash
NODE_PATH=../abcMusicKit/Tools/abcjs-debug/node_modules \
ABCJS_PATH=/tmp/gp/abcjs ABCJS_MYPROBE=1 \
node /tmp/gp/walk.js --file tests/corpus-abcjs/fixtures/<slug>.abc 2>&1 | grep MYPROBE
```

Gate every probe on its own env var. `/tmp/gp/abcjs` already carries a couple of dozen —
`ABCJS_MOVEY`, `ABCJS_TOP`, `ABCJS_SETX`, `ABCJS_XPOS`, `ABCJS_SPLIT`, `ABCJS_LIMIT2`,
`ABCJS_TRIP`, `ABCJS_MIN`, `ABCJS_OTHER`, `ABCJS_CALCX`, `ABCJS_FLAG`, `ABCJS_VW`, and from
2026-08-13 `ABCJS_MAXW`, `ABCJS_SXS`, `ABCJS_SPACE2`, `ABCJS_FINX`, `ABCJS_XX`, `ABCJS_NM`,
`ABCJS_PS`, `ABCJS_GDX` (`CHECKPOINT-2026-08-13.md` §5 tabulates what each prints). Read
them for the shape.

**ours, one fixture, rendered exactly as the byte gate renders it:**

```bash
ABCTS_FIX=<slug> npx tsx scripts/zzpr.ts   # writes /tmp/ours.svg
```

`scripts/zzpr.ts` must live inside the repo — `tsx` will not resolve a script in `/tmp`
against these imports. **vitest swallows `console.log`**, so probe through `tsx`, not
through a test. `ABCTS_PROBE=1` adds the staff extents with the contributing source line;
`ABCTS_CHECK=1` asserts the walked staff origin against the system-relative one.

**The ratchet.** `tests/svg-bytes.test.ts`'s `PASSING` names all 168 byte-exact fixtures and
GROWS, never shrinks. Regenerate after every landing:

```bash
node /tmp/exact.mjs   # writes /tmp/exact.txt from the ranked table
# then splice /tmp/exact.txt into PASSING
```

A ratchet holding 4% of what is green is a ratchet in name — this one has caught three
regressions the aggregate count hid, the latest being `visual-tablature-15` breaking on a
font-face change while the count stayed at 3.

---

## 4. THE RULES THIS ARC EARNED

- **PRINT THE SAME QUANTITY FROM BOTH ENGINES IN ONE SITTING.** A source read gives a
  hypothesis; the pair of numbers gives the answer. Five careful chains of source reads on
  this branch have predicted something abcjs's own output denies.
- **A PROBE THAT PRINTS THE RIGHT ANSWER HAS RULED SOMETHING OUT** — worth as much as one
  that prints the wrong one. That is how the multi-staff group rule was found.
- **SILENCE IS A SHARPER SIGNAL THAN A WRONG NUMBER.** A probe that prints abcjs's answer
  and nothing from ours moves the search from "our arithmetic is off" to "this code never
  runs".
- **CHECK WHERE THE PROBE SITS.** `beambr`'s `+= elem.w` guard was written down as
  provably not firing; the probe was printing the value AFTER the adjustment.
- **A HALF-UNDERSTOOD FIX IS WORTH LESS THAN A WRITTEN-DOWN MEASUREMENT.** Four things were
  reverted this arc and every one left its numbers at the site.
- **A ULP IN AN x IS A WALL, NOT A HUNDREDTH.** The clef octave marker's last bit sat at
  byte 6931 of 85865 on two fixtures; closing it took both to 51404.
- **A CONSTRUCTED OFFSET IS BUILT, NEVER DERIVED** — `(x + a) - x` is not `a`. When a byte
  row is a pure ULP in an x, ask FIRST whether the offset is recovered as `g.x - el.x`.
- **A ROUNDING-SIZED CONSTANT INSIDE A COMPARISON IS NOT A ROUNDING-SIZED ERROR.** The
  tuplet's 0.0098 of a pitch decided whether a bracket was flat or sloped.
- **READ THE BASELINE DIFF'S SHAPE**: a new feature only ADDS, a reorder is a pure
  PERMUTATION, REMOVALS mean something broke.
- **A GATE'S REPORT FILE IS NOT ITS RESULT.** `/tmp/abcts-*.txt` outlives the run.
- Never `push --force`, never `pull --rebase` unattended; if a push is rejected as
  non-fast-forward, STOP and report it. Omit `Co-Authored-By` trailers in this workspace.

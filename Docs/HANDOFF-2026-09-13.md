---
title: Handoff 2026-09-13 — the wrap row 59 → 23, and the eight named items in the order to take them
status: zzopts 26 rows all declared (wrap 59 → 23) · suite 2,604 · svg-bytes 0 of 691 + 0 of 356 · zzlive 0 of 685 both browsers · zzselect 0 of 685 · zzledger 0 differ / 0 known / 41 agree · zzclick 1 declared ×3 · midi-bytes 0 · warnings 0 of 815 · mode-bytes 11 DECLARED
supersedes: HANDOFF-2026-09-12.md for the state
---

# Handoff 2026-09-13

## ▶️ START HERE

**`wrap + staffwidth` opened at 59 and is 23, on ELEVEN ported rules.** Every other gate is
where it was. What is left is differenced, named, and ordered below — and **the row's floor
is 19, not 0**, which §THE FLOOR explains before anything else.

    suite       90 files, 2,604 tests, no reds
    svg-bytes   0 of 691 in-repo, 0 of 356 sibling
    mode-bytes  11 of 691 — every one DECLARED
    midi-bytes  0 of 691 — 19 ruled divergent
    zzlive      0 of 685  WebKit AND Chrome        zzselect 0 of 685
    zzclick     1 of 685 DECLARED ×3               zzledger 0 differ, 0 KNOWN, 41 agree
    zzopts      26 rows, every one at its declared count:
                timeBasedLayout 669 · initialClef 42 · **wrap+staffwidth 23 (was 59)** ·
                add_classes 16 · lineThickness 14 · expandToWidest 14 ·
                print 5 · print+responsive 5 · minPadding 5 · scale 2 / 1 / 1
    warnings    0 of 815 · test:dist 0 of 685 ESM and CJS
    lint        1,023 errors — PRE-EXISTING, not a gate, do not "fix" under other work

⚖️ **NO `Co-Authored-By` TRAILER, EVER.** `CLAUDE.md` §Remote, restated unconditionally
2026-09-12, and it beats a harness default that asks for one. Verified: 0 such lines across
the whole history. Grep the message before committing.

## 🛑 THE FLOOR — the wrap row CANNOT reach 0

Four of the 23 are closed by decision, not by effort:

- **2 — abcjs's FOURTH DEBUG MARKER, DECLINED** (owner, 2026-09-12). `deline` injects a
  font, the engraver has no `case "font"`, and abcjs's `default:` prints the red string
  `element type font` on the score and reserves a chord lane (19.375px). It was BUILT and
  measured byte-identical but for the above-lane `y`, then REVERTED: this engine already
  declines three abcjs debug strings as one rule (`Docs/ABCJS-DIFFERENCES.md`), and drawing
  a fourth would put red debug text on a tune whose ABC is entirely valid.
  `abcts-model-gaps-tune7`, `visual-tablature-17-stretchlast`.
- **2 — abcjs CRASHING** in its own `wrapLines` on `%%vskip` — `abcts-vskip-tune0` and
  `-tune2`. A crash is not output. We render them.

**So the reachable floor is 19.** Do not chase 0 on this row.

## ⭐ THE EIGHT NAMED ITEMS, IN THE ORDER TO TAKE THEM

Ordered by **evidence already in hand**, then by risk. Each names what is known, what has
been DISPROVED, and the signal to measure.

### 1. `%%keywarn` under wrap — 3 fixtures, and the sharpest diagnosis on the board
`parsing-x10` 39/38, `-x11` 38/39, `-x12` 195/187. Notes and bars match exactly; **every
differing element is a key-signature accidental.**

⭐ **The rule is already stated**: abcjs's carried head key is **the last key ALLOWED INTO
THE STREAM**, which is not the key in force. `%%keywarn 0` guards `appendStartingElement` —
the standalone `K:`'s only route in — so no key ELEMENT appears, `lastKeySig` never
advances, and `addLineBreaks` falls back to the DELINED line's own staff key. On `x10`
system 2 abcjs opens with TWO flats, which is **Gm, the header key**, not the `K:F` in force.

⚠️ **DISPROVED, do not repeat**: gating the carry on `keyChangeInline || keywarn` changed
nothing measurable. Stamping `wrapLineHeadKey` on every return path AND treating a section
start as opening a line took `x10` from 39/39 to 39/37 and `x11` from 38/39 to 38/41.

### 2. The wrapped head CLEF — 1 fixture, cause measured
`synth-flattener-20`: **1 of 59 elements**, one octave marker, 72.557px out.
`addLineBreaks` has NO `lastClef` to match `lastKeySig`, so a system cut from a merged run
reprints that run's OPENING clef whatever changed inside it. Measured on
`CDEF|×3 [K:C clef=bass] CDEF|×4`: abcjs's third system opens TREBLE.

⚠️ **DISPROVED**: a `wrapLineHeadClef` carrying the SECTION's opening clef fixes both rungs
exactly and **breaks FOUR ratcheted cases**, `quartet-500` among them. Establish what
`inputStaff.clef` holds PER STAFF first.

### 3. The ending-room pair — 2 fixtures
`timing-06`, `flattener-21`. On `E8| / |1 D8 :|2 C8` the two barlines are 16px apart in
abcjs and 34.5 in ours — **18.5, exactly the `|1` label's `textWidth + 10`**.

⚠️ **DISPROVED**: `minspacing` is spent through a SHORTFALL, which is the `minPadding`
lesson, so moving `endingRoom` from the gap into the LEFT-INK term looks like the port and
**reddens SIX suites including `svg-bytes`** — the unwrapped goldens need the gap form.

### 4. The ULP tail — 3 fixtures, 0 elements moved
`misc-04-stretchlast` and `transpose-06` report **0 of 116 and 0 of 172 moved**: nothing is
misplaced. abcjs writes `408.78100000000006` where we write `408.781`, and `99.28` against
`99.29`. `multi-voice-x03` is one tuplet `3` at dy 0.010. This is arithmetic ORDER, not
geometry — the `(x*s)/s` family this repo has been bitten by three times.

### 5. The sub-0.01 pair — 2 fixtures, essentially closed
`layout-09-endings` 389.23891 against 389.23613; `tablature-20` 247.62746 against 247.62521.
Both were closed STRUCTURALLY this session (element counts match); what is left is the same
ULP family as item 4 and probably closes with it.

### 6. The remaining inside-line geometry — 6 fixtures
`flattener-07` (20/184, a `rests.whole` at dx −0.705), `flattener-46` (15/152, a `dots.dot`
at −0.967), `tablature-24` (67/109), `ledger-gaps-4-tune5` (41/153, first a `path` at dx
−531 — probably a whole system), `selection-03` (54/53), `directives-2-tune3` / `endings-tune2`
(53/55, 31/32). ⚠️ **Difference x POSITIONS, not element sets** — see §THE INSTRUMENTS.

### 7. `%%barsperstaff`'s own splitting defect — NAMED, and NO GATE RENDERS IT
abcjs puts `|:CDEF|` ALONE on line 1 under `%%barsperstaff 2` where we put two measures,
because abcjs counts BAR ELEMENTS there too — the same rule the wrap learned.
`wrapMusicLines` (`parser.ts:3556`) counts measures. ⚠️ **Nothing measures this**, which is
why it has never been seen; a gate has to be built before a fix can be trusted.

### 8. The voice-name wrapped limit — known, load-bearing
Four SOURCE lines with the second voice on one of them, WRAPPED: abcjs `RH X2 RH`, ours
`RH X2 RH X2`. Removing the one-line grace fixes that shape and takes the row **23 → 25**,
so it is load-bearing for the corpus while being wrong here. It is probably compensating for
`lineOfMeasure` mapping a SHORT voice's measures to too few lines — **check that, not the
grace.**

### …and the three arcs that are not on this list
**`timeBasedLayout` 669** is a SECOND LAYOUT ALGORITHM (`layout/layout-in-grid.js`), its own
arc and by far the largest thing here. **`initialClef` 42** — page heights in BOTH
directions, hypothesis `startlimitelem = clef` inside `if (clef)`, ⚠️ its control is MUTE.
**`add_classes` 16**, **`lineThickness` 14** (cause measured, fix scoped, NOT piecemeal),
**`expandToWidest` 14** (needs abcjs's `i = -1` restart; size the re-entrancy first).

## 🔬 THE INSTRUMENTS — build these before diagnosing

Two, and they see different things. **Both were built this session and both paid.**

- **Element-KIND counts.** Group every `path`/`text` by `data-name` and difference the
  counts. Finds a MISSING element where a byte offset only says "spaced differently". It
  turned an undifferentiated 48 into six named families in one sweep, and it is what named
  the standalone-`M:` defect — twelve extra meter DIGITS with systems, bars and `lineBreaks`
  all identical.
- **x/y POSITION diff.** Align the two element lists positionally and report the first
  coordinate that moves. Finds ORDER and PLACEMENT where the kind counts are equal. It found
  a carried ending drawing after the line's dynamics — 4 elements of 690, same coordinates,
  pure order — and the tuplet-side defect that was 108 of 265 moved.

⚠️ **19 of the 23 now draw the same elements at the same page height**, so the kind sweep is
nearly exhausted and the position diff is the working instrument.

## ⚠️ TRAPS — the ones this session actually paid for

- ⭐ **A RULE DERIVED UNDER `{wrap}` MUST BE SHOWN NOT TO FIRE WITHOUT IT.** The voice-name
  grace shipped ungated and regressed the UNWRAPPED path. **NEITHER GATE COULD SEE IT** —
  `svg-bytes` has no fixture of that shape and `zzopts` only renders `{wrap}`. A probe from
  the code-review pass found it, not a gate.
- ⭐ **WRITE THE DELIBERATE BREAK FIRST, NOT LAST.** Four controls were written for
  `ponytail:` markers; **three were MUTE** and agreed with abcjs while a deliberate break
  changed nothing. Two would have been recorded as "measured and correct" without it.
- ⭐ **A RECORDED CAUSE IS A HYPOTHESIS — INCLUDING ONE YOU WROTE AN HOUR AGO.** Three of
  this repo's recorded causes fell this session, two of them written by the session itself.
- ⚠️ **AN AUDIT'S OWN COUNTING NEEDS THE SAME BREAK AS A GATE'S.** The debt ledger's first
  number was 26 open and the real one is 14 — the classifier read only FORWARD from each
  marker and treated `✅ MEASURED` as not a trigger. **The number moved because the tool did.**
- ⚠️ A test can measure the half that was already right; a shared first-three is not a shared
  set; a sample is a lower bound for the WITNESS too; an algebraic identity is not a
  floating-point one; `selectable="false"` is truthy; a floor rots.
- ⚠️ **THE SUITE TIMES OUT UNDER LOAD AND IT IS NOT A DEFECT.** Re-run before believing a red.
- ⚠️ `/tmp` IS CLEANED BETWEEN SESSIONS. Restore playwright with
  `mkdir -p /tmp/gp/pw && cd /tmp/gp/pw && npm init -y && npm i playwright-core@1.61`, and
  abcjs for instrumenting with `cp -R <abcjsRef>/src /tmp/gp/abcjs` — it is CommonJS and
  `require`s straight from node, **and it EARNED ITS KEEP**: two passes over `addLineBreaks`
  could not explain a bar number and one instrumented run found a SECOND WRITE.

## 📋 THE REST OF THE BOARD

- **`Docs/PONYTAIL-DEBT.md`** — 62 markers, 38 with triggers, 10 resolved, **14 genuinely
  open**, triaged by OBSERVABILITY: 8 are NOT observable and should be restated as DECISIONS
  rather than worked.
- **`Docs/CODEBASE-EVALUATION-2026-09-12.md`** — four lenses. Zero runtime dependencies, two
  dead exports, no security findings. ⚖️ **One decision is still the owner's**: `CLAUDE.md`
  is 2,240 lines with 1,355 of blockquote narrative duplicating 56 handoffs, loaded every
  session. Nothing was deleted.
- **`npm run lint`** — 1,023 pre-existing errors, only ever as its own commit.
- **PUBLISH** — metadata in, the npm name `abcts` is FREE, the CLI works. A decision,
  including whether to drop 20.7 MB of sourcemaps from the tarball.

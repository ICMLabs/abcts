# Handoff — 2026-08-06 (second session)

State, findings 106–112 and what is left are in `CHECKPOINT-2026-08-06b.md`.
`CHECKPOINT-2026-08-06.md` keeps findings 104–105 and **THE HARNESS**, which is still the
first tool to reach for. Earlier ledgers: `-08-05c.md` (90–103 and the `ENGRAVE` triage
table), `-08-05b.md` (71–89), `-08-05.md`, `-08-04c.md` (51–70 and the ladder method),
`-08-04b.md` (41–50), `-08-03d.md` (16–40).

---

## THE PROMPT

```
Continue to 100% abcjs parity in the abcts repo (~/ICMLabs/Code/abcts, branch
`geometry/vertical`, git@github.com:ICMLabs/abcts.git). Pushed and green at 703/703.

THE STANDING ORDER: 100% parity with abcjs on EVERY tune — the 41-fixture corpus, the
174-tune harvested corpus, Gonzato, and the audio/MIDI feature set. Work until it is
reached without stopping for approval or status. Checkpoint, hand off and compress as
you go so no context is lost.

FIRST: read Docs/CHECKPOINT-2026-08-06b.md — the state, findings 106-112, and above
all THE GATES CANNOT SEE WHAT IS LEFT. Then Docs/CHECKPOINT-2026-08-06.md for THE
HARNESS, then -08-05c.md (findings 90-103 and the ENGRAVE triage table), -08-05b.md
(71-89), -08-05.md, -08-04c.md (51-70 and the ladder method), -08-04b.md (41-50),
-08-03d.md (16-40), then ARCHITECTURE.md, then CLAUDE.md.

THE RANKED TABLE HAS NO GEOMETRY LEFT ON IT. 173 of 173 measurable fixtures are
exact on all four axes to within 0.05px; the one remaining entry is a tune-count
mismatch with no geometry. So THE TABLE CAN NO LONGER NAME THE NEXT DEFECT, and
that is the single most important thing about this handoff. Every gate is blind to
what is left — the pixel gate sees only what abcjs classes a NOTEHEAD, the
baselines say CHANGED and never WRONG, the structural gate misses everything added
via addOther. The checkpoint has the table.

SO THE FINDINGS NOW COME FROM READING ABCJS, AND A CONTROL TUNE IS THE PROOF.
Finding 111 was written, measured 6 pitch wrong on a four-bar control, and fixed
before it ever touched the corpus — without the control it would have landed under
a green ratchet, because the fixture it was aimed at moved the right way for the
wrong reason. Instrument abcjs in a SCRATCHPAD COPY (the harness is in
-08-06.md; note `node dump-svg.js --file <abs path>` — a bare path is treated as
ABC TEXT and silently renders something else). DO NOT instrument ../abcMusicKit;
another agent is working in that repo.

START WITH `fixVoiceCollisions` (layout/layout.js:140-188, called from :49). It is
measured, named and not implemented: a pass over TIME SLOTS that pushes a rest
clear of the other voice's nearest note by 2 pitch. On
`multi-voice-rest-placement`, two of four rests now land on abcjs's y exactly and
TWO ARE STILL 26.39px AND 10.89px OUT. No gate can see it — rests carry no class
in the pixel gate — so measure it against the golden SVG directly, the way the
checkpoint records.

NEVER RAISE A CEILING TO MAKE A CHANGE PASS. Two are raised and both recorded. The
corpus gate is a RATCHET: an improvement must be recorded in `WITHIN`.

`npx biome check src` is NOT clean and never was — 1 error, 4 warnings, all
pre-existing. Diff against a stash before blaming yourself.

START HERE, EVERY TIME:
  cd /Users/lrettberg/ICMLabs/Code/abcts && npx vitest run tests/corpus-abcjs-ranked.test.ts && cat /tmp/abcts-corpus-ranked.txt

`cd` DOES NOT PERSIST between tool calls. Put the absolute
`cd /Users/lrettberg/ICMLabs/Code/abcts &&` in front of EVERY command. vitest SWALLOWS
console.log on a passing test — use `--disableConsoleIntercept`. DELETE YOUR PROBE
BEFORE COMMITTING; `git add -A` swept one into a commit on 2026-08-06.
```

---

## WHERE TO START

### 1. `fixVoiceCollisions` — the only thing left with a MEASURED number

```js
// layout/layout.js:140-188
var isRealRest  = slot[0].abcelem.rest && slot[0].abcelem.rest.type === 'rest'
var isRealRest2 = slot[lastIndex].abcelem.rest && slot[lastIndex].abcelem.rest.type === 'rest'
if (isRealRest && !slot[lastIndex].abcelem.rest) {
  var distance1 = restTop.bottom - closeTop(slot[lastIndex])
  distance1 -= 2                                    // room between the rest and the note
  if (distance1 < 0 && slot[0].children.length > 0) { … shift top, bottom AND children[0].pitch }
}
```

Note three things before writing any of it:

- It runs on TIME SLOTS after layout, not at creation — the same phase argument that
  governs beams and grace stems.
- It moves `top`, `bottom` **and** `children[0].pitch` together, so it changes the reserve
  as well as the drawn glyph.
- `closeTop`/`closeBottom` skip children of `type === 'chord'`.

Measured gap: `multi-voice-rest-placement`, rests at x 147.79 and 484.75 — ours 83.42 and
52.42, abcjs's 109.81 and 41.53.

### 2. The beam-across-a-rest slice

Unchanged and still the largest single behavioural difference left. See the checkpoint.

### 3. The two rules read but never checked

The whole-measure rest, and `%%barnumbers 0`. Both cheap, both in the checkpoint.

---

## WHAT NOT TO CHASE

- **Anything on the ranked table.** There is nothing on it.
- **The four-step staircase on `visual-layout-04`** — closed by finding 104.
- **`minPadding` and abcjs's `spacing * minSpace > 50` guard** — both measured as 0.
- **The staff ink top as a systemic axis** — 72/73 boundaries exact.
- **An arithmetic order-of-operations difference** (69) and **the staff line's residual
  0.002px** (70). Both ruled out.
- **The 20 biome errors from `npx biome check src tests`** — pre-existing, nearly all in the
  harvested goldens' embedded CSS. And `src` alone is not clean either; see above.

# Handoff — 2026-08-06/07

State, findings 106–124 and what is left are in `CHECKPOINT-2026-08-06b.md`.
`CHECKPOINT-2026-08-06.md` keeps findings 104–105 and **THE HARNESS**, which is still the
first tool to reach for. Earlier ledgers: `-08-05c.md` (90–103 and the `ENGRAVE` triage
table), `-08-05b.md` (71–89), `-08-05.md`, `-08-04c.md` (51–70 and the ladder method),
`-08-04b.md` (41–50), `-08-03d.md` (16–40).

---

## THE PROMPT

```
Continue to 100% abcjs parity in the abcts repo (~/ICMLabs/Code/abcts, branch
`geometry/vertical`, git@github.com:ICMLabs/abcts.git). Pushed and green at 703/703.

THE STANDING ORDER: 100% parity with abcjs on EVERY tune — the 41-fixture corpus,
the 174-tune harvested corpus, Gonzato, and the audio/MIDI feature set. Work until
it is reached without stopping for approval or status. Checkpoint, hand off and
compress as you go so no context is lost.

FIRST: read Docs/CHECKPOINT-2026-08-06b.md — the state, findings 106-124, and
above all THE GATES CANNOT SEE WHAT IS LEFT. Then Docs/HANDOFF-2026-08-06b.md,
then CHECKPOINT-2026-08-06.md for THE HARNESS, then -08-05c.md (findings 90-103
and the ENGRAVE triage table), -08-05b.md (71-89), -08-05.md, -08-04c.md (51-70
and the ladder method), -08-04b.md (41-50), -08-03d.md (16-40), ARCHITECTURE.md,
CLAUDE.md.

READ ABCJS'S CODE. IT HAS THE ANSWERS. This is Lance's correction of 2026-08-06,
restated by him on 08-07 after I ignored it and paid for it: reprinting the key in
force made ragtime WORSE, I guessed at why, implemented the guess, and it changed
nothing. Reading parseKey gave the answer in one pass — `impliedNaturals`, three
lines of source. A MEASUREMENT CAN ONLY RANK HYPOTHESES YOU ALREADY HAVE; THE
SOURCE IS WHERE THE HYPOTHESIS COMES FROM.

BOTH HARVESTED TABLES ARE EMPTY — 0 of 174 rows, no content gaps — and 36 of the
41 are at EXACT ZERO. So NO GATE CAN NAME THE NEXT DEFECT any more. The pixel gate
sees only what abcjs classes a NOTEHEAD; the baselines say CHANGED, never WRONG;
the structural gate misses everything added via addOther; the beam gate had to be
WIDENED before it could express a beam spanning a rest. Findings 113-124 were all
made with the table already empty, and several are exercised by no fixture at all.

SO: read the named function, form ONE hypothesis, and PROVE IT ON A CONTROL TUNE
before touching a fixture. Finding 111 was written, measured 6 pitch wrong on a
four-bar control, and fixed before it ever reached the corpus.

USE THE HARNESS — a SCRATCHPAD COPY of abcjs with env-guarded console.log, written
out in CHECKPOINT-2026-08-06.md. `node dump-svg.js --file <abs path>`; a bare path
argument is treated as ABC TEXT and silently renders something else. DO NOT
instrument ../abcMusicKit — another agent works in that repo, it is DIRTY and it
is NOT you.

START WITH THE CANCELLATION LINE. It is pinned by three controls and not ported;
Docs/HANDOFF-2026-08-06b.md §"WHERE TO START" has abcjs's own per-line key data and
the mechanism. Then ragtime's dx 12.13, whose largest band JUMPS 10.33 between two
adjacent heads — one element's width, not a spread.

NEVER RAISE A CEILING TO MAKE A CHANGE PASS. One is raised and recorded. Nine were
LOWERED on 2026-08-07 and the gate asked for three of them itself. An improvement
must be recorded, or the numbers stop meaning anything.

AND MEASURE BEFORE AND AFTER, NOT AGAINST THE CEILING: EPSILON is 0.05 and several
fixtures recorded at 0.0 measure 0.01-0.03. Run the corpus at your first commit and
again at your last.

`npx biome check src` is NOT clean and never was — 1 error, 4 warnings, all
pre-existing. Diff against a stash before blaming yourself.

START HERE, EVERY TIME:
  npx vitest run tests/corpus-abcjs-ranked.test.ts && cat /tmp/abcts-corpus-ranked.txt

`cd` DOES NOT PERSIST between tool calls, and a `cd` inside a compound command
leaves the shell there. Put the absolute `cd /Users/lrettberg/ICMLabs/Code/abcts &&`
in front of EVERY command. vitest SWALLOWS console.log on a passing test — use
`--disableConsoleIntercept`. DELETE YOUR PROBE BEFORE COMMITTING.
```

---

## WHERE TO START

### 1. `frere-jacques`, dx 21.80 — chased to the parser, not started

The whole of it is ONE system's missing time signature, and the root is `+:` being lexed as
MUSIC in strict. Checkpoint §"WHAT IS LEFT / 6" has the bisect, abcjs's line dump and the
six controls that rule out `P:`, `Q:`, `V:` and five other fields.

**The interesting part is what abcjs does after its own mis-parse.** It still finds a meter
and draws it on the second music line; we lose it outright. Whatever is done here has to
reproduce abcjs's answer, not the reasonable one — `%% example / T: wed` is the second
allowlist entry on this branch to have been written from a plausible reading of abcjs rather
than from its source, and both readings were backwards.

### 2. `little swallow`, dx 21.70 — NOT the same cause

We have its meter. The spread is INSIDE system 1 and it is not a prefix shift: head 0 is
−15.87 where heads 1–2 are −20.73. The corpus's CJK-lyric fixture, and a lyric's width feeds
the element rod.

### 3. `ragtime-nightingale`, dx 13.32 — the oldest number on the list

### 4. THE REMAINING FIXED LANES, then Gonzato, then audio.

`anchorVoltas` (finding 93) is the model, and the checkpoint states the precondition that
made it safe.

---

## WHAT NOT TO CHASE

- **Anything on the ranked table.** There is nothing on it.
- **The four-step staircase on `visual-layout-04`** — closed by finding 104.
- **`minPadding` and abcjs's `spacing * minSpace > 50` guard** — both measured as 0.
- **The staff ink top as a systemic axis** — 72/73 boundaries exact.
- **An arithmetic order-of-operations difference** (69) and **the staff line's residual
  0.002px** (70). Both ruled out.
- **The 20 biome errors from `npx biome check src tests`** — pre-existing, nearly all in the
  harvested goldens' embedded CSS. And `src` alone is not clean either; see above.

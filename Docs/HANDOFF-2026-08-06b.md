# Handoff — 2026-08-06 (second session)

State, findings 106–120 and what is left are in `CHECKPOINT-2026-08-06b.md`.
`CHECKPOINT-2026-08-06.md` keeps findings 104–105 and **THE HARNESS**, which is still the
first tool to reach for. Earlier ledgers: `-08-05c.md` (90–103 and the `ENGRAVE` triage
table), `-08-05b.md` (71–89), `-08-05.md`, `-08-04c.md` (51–70 and the ladder method),
`-08-04b.md` (41–50), `-08-03d.md` (16–40).

---

## THE PROMPT

```
Continue to 100% abcjs parity in the abcts repo (~/ICMLabs/Code/abcts, branch
`geometry/vertical`, git@github.com:ICMLabs/abcts.git). Pushed and green at 703/703.

THE STANDING ORDER: 100% parity with abcjs on EVERY tune — the 41-fixture corpus, the
174-tune harvested corpus, Gonzato, and the audio/MIDI feature set. Work until it is
reached without stopping for approval or status. Checkpoint, hand off and compress as
you go so no context is lost.

FIRST: read Docs/CHECKPOINT-2026-08-06b.md — the state, findings 106-120, and above
all THE GATES CANNOT SEE WHAT IS LEFT. Then Docs/CHECKPOINT-2026-08-06.md for THE
HARNESS, then -08-05c.md (findings 90-103 and the ENGRAVE triage table), -08-05b.md
(71-89), -08-05.md, -08-04c.md (51-70 and the ladder method), -08-04b.md (41-50),
-08-03d.md (16-40), then ARCHITECTURE.md, then CLAUDE.md.

THE RANKED TABLE IS EMPTY — 0 of 174 rows, and CONTENT_GAPS is empty too. The
whole harvested corpus agrees with abcjs on note content and on all four axes to
within 0.05px. So NEITHER THE TABLE NOR THE CONTENT GATE CAN NAME THE NEXT
DEFECT, and that is the single most important thing about this handoff. Every
remaining gate is blind to what is left — the pixel gate sees only what abcjs
classes a NOTEHEAD, the baselines say CHANGED and never WRONG, the structural
gate misses everything added via addOther, and the beam gate had to be widened
before it could express a beam spanning a rest. The checkpoint has the table.

SO THE FINDINGS NOW COME FROM READING ABCJS, AND A CONTROL TUNE IS THE PROOF.
Finding 111 was written, measured 6 pitch wrong on a four-bar control, and fixed
before it ever touched the corpus — without the control it would have landed under
a green ratchet, because the fixture it was aimed at moved the right way for the
wrong reason. Instrument abcjs in a SCRATCHPAD COPY (the harness is in
-08-06.md; note `node dump-svg.js --file <abs path>` — a bare path is treated as
ABC TEXT and silently renders something else). DO NOT instrument ../abcMusicKit;
another agent is working in that repo.

BOTH HARVESTED TABLES ARE EMPTY and 36 of the 41 are at EXACT ZERO. ONE fixture
is left above 0.02: ragtime-nightingale, dx 13.31 over 2009 noteheads.

START THERE, AND HALF OF IT IS ALREADY MEASURED. The prefix reprints `score.key` —
the HEADER key — where abcjs reprints the key IN FORCE, beside a `clefAtMeasure`
that does accumulate. ragtime changes `[K:Ab]`/`[K:Eb]` repeatedly, so its later
systems want four flats and get three: abcjs's key element probes at w=33 against
our 24.25, which is 8.75 and exactly what those systems are out by.

THAT LANDED: dx 13.31 -> 12.13, ox -0.75 -> 0.12. The first attempt reprinted the
key alone and made dx WORSE (14.18), and the ratchet refused it. The missing half
was `impliedNaturals` — on a key change abcjs pushes a NATURAL for every note the
old key had and the new one lacks, copies that list onto the line's key and then
DELETES it, so exactly ONE line cancels. Printing abcjs's per-line key list shows
it in four rows. A MEASUREMENT RANKED THE HYPOTHESES; THE SOURCE SUPPLIED THE
RIGHT ONE.

START WITH THE CANCELLATION LINE, which is PINNED AND NOT PORTED. Three controls
against abcjs's own per-line key data:

  [K:C] at the START of a line   l0 Eb  l1 C+nat  l2 C     l3 C
  [K:C] MID-line                 l0 Eb  l1 Eb     l2 C+nat l3 C
  standalone K:C between lines   l0 Eb  l1 C+nat  l2 C

A `[K:]` BEFORE ANY MUSIC ON ITS LINE BELONGS TO THAT LINE'S PREFIX — `startNewLine`
fires LAZILY, when the first music element is appended, so an inline field parsed
before it is absorbed into the line's own params. That is the same lazy-line
mechanism the standalone `M:` and the bar-number transfer both turned on. We
implement row 2 for every row; fold a change whose `keyChangeSourceRange` precedes
every event of a system-starting measure into the prefix and suppress the inline
draw.

THEN ragtime's dy 0.25 and dx 12.13 — the latter's largest band JUMPS 10.33
between two adjacent heads (golden x 323.1 and 442.9, the y≈4600 system), so it is
ONE element's width, not a spread. And a key-signature GROUP we emit where abcjs
emits none (S6-keys 5/11/7 against 4/12/8; `createKeySignature` returns null for an
empty accidental list), which moves no noteheads.

AND MEASURE BEFORE AND AFTER, NOT AGAINST THE CEILING. `EPSILON` is 0.05 and
several fixtures recorded at 0.0 measure 0.01-0.03. Running the corpus at the
session's first commit and again at its last is the only thing that establishes
which way a change went.

NEVER RAISE A CEILING TO MAKE A CHANGE PASS. Two are raised and both recorded. The
corpus gate is a RATCHET: an improvement must be recorded in `WITHIN`.

`npx biome check src` is NOT clean and never was — 1 error, 4 warnings, all
pre-existing. Diff against a stash before blaming yourself.

START HERE, EVERY TIME:
  cd /Users/lrettberg/ICMLabs/Code/abcts && npx vitest run tests/corpus-abcjs-ranked.test.ts && cat /tmp/abcts-corpus-ranked.txt

`cd` DOES NOT PERSIST between tool calls. Put the absolute
`cd /Users/lrettberg/ICMLabs/Code/abcts &&` in front of EVERY command. vitest SWALLOWS
console.log on a passing test — use `--disableConsoleIntercept`. DELETE YOUR PROBE
BEFORE COMMITTING; `git add -A` swept one into a commit on 2026-08-06.
```

---

## WHERE TO START

### 1. `frere-jacques`, dx 21.80 — chased to the parser, not started

The whole of it is ONE system's missing time signature, and the root is `+:` being lexed as
MUSIC in strict. Checkpoint §"WHAT IS LEFT / 6" has the bisect, abcjs's line dump and the
six controls that rule out `P:`, `Q:`, `V:` and five other fields.

**The interesting part is what abcjs does after its own mis-parse.** It still finds a meter
and draws it on the second music line; we lose it outright. Whatever is done here has to
reproduce abcjs's answer, not the reasonable one — `%% example / T: wed` is the second
allowlist entry on this branch to have been written from a plausible reading of abcjs rather
than from its source, and both readings were backwards.

### 2. `little swallow`, dx 21.70 — NOT the same cause

We have its meter. The spread is INSIDE system 1 and it is not a prefix shift: head 0 is
−15.87 where heads 1–2 are −20.73. The corpus's CJK-lyric fixture, and a lyric's width feeds
the element rod.

### 3. `ragtime-nightingale`, dx 13.32 — the oldest number on the list

### 4. THE REMAINING FIXED LANES, then Gonzato, then audio.

`anchorVoltas` (finding 93) is the model, and the checkpoint states the precondition that
made it safe.

---

## WHAT NOT TO CHASE

- **Anything on the ranked table.** There is nothing on it.
- **The four-step staircase on `visual-layout-04`** — closed by finding 104.
- **`minPadding` and abcjs's `spacing * minSpace > 50` guard** — both measured as 0.
- **The staff ink top as a systemic axis** — 72/73 boundaries exact.
- **An arithmetic order-of-operations difference** (69) and **the staff line's residual
  0.002px** (70). Both ruled out.
- **The 20 biome errors from `npx biome check src tests`** — pre-existing, nearly all in the
  harvested goldens' embedded CSS. And `src` alone is not clean either; see above.

# Handoff — 2026-08-08b

State and findings 147–150 are in `CHECKPOINT-2026-08-08b.md`. `CHECKPOINT-2026-08-08.md`
keeps the ARC DECISION and the audio sizing verbatim — both still exactly right.
`-08-07b.md` keeps 134–146 and the method; `-08-06.md` keeps **THE HARNESS**, still the
first tool to reach for; `-08-07.md` keeps 125–133. Earlier ledgers: `-08-06b.md`
(106–124), `-08-05c.md` (90–103 and the `ENGRAVE` triage table), `-08-05b.md` (71–89),
`-08-04c.md` (51–70 and the ladder method), `-08-04b.md` (41–50), `-08-03d.md` (16–40).

---

## THE PROMPT

```
Continue abcts toward 100% abcjs parity (~/ICMLabs/Code/abcts, branch `main`,
git@github.com:ICMLabs/abcts.git). Pushed and green at 925/925.

STAY INSIDE THE abcts REPO, and commit to `main`. The vendored abcjs under
../abcMusicKit is READ-ONLY reference: instrument a SCRATCHPAD COPY of it, never
that tree, which is another agent's and is dirty.

THE STANDING ORDER: 100% parity with abcjs on EVERY tune. GONZATO IS DEFERRED by
decision (2026-08-08) — do not start it. THE GEOMETRIC TAIL IS CLOSED, so the
work is AUDIO. Work until it is reached without stopping for approval or status.
Checkpoint, hand off and compress as you go so no context is lost.

FIRST: read Docs/CHECKPOINT-2026-08-08b.md — the state and findings 147-150.
Then Docs/CHECKPOINT-2026-08-08.md, which keeps the ARC DECISION and the AUDIO
SIZING and is still exactly right. Then HANDOFF-2026-08-08b.md, -08-07b.md
(134-146 and the method), -08-06.md for THE HARNESS, -08-07.md (125-133),
-08-06b.md (106-124), -08-05c.md (90-103 and the ENGRAVE triage), ARCHITECTURE.md,
CLAUDE.md.

START HERE, EVERY TIME — FIVE gates now, two of them new:
  npx vitest run tests/pixel-parity.test.ts && cat /tmp/abcts-pixel-ranked.txt
  npx vitest run tests/corpus-abcjs-ranked.test.ts && cat /tmp/abcts-corpus-ranked.txt
  npx vitest run tests/above-lane-order.test.ts tests/glyph-ycorr.test.ts

BOTH TABLES ARE EMPTY — 0 of 119 and 0 of 174 — and that is the STATE, not a bug.
The staff-line gate is empty too. So no gate can name a geometric defect, and the
last four were all found the same way: ASK WHAT NONE OF THE GATES CAN REPRESENT,
then BUILD ONE.

AUDIO IS THE ARC AND ITS CORPUS AND ORACLE ARE ALREADY IN THIS REPO. The
implementation is genuinely absent — no `src/midi/`, no `src/synth/`, and `%%MIDI`
appears in the parser ZERO times — but 61 of the 174 harvested fixtures ARE
abcjs's own synth tests (46 flattener, 12 timing, 3 midi, paired in SOURCES.json),
and flattener.test.js is 8,203 lines of expected event lists written as JSON
literals. HARVEST THE ORACLE BEFORE WRITING A LINE OF THE FLATTENER. The parity
surface is EVENT GENERATION — abc_midi_flattener.js, abc_midi_sequencer.js,
chord-track.js; soundfonts and WebAudio are host playback and OUT of scope, the
same split the renderer makes between geometry and glyph outlines.

READ ABCJS'S CODE. IT HAS THE ANSWERS. Every finding of 2026-08-08 was a read of
a named function: `chartable.rest` (147), `setUpperAndLowerElements` (148),
`printSymbol` + `getYCorr` (149, 150).

A LADDER OF CONTROLS, ONE VARIABLE PER RUNG. Nothing in either corpus combines
two above lanes, and a tune with two decorations hides the second inside the
first's stacking cursor. Eleven controls, one per PAIR of lanes, found five
defects up to 27.13px; seventeen controls, one per GLYPH, found ten more.

THE SIGN OF AN ERROR IS EVIDENCE. Our dynamic sat 15.13px above abcjs's ABOVE the
staff and 15.14px above it BELOW — same direction both ways, which no outline
difference can produce, so it was a missing offset. Two fermatas were off by one
pitch in OPPOSITE directions, which nothing but a per-glyph table can produce.
Ask what the error's SIGN and MAGNITUDE rule out before hunting a cause.

A LANE ORDER IS INVISIBLE TO A SUM, and a RESERVE cannot see a DRAW-TIME shift.
Both of 2026-08-08's structural findings hid in a quantity every gate agreed on.

USE THE HARNESS — a SCRATCHPAD COPY of abcjs with env-guarded console.log, written
out in CHECKPOINT-2026-08-06.md, and already built at
/tmp/abcts-probe/abcjs-probe. Copy `dump-elements-char-widths.js` beside
`dump-svg.js` or it throws MODULE_NOT_FOUND. `node dump-svg.js --file <abs path>`;
a bare path argument is treated as ABC TEXT and silently renders something else.

THE PROBE POINTS THAT EARNED THEIR KEEP:
  voice-elements.js, after setX            type/x/w/extraw/minsp/heads
  set-upper-and-lower-elements.js, end     staff.top/.bottom + specialY + positionY
  layout/layout.js, setXSpacing            w / newspace / spacingUnits per iteration
  draw/dynamics.js, drawDynamics           params.pitch and calcY of it
  abstract-engraver.js, createNote         elem.rest + duration + measureLength
And OURS: ABCTS_PROBE=1 prints `PROBE staff … topBy=Lnnnn` and `PROBE item …`.

NEVER RAISE A CEILING TO MAKE A CHANGE PASS. MEASURE BEFORE AND AFTER OVER ALL 119
TARGETS, not against the ceiling. EPSILON is 0.05 and a fixture recorded at 0.0 can
measure 0.03.

`npx biome check src` is NOT clean and never was — same rows before and after,
all pre-existing. Diff against a stash before blaming yourself.

`cd` DOES NOT PERSIST between tool calls, and a `cd` inside a compound command
leaves the shell there. Put the absolute `cd /Users/lrettberg/ICMLabs/Code/abcts &&`
in front of EVERY command. vitest SWALLOWS console.log on a passing test — use
`--disableConsoleIntercept`. The session's probe file is kept at
/tmp/abcts-probe/zz-probe.test.ts; copy it in, and DELETE IT BEFORE COMMITTING.
```

---

## WHERE TO START

```
0 of 119 tunes are off some axis by 0.05px or more
0 of 174 fixtures are off some axis by 0.05px or more
0 of 41 staff lines off abcjs's span
925 / 925 tests
```

**GEOMETRY IS DONE.** Both items of the previous handoff's work order are closed, and the
`ponytail:` notes that deferred to them are retired. What is left is AUDIO, and the arc has
a fixed first step.

### 1. `npm run harvest:audio` — the ORACLE, before any implementation

Extract the `expected*` objects from abcjs's three synth test files and pair them to the
fixtures already on disk. `scripts/harvest-abcjs-tests.mjs` is the model for the
extraction; `scripts/harvest-abcjs-goldens.mjs` currently shells out to `dump-svg.js` and
captures SVG only.

```
46  tests/synth/flattener.test.js     8,203 lines, almost all expected event lists
12  tests/synth/timing.test.js          526 more
 3  tests/synth/midi.test.js
```

The shape is `{tempo, instrument, tracks[]}` around rows like

```js
{ "cmd": "note", "pitch": 67, "volume": 85, "start": 0,
  "duration": 0.125, "instrument": 0, "gap": 0 }
```

### 2. A RANKED TABLE, the third of its kind

Per fixture, the FIRST event that differs. It will start at 61 of 61 and that is the point
— every gate this repo has built was worth more than the fix it enabled, and findings 144,
148 and 150 are the newest proof.

### 3. `%%MIDI` in the parser

Zero occurrences today. `program`, `bassprog`, `chordprog`, `gchord`, `drum`, `percmap`,
`channel`, `transpose`.

### 4. The flattener, against the table

`abc_midi_flattener.js` (31KB) is what the oracle tests; `abc_midi_sequencer.js` (26KB) and
`chord-track.js` (22KB) are the rest of the parity surface.

**WHAT IS NOT PARITY WORK:** `create-synth.js`, `synth-controller.js`, soundfont loading,
WebAudio — ~90KB of host playback. A `{cmd, pitch, start, duration}` list compared against
abcjs's own is the whole gate.

---

## DEFERRED BY DECISION

**GONZATO.** A COVERAGE question, not a geometry one, and the only part of the standing
order whose inputs are not already in this repo. Nothing above depends on it.

## WHAT NOT TO CHASE

- **Either ranked table, or the staff-line gate.** All three are empty. They are
  regression nets now, not instruments.
- **The dynamic glyph's last 0.37px.** Bravura's precomposed `dynamicMF` against abcjs's
  `m` and `f` set side by side, measured as box centres. Same sign above and below, which
  is what proves it is the outline. Outlines are out of scope; the gate asserts the delta
  between lanes exactly and the absolute only to 0.5px.
- **`scripts.stopped`.** The one abcjs glyph still unmapped — it is abcjs's `+`, we draw
  `brassMuteClosed`, and no control has been built for it.
- **The `-classes-tuneN` and `-print-tuneN` goldens.** Same tunes, three times the rows.
- **`minPadding` and abcjs's `spacing * minSpace > 50` guard.** Both measured as 0.
- **An arithmetic order-of-operations difference** (69) and **the staff line's residual
  0.002px** (70). Both ruled out.
- **Glyph OUTLINES.** abcts draws Bravura and abcjs its own font where the two differ; the
  difference is intended and is why abcts's output is smaller. Where a glyph is PLACED is
  in scope — but see finding 141 before writing that off again on any specific row.

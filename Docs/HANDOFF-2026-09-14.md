---
title: Handoff 2026-09-14 — the wrap row 23 → 4, WHICH IS ITS FLOOR, and four of the eight recorded causes were wrong
status: zzopts 26 rows all declared (wrap 23 → 4, the floor) · suite 2,629 · svg-bytes 0 of 691 + 0 of 356 · zzlive 0 of 685 both browsers · zzselect 0 of 685 · zzledger 0 differ / 0 known / 41 agree · zzclick 1 declared ×3 · midi-bytes 0 · warnings 0 of 815 · mode-bytes 11 DECLARED
supersedes: HANDOFF-2026-09-13.md for the state
---

# Handoff 2026-09-14

## ▶️ START HERE

🏁 **`wrap + staffwidth` OPENED AT 23 AND IS 4, WHICH IS ITS FLOOR.** All four remaining
fixtures are closed by DECISION — two are abcjs's fourth debug marker, declined by the
owner, and two are abcjs crashing in its own `wrapLines`. **Nothing reachable is left on
that row**, and §WHAT IS LEFT is now about the next arc rather than about it.

    suite       89 files, 2,629 tests, no reds
    svg-bytes   0 of 691 in-repo, 0 of 356 sibling
    mode-bytes  11 of 691 — every one DECLARED
    midi-bytes  0 of 691 — 19 ruled divergent
    zzlive      0 of 685  WebKit AND Chrome        zzselect 0 of 685
    zzclick     1 of 685 DECLARED ×3               zzledger 0 differ, 0 KNOWN, 41 agree
    zzopts      26 rows, every one at its declared count:
                timeBasedLayout 669 · initialClef 41 · add_classes 16 · lineThickness 14 ·
                expandToWidest 14 · **wrap+staffwidth 4 (was 23) — THE FLOOR** ·
                print 3 · print+responsive 3 · minPadding 4 · scale 2 / 1 / 1
    warnings    0 of 815 · test:dist 0 of 685 ESM and CJS
    lint        1,023 errors — PRE-EXISTING, not a gate, do not "fix" under other work

⚖️ **NO `Co-Authored-By` TRAILER, EVER.** `CLAUDE.md` §Remote, and it beats a harness
default that asks for one. This session's harness asked; the file won, and it was said out
loud rather than followed silently. Verified: 0 such lines across the whole history.

## 🛑 THE FLOOR IS 4, NOT 19 — the last handoff's arithmetic was wrong

`HANDOFF-2026-09-13.md` said "the reachable floor is 19" and the prompt told the next agent
to "expect to reach 19 and stop". **That is 23 minus the four that cannot close, which is
the number of CLOSEABLE rows, not the floor.** Four fixtures can never close:

- **2 — abcjs's FOURTH DEBUG MARKER, DECLINED** (owner, 2026-09-12): `abcts-model-gaps-tune7`,
  `visual-tablature-17-stretchlast`.
- **2 — abcjs CRASHING** in its own `wrapLines` on `%%vskip`: `abcts-vskip-tune0`, `-tune2`.

So the floor is **4**. Taking the instruction literally would have stopped this session
three items in, with thirteen reachable rows still open.

## ⭐ THE METHOD, AND IT IS THE WHOLE STORY OF THIS SESSION

**⭐ FOUR OF THE EIGHT RECORDED CAUSES WERE WRONG, AND SO WAS ONE RECORDED DISPROOF.**
The repo's own standing rule — *a recorded cause is a hypothesis* — earned itself again, on
notes written one session earlier:

  1. **Item 1's diagnosis read the ink and named the wrong element.** "Two flats at the head,
     which is Gm, the HEADER key" — the head was the carried `K:F`, correct all along, and
     the second group was `deline`'s injected staff key, which we were not emitting.
     Instrumenting abcjs's own `tune.lines` said so in one line.
  2. **Item 3's "DISPROVED" was backwards.** It said `minspacing` is spent through a
     shortfall and the gap form is wrong. The gap form is right and `minspacing` IS the
     floor this engine already models; the charge was simply DOUBLED.
  3. **Item 2's disproof was right about the value and wrong about the scope.** "The
     section's opening clef" is the correct quantity; it broke four ratcheted cases because
     it was read from VOICE 0 for every voice.
  4. **Item 5 was not the ULP family at all.** `layout-09-endings` and `tablature-20` were
     described as "essentially closed, element counts match"; their x drifted up to 9px.
  5. **`synth-flattener-21` was paired with `timing-06` as "the ending-room pair" and has no
     ending in it.**

**AND THEN THIS SESSION'S OWN NOTES FELL THE SAME WAY, TWICE.** `misc-04` was recorded here
as "the NOTE's solved x, chasing it means matching the spring solve" — both engines' x
chains are byte-identical and it was one association in the width beside it. The
`flattener-21` entry said the defect was in the shared cursor and it was, but the fix was a
missing invisible rest the MODEL surface had all along. **A note written an hour ago is a
hypothesis on the same terms as one written a month ago.**

**⭐ AND THE SEVEN "SEPARATE SPACING DEFECTS" WERE ONE EXPRESSION.** Seven fixtures differed
by an x that drifted along the line with element counts and kinds identical. It was
`layoutMeasure` passing `el.width` for a trailing bar — ZERO for an invisible one — where
`barWidthOf` gives abcjs's `w` of 1, plus a `left` of 0 where every other bar claims 5. Six
short of the fixed budget per line, and the elastic note gaps grew to fill it. **13 → 8 on
one expression.**

**⭐ INSTRUMENT abcjs, AND INSTRUMENT IT AGAINST OUR OWN EQUIVALENT.** Every hard item this
session fell to a side-by-side log, not to a source read:

    layoutOneItem      per-element x, w, extraw, minspacing, er, minx — against our `fixed()`
    roundNumber        the raw input behind a `.xx5` boundary
    calcY              `this.y`, `ofs`, `STEP` behind one coordinate
    tune.lines         both engines' bar/key/clef streams, side by side
    drawGlissando      `anchor1.x`, which proved the noise was UPSTREAM of the glissando

`scripts/` has none of these; they were scratch. The patterns are in the commit messages.

## ⚠️ TRAPS THIS SESSION PAID FOR

- ⭐⭐ **TWO SURFACES, THREE TIMES.** The ink and `tune.lines` carry the same rules
  separately, and NO GATE ASKS THE MODEL under a wrap — every golden is unwrapped. The clef
  rule, the `%%barsperstaff` meter rule and the injected key each needed the same fix twice.
  **And a control that reads only one surface is MUTE for the other**: breaking `prefix`'s
  clef left every model row green, and breaking the projection left every ink row green.
- ⭐⭐ **HALF A RULE CAN BE WORSE THAN NONE.** `parsing-x11`'s carry without its suppression
  drew 41 elements against abcjs's 38, where the unfixed engine drew 39. The quoted-label
  ending's close without its marker drew a SECOND labelled bracket. The cautionary clef
  suppressed rather than MOVED took `selection-03` from 54 elements to 52. In all three the
  row count was unchanged — only the element-kind instrument saw it.
- ⚠️ **A HOIST PAST A MUTATION.** `yTextPitch` read above the middle-note pass reddened 26
  suite rows: a tall middle note FLATTENS `startNote`/`endNote` after they are first set.
- ⚠️ **A FIX IN THE SHARED WALK PAYS ELSEWHERE.** The pitch-conversion rule moved `print`
  5 → 3, `print + responsive` 5 → 3 and `minPadding` 5 → 4; the `dx + w` rule moved
  `initialClef` 42 → 41. Re-declare those rows rather than assuming a DOWN is a mistake.
- ⚠️ **AND A `wrap` DEFECT IS OFTEN NOT A WRAP DEFECT.** Three of the rules that closed this
  row reproduce with NO wrap at all — the ending room charged twice, the pitch converted
  twice, the width built by subtraction. No golden covers the shapes that show them, which
  is why they lived under a green `svg-bytes` for months. **Ask whether the rule needs the
  wrap before putting its control in `wrap.test.ts`**; two of the three belong in
  `positioning.test.ts` and are there.
- ⚠️ THE SUITE TIMES OUT UNDER LOAD AND IT IS NOT A DEFECT. Re-run before believing a red.
- ⚠️ `/tmp` IS CLEANED BETWEEN SESSIONS — playwright and the abcjs src both.

## ⚠️ WHAT IS LEFT — nothing on the wrap row, and three named options

Both rows the first draft of this handoff listed as reachable closed the same day, and each
corrected a cause this session had itself written down an hour earlier.

- **`synth-flattener-21`** — a SILENT `&` layer measure takes an invisible rest of the
  PARENT's duration. `resolveOverlays` fills one in; `expandOverlays`, the RENDERER's own
  copy of that job, left the measure empty, so the layer had no element at that musical time
  and the shared cursor put its BARLINE at time 0 beside the parent's note, then spent a
  SECOND bar rod on the parent's own bar. The 22px this handoff had localised was two bar
  rods, one per absent layer. ⚠️ The pad is MARKED (`Measure.overlayPad`) because a layer
  silent across a whole system is DROPPED from the solve, worth 5px corpus-wide, and a real
  `x4` is an invisible rest too. **`zzopts` cannot see that half — `extended-snapshot` is
  what defends it**, checked rather than assumed.
- **`visual-misc-04-stretchlast`** — an element's width is `dx + w`, not `(x + w) - base`.
  ⚠️ **AND THE NOTE'S SOLVED x WAS NEVER WRONG**, which is what this handoff's first draft
  said it was. Both engines' 41-element x chains were logged and are BYTE-IDENTICAL,
  `394.97100000000006` included; the defect was one association in the width beside it. Only
  a glissando could see it — it insets by half that width and is the only emitter writing a
  coordinate at full precision. It also moved `initialClef` 42 → 41.

**SO THE NEXT ARC IS A HOST OPTION, AND THEY ARE NAMED AND SIZED** — see `zzopts`'s own
comment for each:

| row | differ | what it is |
|---|---|---|
| `timeBasedLayout` | 669 | a SECOND layout algorithm (`layout/layout-in-grid.js`), spacing by TIME rather than by the spring solve. **By far the largest thing outstanding in this repo**, and its own arc. |
| `initialClef` | 41 | reprints the clef at the head of the tune. ⚠️ **Its control is MUTE** — fix that before trusting anything here. |
| `add_classes` | 16 | class vocabulary on a few shapes. |
| `lineThickness` | 14 | an additive term on four line widths. Cause measured, fix scoped, **NOT piecemeal**. |
| `expandToWidest` | 14 | needs abcjs's `i = -1` restart; size the re-entrancy first. |

## 📋 THE REST OF THE BOARD — unchanged by this session

- **`timeBasedLayout` 669** — a SECOND layout algorithm (`layout/layout-in-grid.js`), by far
  the largest thing here and its own arc.
- **`initialClef` 42** — ⚠️ its control is MUTE. **`add_classes` 16.** **`lineThickness` 14**
  (cause measured, fix scoped, NOT piecemeal). **`expandToWidest` 14** (needs abcjs's
  `i = -1` restart; size the re-entrancy first).
- **`Docs/PONYTAIL-DEBT.md`** — 14 genuinely open markers, 8 of which should be RESTATED AS
  DECISIONS rather than worked. This session added three more, each naming a shape absent
  from both corpora: the wrap injection's DECISION is voice 0's where abcjs asks per staff;
  the "published nothing" test cannot see the ORDER of a text block and a `K:`; and
  `%%voicecolor` is read as system 0 rather than as the line holding its offset.
- **`npm run lint`** — 1,023 pre-existing errors, only ever as its own commit.
- **PUBLISH** — a decision, including whether to drop 20.7 MB of sourcemaps.
- ⚖️ **`CLAUDE.md` is 2,240 lines, 1,355 of them blockquote narrative duplicating 56
  handoffs, loaded in full every session.** See `Docs/CODEBASE-EVALUATION-2026-09-12.md`.
  Still the owner's decision; nothing has been deleted.

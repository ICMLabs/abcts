# HANDOFF — abcts, 2026-09-26

Pick-up point after a pause. The session's detail is in `HANDOFF-2026-09-25.md` (items 1–8)
and `PARITY-STATUS.md` §3b-wrap, §3e, §3f; this file says where things stand and how to resume.

## State — every gate green, nothing undeclared, tree clean at `c60c2097` (pushed)

    suite       128 files, 3,448 tests · corpus 706 tunes, 700 comparable
    svg-bytes   0 of 706 in-repo, 0 of 359 sibling
    zzlive      0 of 700  WebKit AND Chrome        zzselect 0 of 700
    zzopts      26 rows at declared counts (wrap+staffwidth: 4 declared)
    zzledger    0 differ, 88 agree      zzclick 0      zzfuzz 1 of 48 declared
    zzaudio     0 of 5                  test:dist 0 of 700 ESM and CJS
    zzwraplines 2 of 706 declared (abcts-vskip 0/2 — abcjs throws)
    zzelemset   6 of 706 declared (abcjs debug markers)
    speed       ~1.4x abcjs render+layout in WebKit; owner: LEAVE IT (§3f)

## What the last two days did

- **Wrapped `tune.lines`**: gate built (529 of 697 differing), closed by porting
  `addLineBreaks` and running `deline → findLineBreaks → addLineBreaks` over the unwrapped
  projection; ~330 lines of case-by-case code deleted.
- **The playground found three real defects the gates could not see** — the playback cursor
  was handed stand-ins instead of live `<g>` nodes (new gate `zzelemset`); an empty group
  shifted every later `DrawnElement` ordinal (`rangeHighlight` lit the wrong bar); a barline
  after a tempo took a left shift abcjs does not (control `abcts-tempo-before-bar`).
- **Parse −29%** (`deepFreeze` visited set via `isFrozen`). **Gonzato removed** from the
  standing order (owner). The old stash and `geometry/vertical` branch deleted (owner).

## The playground — `~/ICMLabs/Code/abcts-site/` (local, NOT in any repo)

    cd ~/ICMLabs/Code/abcts-site && python3 -m http.server 8123 --bind 127.0.0.1
    http://127.0.0.1:8123/                 abcts        ?engine=abcjs  the same page on abcjs
    http://127.0.0.1:8123/ref/abcjs-full-synth.html   abcjs's own example, unmodified
    http://127.0.0.1:8123/ref/abcts-full-synth.html   the same, engine swapped

`dist` is a symlink to `abcts/dist` — `npm run build` and reload. Its playback setup now
mirrors abcjs's `full-synth.html` exactly. ⚠️ **A cursor-ahead-of-sound drift was reported
and was NOT an engine defect** (PARITY-STATUS §3e): engines identical, reference page fine,
and every configuration — the original included — played in sync after reloads. If it
recurs, capture state BEFORE reloading.

**The playground is the most productive instrument this repo has had lately** — three defects
in two days that 700 tunes of byte gates missed, because it is a CONSUMER: it clicks, plays,
highlights and times. The owner trying their own tunes in it is the likeliest source of the
next finding; compare anything suspicious with `?engine=abcjs` first.

## Open — owner decisions only

1. **Publish** — prepared, not run (`npm publish`, name `abcts`, version 6.7.1).
2. **Bundle size** — ~25% over abcjs over the wire, uninstrumented.
3. **Render speed** — ~1.4x, mostly the SVG-string `innerHTML` parse; owner said leave it.

## Traps (still live)

- ⚠️ `git checkout <file>` to undo an experiment reverts EVERY uncommitted fix in that file —
  back up with `cp`.
- ⚠️ `zzopts`, `zzlive` and friends render `dist/` — `npm run build` first.
- ⚠️ A timer around `renderAbc` alone is unfair to abcts (it forces layout abcjs defers) —
  include `getBoundingClientRect()` for both (§3f).
- ⚠️ WebKit's `performance.now()` is 1 ms-grained; time batches or long tunes.
- `/tmp` is cleaned between sessions — restore `/tmp/gp/pw` (playwright-core 1.61) per
  `NEXT-AGENT-PROMPT.md`. Chrome for Playwright is not installed; `zzlive ENGINE=chrome`
  uses the system Chrome.
- NEVER attribute a commit to Claude (`CLAUDE.md` §Remote) — grep the message first.

## Not abcts — passed to another agent

A Library-search analysis for abcMusicStudio (what the search field covers, how results show,
and a gap where several sections ignore the query but show the "Matching …" banner) was
written in this session by mistake and handed to the owner for the Studio agent. Nothing was
changed in abcMusicStudio.
